# Handoff：P4B-02 AMI 参数列表家族交接（round 233 暂停）

## 目标背景
- 本次交接从当前工作树状态继续，当前 goal 为「继续推进」（maxGoalRounds 256，round 233 暂停，phase 由 active 转为 paused 以等待接续者重新 arm）。
- 近期核心工作集中在 **P4B-02 typed AMI 参数语义家族**（sipi-ami-text crate），按「小而完整的纵向能力切片」节奏推进：每个 goal round 交付一个切片，每个切片自带 charter / source map / verifier / unittest / crosscheck / audit / PLAN 行 / ledger gate。
- 工作区存在大量未提交改动（整个 P4B-02 家族均为工作树状态），**不建议回滚**；请仅基于当前状态继续。

## 当前健康态（round 233 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=331, VALID_KEY=307, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=401, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=349**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：177（02b003..02b179，harness=false，与 runner 文件数一致）
  - `crates/sipi-ami-text/src/parameter_list_*.rs` 模块：85
  - ledger P4B-02b verifier gate 条目：179
  - evidence 文件（p4b-02b*-crosscheck-evidence.v1.yaml）：177
  - PLAN.md：655 行，CRLF 保持（0 bare LF）

## 已完成关键事实（P4B-02 家族，按建议接续顺序）
1. **值级列表族全覆盖（02b83..02b179）**，本会话推进 02b108..02b179：
   - 02b108 occurrence-count、02b109 slice、02b110 index-of、02b111 last-index-of、02b112 remove-all、02b113 keep-only、02b114 split、02b115 rotate-left、02b116 chunk、02b117 head-tail、02b118 window、02b119 run-length-encode、02b120 longest-run、02b121 frequency、02b122 most-frequent、02b123 least-frequent、02b124 is-sorted、02b125 is-strictly-sorted、02b126 is-palindrome、02b127 contains-sublist、02b128 sublist-index、02b129 last-sublist-index、02b130 longest-common-prefix、02b131 longest-common-suffix、02b132 interleave、02b133 inversion-count、02b134 equal-adjacent-count、02b135 distinct-pair-count、02b136 unique-item-count、02b137 duplicate-item-count、02b138 first-duplicate-index、02b139 last-duplicate-index、02b140 multi-remove-all、02b141 multi-keep-only、02b142 lcs-length、02b143 edit-distance、02b144 has-subsequence、02b145 hamming-distance、02b146 starts-with、02b147 ends-with、02b148 intersection、02b149 symmetric-difference、02b150 union、02b151 relative-complement、02b152 multiset-equal、02b153 contains-multiset、02b154 jaccard-index、02b155 dice-index、02b156 overlap-coefficient、02b157 contains-sequence、02b158 tversky-index、02b159 longest-common-subsequence(items)、02b160 all-equal、02b161 min-max-item、02b162 nth-smallest-item、02b163 nth-largest-item、02b164 median-item、02b165 mode-items、02b166 anti-mode-items、02b167 dedup-keep-last、02b168 run-count、02b169 sorted-rank、02b170 longest-run-item、02b171 longest-run-start-index、02b172 adjacent-change-count、02b173 total-equal-pair-count、02b174 majority-item、02b175 is-alternating、02b176 shannon-entropy、02b177 gini-impurity、02b178 normalized-entropy、02b179 mode-frequency
2. **f64 位精确家族**（02b154/155/156/158/176/177/178）：crosscheck 采用 **12 位小数字符串逐位相等**（产品 `format!("{:.12}")`，Python `f"{x:.12f}"`）；f64 枚举仅 derive PartialEq。
3. **机制层（前 checkpoint 保留）**：`crates/sipi-com/src/bathtub_fit_v1.rs`（02h）、`eye_contour_v1.rs`（02i）、`crates/sipi-channel/src/cdr_lock_v1.rs`（04b）。
4. **本会话修复记录**：
   - 02b169 sorted-rank：测试误断言 `(A,a)` 中查询 `"a"` 为 ItemNotFound（实际存在、秩 1），修正为 `Ok(1)` 并新增 `" A "`（不 trim）→ ItemNotFound 断言；实现未改动。
   - 02b178 normalized-entropy：单项目列表 `0.0/log2(1)=NaN` 被单测捕获，修复为显式 `item_count <= 1.0 → Ok(0.0)`（0/0 解析为极限 0）；证据/crosscheck 未改动（复跑仍 4/4）。
5. 每个切片配套治理工件完整（详见下）。

## 治理工件（每个切片一套）
- 实现：`crates/sipi-ami-text/src/parameter_list_<name>_v1.rs`
- runner：`crates/sipi-ami-text/tests/p4b_02bNNN_<name>_runner.rs`（harness=false，`--input`/`--report`）
- 注册：`lib.rs`（mod + pub use 块）、`Cargo.toml`（[[test]]）
- charter：`docs/baselines/p4b-02bNNN-<name>-stage.v1.yaml`（based_on = 前一 charter 列表 + 前一切片路径，逐轮累加）
- source map：`docs/baselines/p4b-02bNNN-mit-source-map.v1.yaml`（mapping 长度 2）
- crosscheck：`tools/run_p4b_02bNNN_<name>_crosscheck.py`（4/4 或 5/5 用例，matched_hash_bound）
- evidence：`docs/baselines/p4b-02bNNN-<name>-crosscheck-evidence.v1.yaml`
- verifier：`tools/verify_p4b_02bNNN_<name>.py`（绑定 charter/source map/evidence/Rust tokens/PLAN 行）
- unittest：`tools/test_verify_p4b_02bNNN_<name>.py`（6 个，含证据篡改拒绝）
- audit：`docs/baselines/audits/2026-08-16-p4b-02bNNN-<name>.md`
- PLAN.md P4B-02 行叙事 + audit 链接；ledger P4B-02 note（'+ ' 前缀）+ gate 条目；`tools/verify_plan_open_items_gate_coverage.py` gates 列表 + `tools/test_verify_plan_open_items_gate_coverage.py` 锚点（现 349）

## 语义锚点（值级原语族，勿改动）
- NotAList / MalformedList（防御，try_new 构造不可达）/ IndexOutOfRange{index,item_count} / ItemNotFound / SublistNotFound / NoDuplicate / LengthMismatch / InvalidChunkSize / InvalidWindowSize / EmptyNewItem / NoMajority
- 空 token `()` 语义：token 层全函数、不再校验（02b100 起）
- 查询项不 trim、列表项 trim（02b0 原始字节绑定）
- 跨切片共享类型勿改：02b90 用自有 `ParameterProfileTypedSubsetMismatchV1`；每个新切片自有 Error enum

## 当前阻塞 / 不可推进项（勿重复实现）
- 需外部资产/决策（已有 gate 看守）：P3C-02 剩余 eye folding/bins（v2 contract 冻结但仓库内无契约工件）、P3B-05 jitter observable（与 sipi-compare prbs9 TIE 重叠）、P3C-03/P5-02/P5-06 剩余（oracle 矩阵/MLSE-DER-CDR warning contract）、P4B-08/09（ADS 资产）、P1-04B/P2-06/P3B-02/P3C-01/P4A-01/02（owner 决策/契约）
- P4B-02 主项仍 open：reserved-name catalog 与 profile 规则 pending；ledger note 尾部记录「a specific AMI profile is fed later (owner B2 authorized ADS)」

## 建议接续动作（优先级）
1. 继续 P4B-02 值级列表族（02b180 起）候选：prevalence-ratio、frequency-normalized（占比）、run-boundaries（游程边界列表）、first-occurrence-indices、is-balanced（值无关位置统计）、pairwise-distinct-adjacent 等；每个仍按「模块+注册→runner+crosscheck→verifier+unittest→charter+source map+audit→updater（PLAN/ledger/coverage gate）→verify+pytest+session health」模板执行。
2. 或转向 profile 层操作（02b44/47/48/64/69/74/76/77/79-82 已有基础，可继续 typed 合并/序列化/指纹等组合切片）。
3. 若继续其它主项，先读对应 charter/evidence 现状再动手（fail-closed）。

## 已知风险与注意事项
- **传输层反斜杠剥离**：write/edit 的 new_string 中 `\` 可能被吞。对策：Python 补丁脚本用 `chr(92)`/`chr(34)`/`chr(39)`/`chr(13)`/`chr(10)`/`chr(96)` 构造；先求值再写行；模板字面量整体重写文件；写含正则的 crosscheck 后必须检查 `repr(line)` 确认 `\|`/`\(`/`\)`/`\s` 幸存。
- **JS 模板字面量中的反引号**：Python 内容含 markdown 反引号会终止模板字面量；用 `chr(96)` 或 `BT` 辅助函数构造。
- **f64 位精确**：比率/熵切片必须 12 位小数字符串比较（逐位一致），不要用浮点近似容差。
- **PLAN.md 必须保持 CRLF 字节级**（655 行、0 bare LF）；updater 脚本写后断言 `out.count(b"\\n") == out.count(b"\\r\\n")`。
- **pwsh tool**：`description` ≤4 词；cargo 不在 PATH 需前置 `$env:Path = 'C:\Users\z3312\.cargo\bin;' + $env:Path`；Windows 控制台 GBK 乱码 UTF-8 —— 用文件中转（`> file 2>&1` 后 read）或 Python `repr()`。
- **命名约定**：policy 常量 `sipi.p4b-02bNNN.<name>-v1.<short>`；evidence schema `sipi.p4b-02bNNN.<name>-crosscheck-evidence.v1`；charter schema `sipi.p4b-02bNNN.<name>-stage.v1`、status `<name>_ported`；source map schema `sipi.p4b-02bNNN.mit-source-map.v1`。
- 严格约束：不放宽容差、不改 golden、不绕过验证门禁；approval prompts disabled（不得设 sandbox_permissions）；每轮验证后清理临时脚本（`_rNNN_*`、`_mk*_charter.py`、`_update*_ami.py`）。

## 便于接续的最小复核命令
- `git status --short`
- `git log --oneline -n 8`
- `python -B tools/verify_session_health.py`（期待 6/6 valid：TOTAL=331, VALID_KEY=307, gates=349, links=401）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（期待 gates=349 锚点）
- `cargo test -p sipi-ami-text --lib`（期待全绿；harness=false runner 仅用于 crosscheck，cargo 直接跑会 panic 属预期）
