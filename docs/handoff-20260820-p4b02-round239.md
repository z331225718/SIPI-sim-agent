# Handoff：P4B-02 AMI 参数列表家族交接（round 239 完成 02b186 first-occurrence-map，未提交）

## 目标背景
- 本轮交付 **02b186 parameter-list-first-occurrence-map**（首现序→首现下标映射表），未提交。
- 02b186 是 02b182 first-occurrence-indices 的 map 形态伴侣：相同数据，不同结构（BTreeMap keyed by item vs ordered Vec of pairs）。

## 当前健康态（round 239 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=338, VALID_KEY=314, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=408, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=356**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：**184**（02b003..02b186，与 runner 文件数一致 184）
  - `parameter_list_*.rs` 模块：**92**
  - evidence 文件：**184**
  - PLAN.md：659 行，CRLF 保持（659 CRLF / 0 bare LF）；ledger 526 CRLF
  - `cargo test -p sipi-ami-text --lib`：**1067 全绿**

## 本会话交付：02b186 parameter-list-first-occurrence-map
- **语义**：`parameter_list_first_occurrence_map_v1` 返回 `BTreeMap<String, usize>`，键为每个 distinct trimmed item，值为该项首次出现的 0-based 下标。02b182 的 map 形态伴侣（相同信息，BTreeMap O(1) 按键查找 vs Vec 顺序迭代）。
- **工件**：Rust 模块（6 单测）+ runner（单值 JSON 输入模式）+ crosscheck 4/4 + verifier + unittest 6 + charter（based_on=185）+ source map（mapping×2）+ audit + PLAN 叙事 + ledger note+gate。
- **一次到位**：无踩坑，遵循 02b184/02b185 确立的单值 runner 模式和命名约定（schema 连字符 / policy 点号）。

## 未提交文件清单（10 新 + 6 改）
- 新增：`src/parameter_list_first_occurrence_map_v1.rs`、runner、crosscheck、verifier、unittest、charter、source map、evidence、audit、handoff
- 修改：lib.rs（+mod+pub use）、Cargo.toml（+[[test]]）、PLAN.md（+叙事）、ledger（note+gate）、coverage gate、test 锚点 355→356

## 建议接续动作
1. **先提交 02b186**：`git add` 上述文件，排除 `_` 前缀临时脚本。
2. **继续 02b187+**：候选切片包括 profile 层操作或更多值级列表原语。
3. 或转向其它主项（先读 charter/evidence 现状）。

## 便于接续的最小复核命令
- `git status --short`（10 新 + 6 改）
- `python -B tools/verify_session_health.py`（6/6：TOTAL=338, VALID_KEY=314, gates=356, links=408）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（gates=356 锚点）
- `cargo test -p sipi-ami-text --lib`（1067 全绿）
