# Handoff: SIPI Channel 统一入口与 ADS 点对点诊断

## Current state

- 唯一交付仓库是 `C:/Users/z3312/code/SIPI-sim-agent`，当前 `main` 与 `origin/main` 均在 `e928396c`。三个上游库的可复用代码已在 SIPI 的 crates 中；外部 Agent-Spice、PyBERT、Agent-COM 只作只读 oracle/reference，不能另起产品入口。
- `sipi channel simulate` 的入口、`sipi-pybert-direct` 的 native workflow 和可选 `physical-voltage-v1` 都在本仓库。默认构建仍隔离 direct candidate；启用 `pybert-direct-integration` 才编译 Channel 候选。
- `crates/sipi-cli/src/channel_native_cli.rs` 新增完整离线报告数据写出：`report.html` 嵌入全部原始 waveform/impulse f64，`channel-report.js` 提供连续窗口、阶段开关、游标和原始样点读数；CSP 只允许同目录脚本，无网络依赖，禁用 JS 仍能看静态首窗。
- `channel-report.js` 是产品输出的一部分，收据的工件列表和 `report_data_policy` 已绑定；报告有 256 MiB 写入上限，避免先复制整份大数组。
- 数值结果没有因报告改动变化。`results/channel-native-browser-20260909/update-verification/result.json` 证明 compat/physical 两种模式的 request、meta、NPZ、CSV（以及 physical 的 frequency-response）与修改前冻结 exe 逐字节一致；两种模式各在 1440x1000 与 390x844 通过完整数据、交互、静态 fallback 和像素检查，共核对 167,936 个浏览器数据值。
- 验证结果：`cargo test --locked -p sipi-cli` 49 项通过；加 `--features pybert-direct-integration` 60 项通过；ADS Python 定向套件 22 项通过；release build 成功。新 exe SHA-256 为 `819534e59777381453bed1215fc6de67ecc3369259ff66bbb39d8acc31750f00`，旧冻结候选为 `021880c643d6a2930f4dffd95adc8419e4c5d845b936b66e137c93fb35b1ff19`。
- ADS 方面已完成固定电路/源/门限的四档时间步长研究：`dt/16`、`dt/64`、`dt/256`、`dt/1024`，每档五例双重复。原始观察键在 `dt/256` 起通过，但“分数延迟 + RC”在全部原生 ADS 时点于 `dt/1024` 仍有 2,432 个失败点、最坏误差/门限约 1.96；`acceptance=false` 保持。审计在 `results/channel-ads-primitives-20260909/integration-convergence/`，没有继续盲扫更细步长。
- 新校验器兼容旧报告与新 `channel-report.js` 工件。`matched-native-grid` 双重复 ADS smoke 的 `numerical_status=passed`，但 `complete_bench_verified=false`；旧 v3 七例 14 次复核仍 `numerical_status=failed`，没有被显示层升级。
- 本轮没有提交或推送新的改动。工作树有既有修改和研究产物，提交前必须逐项审阅 `git status`，不要把外部/实验结果误当产品发布内容。

## Next steps

1. 先审阅当前 SIPI diff 和工件边界，必要时补一笔针对报告工件的 commit；只有用户明确要求时再 push。保持 `acceptance=false`，不重写冻结结果。
2. 回到 Channel 数值主线：把统一入口从 metallic-line/impulse-response 扩展到完整 Touchstone 网络工作流。优先补 typed 的端口映射、参考阻抗、频率单位/网格、DC/Nyquist 覆盖、passivity/reciprocity/因果性诊断；不只取 S21，不隐式插值/对齐/缩放。
3. 实现并测试网络级联与一次最终 FD-to-TD：先用直通、纯延迟、失配和零耦合解析例，再接 B2 high-loss S4P、B3 TX/channel/RX fixture。每个阶段导出原始节点、完整频率键、kernel 和波形，明确哪些只是诊断而不是验收。
4. 依次推进 B4 EQ、B5 DFE/CDR（训练/锁定/冻结）、B6 noise/jitter/三眼统计与 BER contour、B7 AMI/PAM4/NEXT/FEXT。复用已有 Rust 内核，先固定输入和观察窗口，再增加优化/统计，不重写已有 PRBS/FFT/CTLE/DFE。
5. ADS bench 的 candidate 始终调用 SIPI `sipi.exe`；保留双重复、SDK 重导出、全量 CSV 和原始点对点门限。物理模式的有限带宽 IFFT/sample-hold 不得宣传为连续时间 ADS parity；不要因为 AC 或参考控制通过就关闭 TD 失败。
6. Windows 交付仍需真实干净机验证：源码构建需要 Rust + MSVC C++ Build Tools，运行已构建 exe 可能需要匹配的 VC++ v14 Redistributable；`README.md` 与 `tools/build_windows.ps1` 是当前说明入口。

## Relevant artifacts

- `README.md` — Windows 构建、单一 SIPI CLI 和依赖边界。
- `docs/channel-native-workflow.md` — Channel CLI、物理模式、离线报告和 ADS 入口。
- `docs/channel-native-ads-diagnostics-20260909.md` — ADS 分层诊断、带宽隔离、原生时点和时间步长收敛证据。
- `docs/agent-bert-channel-bench-20260905.md` — B0-B7 范围、历史约束和逐层验收合同。
- `docs/agent-bert-channel-roadmap-20260905.md` — TXF-01 辅助边界及 measured-TX、PAM4、级联/串扰、EQ/CDR 后续方向。
- `docs/adr/ADR-015-upstream-capability-first-rust-consolidation.md` — 三库整合、外部 oracle 和 Rust promotion 边界。
- `crates/sipi-cli/src/channel_native_cli.rs` / `crates/sipi-cli/src/channel_report.js` — 统一入口与离线报告。
- `crates/sipi-pybert-direct/src/physical_channel.rs` / `crates/sipi-pybert-direct/src/touchstone.rs` — 当前物理通道与 Touchstone 解析候选。
- `tools/run_channel_native_ads_bench.py` / `tools/verify_channel_native_ads_bench.py` — ADS candidate 运行与证据复核。
- `results/channel-native-browser-20260909/update-verification/result.json` — 报告改动不影响数值的审计。
- `results/channel-ads-primitives-20260909/integration-convergence/result.json` — 四档时间步长收敛审计。

## Suggested skills

- `code-review` — 提交前检查当前大 diff、工件边界和未提交研究文件。
- `tdd` — 为 Touchstone 端口映射、级联和 B2/B3 逐点合同先写解析例测试。
- `webapp-testing` — 修改离线报告控件或新增 Channel 视图时复跑桌面/移动端与像素检查。
- `ponytail` — 控制新增抽象和依赖，优先复用现有 Rust Channel/Touchstone/分析内核。
- `handoff` — 下一阶段若再次切换会话，继续更新本目录交接文档。
