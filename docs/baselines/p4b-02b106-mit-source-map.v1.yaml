{
  "schema": "sipi.p4b-02b106.mit-source-map.v1",
  "status": "parameter_list_join_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_join_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "list value join over validated AMI parameter values" },
  "mapping": [
    { "concept": "list value join", "rust": "join_parameter_list_values_v1", "notes": "concatenates trimmed items of two List values, duplicates preserved, rejoined with ", "" },
    { "concept": "join errors", "rust": "ParameterListJoinErrorV1", "notes": "NotAList when either value is not List, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
