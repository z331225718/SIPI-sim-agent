{
  "schema": "sipi.p4b-02b178.mit-source-map.v1",
  "status": "parameter_list_normalized_entropy_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_normalized_entropy_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "normalized Shannon entropy (evenness) over the empirical frequency distribution of a validated AMI parameter list value" },
  "mapping": [
    { "concept": "normalized entropy", "rust": "parameter_list_normalized_entropy_v1", "notes": "Shannon entropy bits divided by log2(len) over distinct trimmed items by raw byte equality, f64 in [0, 1]" },
    { "concept": "evenness errors", "rust": "ParameterListNormalizedEntropyErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}