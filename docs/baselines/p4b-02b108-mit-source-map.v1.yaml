{
  "schema": "sipi.p4b-02b108.mit-source-map.v1",
  "status": "parameter_list_occurrence_count_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_occurrence_count_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "list item occurrence counting over validated AMI parameter values" },
  "mapping": [
    { "concept": "item occurrence count", "rust": "count_parameter_list_item_occurrences_v1", "notes": "counts trimmed items equal to the raw query item by byte equality" },
    { "concept": "count errors", "rust": "ParameterListOccurrenceCountErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
