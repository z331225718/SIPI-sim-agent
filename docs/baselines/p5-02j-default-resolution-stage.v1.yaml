{
  "schema": "sipi.p5-02j.default-resolution-stage.v1",
  "status": "default_resolution_scalar_catalog_ported",
  "purpose": "P5-02j value-consumption / default-resolution stage: resolve a default-rule expression from a consumed workbook parameter surface into a deterministic value category. Port of agent_com config.materialize._resolve_default plus the literals._scalar arithmetic evaluator. Scalar-catalog categories only: scalar literals, derived scalar arithmetic, booleans, simple strings, empty literals, and the DFE-lower-limit special case. Multi-element array/matrix defaults (snpPortsOrder [1 3 2 4], R_diepad [50,50], ts_sample_adj_range [0 0], string-matrix pkg_Z_c) need a vector layout and are deferred to a dedicated slice.",
  "source_map_ref": "docs/baselines/p5-02j-mit-source-map.v1.yaml",
  "admission": {
    "scalar_literals": true,
    "derived_scalar_arithmetic": true,
    "booleans": true,
    "simple_strings": true,
    "empty_literals": true,
    "dfe_lower_limit_special": true,
    "vector_matrix_defaults": false,
    "behavior_profile": false,
    "release_evidence": false,
    "product_runtime_invoked": false
  },
  "non_claims": ["not_vector_matrix_defaults", "not_behavior_profile", "not_profile_acceptance", "not_release_evidence"]
}
