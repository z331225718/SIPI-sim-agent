{
  "schema": "sipi.p4b-02b125.mit-source-map.v1",
  "status": "parameter_list_is_strictly_sorted_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_is_strictly_sorted_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "strict sortedness check over validated AMI parameter list values" },
  "mapping": [
    { "concept": "strict sortedness check", "rust": "parameter_list_is_strictly_sorted_v1", "notes": "reports whether adjacent trimmed items are in strictly increasing or strictly decreasing byte order" },
    { "concept": "order errors", "rust": "ParameterListIsStrictlySortedErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
