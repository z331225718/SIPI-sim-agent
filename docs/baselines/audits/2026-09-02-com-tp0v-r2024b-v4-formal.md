# COM TP0V R2024b v4 formal record

This record freezes four fresh replay receipts created after the v4 tolerance
contract was committed and after candidate `b255967c` was archived. Two use
the pinned MATLAB R2024b core; two build and invoke the public Rust
`sipi com run` route. Each receipt contains two exact internal repeats.

The aggregate passes the 12-value scalar surface, exact MAT cache bridge,
ordered raw normal-ERL vector comparison for package case 0 / port 1, and the
strict Rust-faster timing gate. Raw vectors are compared without fitting,
alignment, resampling, interpolation, truncation, or delay correction.

This is selected-profile evidence only. It does not claim other workbooks,
complete Agent-COM coverage, or a TD_ILN result. TP0V has
`COMPUTE_TDILN=0`, so D3 remains globally open and is explicitly not treated
as a zero or an alias.
