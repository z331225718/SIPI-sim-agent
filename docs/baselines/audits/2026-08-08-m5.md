# M5 Audit Record

## M5A-06a (Agent-Spice migration preflight)

- Commit: `db3889a` (`feat(m5a): lock agent-spice migration preflight`).
- Source anchor: `agent-spice@2cc92316c2fb89a159f18fcb1ff2ba249f0e22f5`, tree
  `b6bde97128030d6cea0d68b2f0a35d807be8c402`; baseline tag
  `sipi-baseline/agent-spice/20260807.2` resolves to
  `80f66a384b1b4b52b91390dc40f12a7af6cf0e2a` and is an ancestor.
- `docs/baselines/migrations/m5a-agent-spice-preflight.v1.json` fixes the
  target prefix and keep/drop policy. It retains the production Python/Rust
  sources, build metadata, docs, tests, and C# reference while excluding
  `third_party`, external-reference assets, local native binaries, generated
  output, and unrelated tooling history.
- `tools/verify_m5a_agent_spice_preflight.py` validates source commit/tree,
  baseline commit/tree/ancestry, evidence SHA-256 values, path policy, and
  restricted-path exclusion. The real source report was ready with 371 retained
  paths and 651 dropped paths. `tools/test_m5a_agent_spice_preflight.py` covers
  ready, missing restricted drop, and source tree mismatch cases.
- `license-manifest.v1.yaml` records the M5A source anchor, aligns the
  production source scope, refreshes the Rust crate evidence hash, and marks
  locally built native binaries `external_reference_only`.
- Verification: `tools/verify_m5a_agent_spice_preflight.py --source
  C:\Users\z3312\code\agent-spice`, `tools/verify_license_manifest.py`, and
  `tools/run_all_tests.py` (55/55) passed.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_681a317f48a4`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_668804ab5a9b`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-06b may create and filter its temporary clone, but
must rerun this preflight before merging the rewritten history.

## M5A-06b (Agent-Spice filtered-history import)

- Commit: `c89bb90` (`feat(m5a): import filtered agent-spice history`), a merge
  of the SIPI preflight lineage and filtered temporary-clone head
  `b96f0aa19ec1d0800c76550d3552693fcbc3026f`.
- Filtering was performed in a temporary clone outside the user Agent-Spice
  source worktree. The source anchor `2cc92316c2fb89a159f18fcb1ff2ba249f0e22f5`
  maps to `b96f0aa19ec1d0800c76550d3552693fcbc3026f`.
- `docs/baselines/migrations/history-map.v1.json` records 369 commit mappings,
  the source and baseline commit/tree evidence, the baseline tag mapping, the
  preflight SHA-256, keep/drop/port disposition, and the five inherited
  documentation whitespace diagnostics. The diagnostics are preserved to keep
  imported source content byte-equivalent; they do not affect code, numerics, or
  golden artifacts.
- The imported tree contains 371 paths, all below `engines/agent-spice`; the
  staged path set exactly matched the filtered tree. `third_party`, `artifacts`,
  `pdn_analysis`, `pdn_analysis_output`, and
  `src/agent_spice/lib/native` were not imported.
- Verification: preflight report ready; history-map JSON validation (369
  mappings); `git fsck --no-dangling --no-reflogs`; Rust `cargo fmt --check`,
  `cargo clippy --locked -- -D warnings`, and `cargo test --locked` (20 tests);
  `tools/run_all_tests.py` (55/55). Independent cross-audit also recomputed the
  371-file set and SHA-256 content equality with zero mismatches.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_0eb999b37d12`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_094ab60aab3c`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-07 may move the imported Rust crate with Git history
preservation, must leave no duplicate crate, and must validate baseline-tag
artifact equivalence.

## M5A-07 (Circuit crate history-preserving move)

- Commit: `2bf67ae` (`feat(m5a): move circuit crate into native workspace`).
  The 22-file crate was moved from
  `engines/agent-spice/native/agent-spice-sim` to
  `native/crates/sipi-circuit`; the old path has no tracked crate files.
- The crate keeps `agent-spice-sim` package/binary and `agent_spice_sim`
  library names. Only `tests/determinism.rs` and `tests/numeric_parity.rs`
  changed, adjusting their `CARGO_MANIFEST_DIR` fixture traversal for the new
  path; all algorithm, resolver, and golden content is unchanged.
- `docs/baselines/migrations/m5a-agent-spice-move.v1.json` and
  `tools/verify_m5a_agent_spice_move.py` verify imported ancestry, source and
  target trees, equal 22-file sets, the exact two-file adjustment allowlist,
  no duplicate tracked path, and retained package identity.
- Baseline-tag artifacts were built in an isolated temporary worktree with
  fixed build-info environment. Raw PE bytes differ only in linker metadata;
  after normalizing COFF/debug timestamps and CodeView RSDS GUID, both hashes
  are `6C76276C73D54B07CBAAD9493EA0CC09D4A48FF166ABA724A3BD670128B708C8`.
  `build-info --json` is identical and pins the baseline source revision.
- Verification: Rust `cargo fmt --check`, `cargo clippy --locked -- -D
  warnings`, and `cargo test --locked` (20 tests); move verifier including
  artifact evidence; `git fsck --no-dangling --no-reflogs`; and project
  `.venv` `tools/run_all_tests.py` (56/56).
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_cf390620a45e`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_9fc7ca8b8277`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-08 may take over the Agent-Spice Python production
package while preserving its namespace and compatibility CLI surface.

## M5A-08a (Agent-Spice source-runtime reconnection)

- Commits: `82b4b0a` (`fix(m5a): reconnect agent-spice source runtime`) and
  `ba4abcd` (`fix(m5a): preserve circuit move evidence`). The production
  package remains under `engines/agent-spice` with the `agent_spice` namespace
  and fit/RFM/HSPICE CLI surface unchanged.
- The development Rust-engine candidate now resolves from the single moved
  crate at `native/crates/sipi-circuit/target/release`. Package-local wheel
  candidates and the C# fallback retain their established ordering; this slice
  adds no second resolver and changes no numerical algorithm or golden data.
- Tests, oracles, license-manifest references, and operator documentation now
  use the moved-crate location. Documentation explicitly does not make a
  Linux/macOS certification claim.
- OMP found that the first commit modified the moved crate README and thereby
  invalidated M5A-07's fixed-tree evidence. `ba4abcd` restores that README to
  its exact `73b43f3` blob rather than weakening the M5A-07 verifier. The move
  verifier reports `ready: true` and `tools/test_m5a_agent_spice_move.py`
  passes 2/2.
- Verification: Rust fmt/clippy/test/build, direct source-runtime resolution,
  the move verifier, and project `.venv` `tools/run_all_tests.py` (56/56)
  passed. Two existing Agent-Spice `.lin` tests remain red in the original
  `agent-spice@2cc92316` source worktree as well, so they are recorded as
  pre-existing rather than a regression from this slice.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, initial
  P1 `msg_cd997e888d42`, remediation review `msg_bb2faa492e3e`, final 0 P1 /
  0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_f1d27325f61c`, final 0 P1 / 0 P2.

Conclusion: accepted. M5A-08 remains open for its packaging/provenance work;
this slice only reconnects source-tree execution without changing the public
compatibility contract.

## M5A-08b (Windows x64 compatibility-wheel gate)

- Commit: `aceb252` (`test(m5a): gate staged agent-spice wheel`).
  `tools/verify_m5a_agent_spice_wheel.py` builds the one Rust engine from
  `native/crates/sipi-circuit`, stages it only in a temporary directory, and
  builds the `agent_spice` Windows x64 compatibility wheel from that copy.
- The gate verifies the `py3-none-win_amd64` wheel tag, native package path,
  isolated wheel installation, release `build-info`, and the `rc.cir` native
  smoke result (8 points). All wheel, venv, and smoke artifacts are temporary;
  no binary is added to the tracked source tree.
- The root CI now has a Windows-only Agent-Spice wheel job. The report labels
  `certification: not_claimed`, creates no promotion manifest, and makes no
  Linux/macOS certification claim.
- Verification: `tools/verify_m5a_agent_spice_wheel.py` passed; project
  `.venv` `tools/run_all_tests.py` passed 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_3d380fd32f6e`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_7a90a7220fef`, 0 P1 / 0 P2. Its use of `uv run` refreshed the unrelated
  pre-existing `sipi-runtime` lock metadata; that generated side effect was
  restored to `HEAD` and is not part of this slice.

Conclusion: accepted. M5A-08 is complete: the imported Agent-Spice production
package retains its namespace and compatibility CLI surface, resolves the
single moved Rust engine in source development, and has a Windows x64 staged
wheel gate. M5A-09 is next.

## M5A-09a (Move-evidence evolution anchor)

- Commit: `fb81b6e` (`fix(m5a): anchor circuit move evidence`).
  `docs/baselines/migrations/m5a-agent-spice-move.v1.json` now records the
  history-preserving target-move commit `2bf67ae`.
- `tools/verify_m5a_agent_spice_move.py` evaluates the imported tree, target
  file set and blobs, old-source removal, path-adjustment allowlist, and Cargo
  identity at that recorded commit rather than at the evolving `HEAD`. It
  fails closed unless the recorded target remains an ancestor of `HEAD`.
- This preserves M5A-07's historical-move evidence while permitting subsequent
  M5A-09 clean-room work in the moved crate. It changes no numerical algorithm,
  resolver ordering, or golden data.
- Verification: direct move verifier ready; `tools/test_m5a_agent_spice_move.py`
  2/2; project `.venv` `tools/run_all_tests.py` 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_4ddfc41d2688`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_c7fba497146e`, 0 P1 / 0 P2.

Conclusion: accepted. The M5A-07 evidence gate continues to protect the
historical move and no longer blocks clean-room M5A-09 evolution.

## M5A-09b (Clean-room IBIS structural scanner)

- Commit: `e1cb1bd` (`feat(m5a): add clean-room IBIS scanner`). Added the
  standalone MIT `native/crates/sipi-ami` Rust crate, authored from public
  IBIS/AMI specifications without importing, executing, translating, or
  inheriting PyAMI or PyBERT code.
- The initial, explicitly limited API structurally scans `[IBIS Ver]`-headed
  IBIS text into ordered keyword sections and records. It preserves values,
  records, and one-based physical source lines; handles unquoted `|` comments
  while retaining quoted data; and rejects missing, out-of-order, valueless, or
  malformed headers fail-closed. It does not claim complete IBIS/AMI semantic
  coverage, DLL-host support, or platform certification.
- CI adds a Windows Rust job that runs fmt, clippy with warnings denied, and
  locked unit tests for this crate. No numerical algorithm, resolver, or golden
  data changed.
- Verification: Rust fmt and clippy with `-D warnings`; `cargo test --locked`
  4/4; project `.venv` `tools/run_all_tests.py` 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_08832a62d334`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_d2ad2dd86249`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-09 may add AMI parameter-tree semantics on this
clean-room structural foundation.

## M5A-09c (Clean-room AMI parameter semantics)

- Commit: `baa3e52` (`feat(m5a): add AMI parameter semantics`). The
  `sipi-ami` crate now parses AMI parenthesized parameter records into a
  source-ordered, line-aware tree, preserving atoms, quoted values, and nested
  expressions without model-specific coercion.
- `AmiHostMetadata` validates the host-facing `AMI_Version` string and the
  `Init_Returns_Impulse` / `GetWave_Exists` Boolean declarations. Missing,
  malformed, multi-valued, incorrectly typed, or non-Boolean values fail
  closed with structured errors. This is limited host metadata interpretation,
  not a claim of complete AMI semantic coverage or DLL-host capability.
- Syntax checks cover comments, quotes/escapes, parentheses, required names,
  physical line numbers, and malformed input. The clean-room boundary remains
  explicit: no PyAMI/PyBERT source, runtime dependency, or translation is used.
- Verification: Rust fmt and clippy with `-D warnings`; `cargo test --locked`
  8/8; project `.venv` `tools/run_all_tests.py` 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_8585037c3705`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_88a35cd32cca`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-09 may add the `AMI_Init` / `AMI_GetWave` pulse
response contract on this clean-room parameter foundation.

## M5A-09d (AMI waveform contract)

- Commit: `d2fb9b1` (`feat(m5a): define AMI waveform contract`). The
  clean-room crate now defines explicit, non-numerical request and response
  boundaries for `AMI_Init` and `AMI_GetWave`; this remains a contract for a
  later DLL adapter, not a vendor ABI implementation.
- The contract rejects non-positive or non-finite sample intervals, empty or
  non-finite waveforms, capability/output mismatches, and output waveform
  shape mismatches. `GetWave_Exists` is enforced before the host boundary;
  clock-time values are required to be finite. No channel solver, resolver,
  golden data, Python host, or platform-certification claim was added.
- Verification: Rust fmt and clippy with `-D warnings`; `cargo test --locked`
  12/12; project `.venv` `tools/run_all_tests.py` 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_eb99663145f7`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_6ff99174d02d`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-09 may add the Rust `libloading` vendor DLL host on
this validated clean-room contract.

## M5A-09e (AMI DLL entry-point loader)

- Commit: `b0acc8e` (`feat(m5a): load AMI DLL entry points`). The clean-room
  crate now uses Rust `libloading` to own an AMI executable library and resolve
  the public C ABI declarations for `AMI_Init`, `AMI_GetWave`, and `AMI_Close`.
- `AMI_Init` and `AMI_Close` are mandatory; `AMI_GetWave` is required only
  when parsed `GetWave_Exists` metadata says it is available. Missing required
  symbols fail closed. The retained `Library` is private and outlives all
  retained ABI pointers. This slice resolves symbols only: it bundles no vendor
  DLL, invokes no vendor model, and makes no vendor or cross-platform
  certification claim.
- Verification: Rust fmt and clippy with `-D warnings`; `cargo test --locked`
  13/13, including a Windows system-library rejection check; project `.venv`
  `tools/run_all_tests.py` 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_6aaefa1e8387`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_db90ed6d7019`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-09 may add the owned model lifecycle and
`AMI_Init` / `AMI_GetWave` invocation adapter on this resolved ABI foundation.

## M5A-09f (AMI Init lifecycle)

- Commit: `f3b21e0` (`feat(m5a): invoke AMI init lifecycle`). `AmiModel` now
  owns a resolved library plus one model-memory lifecycle, invokes the public
  nine-argument `AMI_Init` ABI for the primary impulse column, and adapts the
  modified buffer through the existing typed contract.
- Non-finite/non-positive bit times, parameter strings with an interior NUL,
  non-success model statuses, invalid returned strings, contract errors, and
  lifecycle reentry fail closed. All initialization failure paths close any
  returned memory handle; `close` prevents double-close and `Drop` is a final
  cleanup guard. Model-owned strings are copied but not freed by the host.
- Verification: Rust fmt and clippy with `-D warnings`; `cargo test --locked`
  15/15 with injected ABI fixture coverage; project `.venv`
  `tools/run_all_tests.py` 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_d27b5d34edd4`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_30760add2b23`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-09 may add the optional `AMI_GetWave` adapter on
this validated initialization lifecycle.

## M5A-09g (AMI GetWave lifecycle)

- Commit: `721613b` (`feat(m5a): invoke AMI GetWave lifecycle`). The owned
  host now calls the optional public five-argument `AMI_GetWave` ABI only after
  parsed metadata and a successful initialization permit it.
- It preserves the initialization sample interval, rejects incompatible calls
  before FFI, preinitializes caller-owned clock storage with the public `-1`
  terminator, returns only values preceding that terminator, and rejects
  non-finite or invalid negative clocks. Any post-FFI failure closes the model
  and permanently retires the instance rather than reusing uncertain state.
- Verification: Rust fmt and clippy with `-D warnings`; `cargo test --locked`
  17/17 with injected GetWave/sentinel coverage; project `.venv`
  `tools/run_all_tests.py` 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_aa1d01cf6fd9`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_4536a19ecac8`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-09's clean-room parser, semantic contract, and
Init/GetWave/Close ABI host are now present; in-process `agent-spice-sim` use
and the M5A acceptance gate remain.

## M5A-09h (In-process AMI inspection integration)

- Commits: `8ae8b06` (`feat(m5a): inspect AMI inputs in circuit CLI`) and
  `a9bcf4f` (`fix(m5a): minimize AMI CLI lock update`).
- `agent-spice-sim ami-inspect <model.ibs> <model.ami>` now calls the
  clean-room `sipi-ami` IBIS scanner, AMI parameter parser, and strict host
  metadata conversion in-process. It emits the versioned
  `agent-spice.ami-inspect.v1` summary and ships minimal structural fixtures.
  It neither loads a vendor DLL nor advertises platform certification.
- Review of the initial integration found one P2 (`msg_b33b407b9af2`): an
  unnecessary broad Cargo.lock refresh. The correction retains only the
  `sipi-ami` path dependency plus `libloading` entries required by that crate;
  the final combined delta from `d4565de` is 18 added lockfile lines and no
  unrelated package-version drift.
- Verification: locked fmt and clippy with `-D warnings`; locked tests
  `sipi-circuit` 21/21 and `sipi-ami` 17/17; fixture invocation emits the
  expected v1 summary; project `.venv` `tools/run_all_tests.py` 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, terminal
  conclusion 0 P1 / 0 P2 after the correction (orchestration request
  `msg_d50f002a171c`).
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_6bfd45cd4d39`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-09 is complete. The remaining work is the aggregate
M5A acceptance gate; it does not assert AMI golden/parity or authorize removal
of the retained PyAMI reference.

## M5A Aggregate Acceptance Gate

- Scope: M5A-06 through M5A-09h at `e4ab95d`. The gate covers the filtered
  Agent-Spice history, history-preserving move to the sole circuit crate,
  Python compatibility package and Windows wheel gate, and the clean-room
  Rust IBIS/AMI parser, metadata contract, `libloading` host, and in-process
  inspection command.
- Final verification: preflight reports ready with 371 retained and 651
  dropped source paths; move and license verifiers report ready/valid; the
  isolated Windows wheel gate reports `certification: not_claimed` with its
  eight-point smoke; locked Rust tests pass (`sipi-circuit` 21/21,
  `sipi-ami` 17/17); root `tools/run_all_tests.py` passes 56/56.
- Explicit boundary checks: no numerical algorithm, second resolver, or golden
  rewrite; the AMI structural fixtures are not golden/parity evidence; PyAMI
  remains a retained reference; no Linux/macOS certification is claimed.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, terminal
  conclusion 0 P1 / 0 P2 (orchestration request `msg_c6408f9b0116`).
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_9233b8df58a4`, 0 P1 / 0 P2.

Conclusion: accepted. **M5A is complete.** The next direction is M5B under
its own parity, topology, license, and reference-retention gates.

## M5B-01/02 (PyBERT source tiering closure)

- Source anchor: clean PyBERT migration candidate
  `b3d072e`; the user-owned `Py-bert-agent` main worktree was not used or
  modified. Commits `056dd23` (`feat(m5b): gate strict Rust tier by approved
  profiles`) and `975c0a7` (`docs(m5b): record strict Rust tier and remaining
  gates`) are on `codex/m5b-source-gates`.
- `PYBERT_ENGINE=rust` now resolves to an approved-profile wrapper: it
  validates the raw candidate and requires the existing pinned
  `native_auto_parity_gate` to return `approved`; unregistered profiles,
  missing/incompatible wheels, and build-contract mismatches fail closed with
  no Python fallback. `auto` and `compare` continue to instantiate the raw
  native candidate, preserving their established fallback and comparison
  semantics. The legacy `sim-rust` command remains an explicit raw-candidate
  development command.
- Scope was policy, diagnostics, tests, and documentation only. No Rust/Python
  numerical algorithm, channel resolver, or golden artifact changed. The
  source documentation explicitly preserves the distinction between the seven
  approved profiles and the still-open Task 13 reliability evidence (nightly
  slow fixtures, end-to-end RSS/performance budgets, remote/corrupt wheels,
  panic/OOM, and real child-process faults).
- Verification: a release wheel built from the audited source reports
  `contract_version=pybert.native-contract.v1`; 240 independent backend,
  contract, determinism, adapter, report, and native tests passed (three
  deliberate skips), and all twelve frozen-golden capture verifiers passed.
  The baseline verifier repeated ten fixtures ten times each. An older local
  extension without `contract_version` was blocked as intended; the audited
  source wheel then passed all admission tests.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_67f33dcd1ae8`, 0 P1 / 0 P2 / 0 P3.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_64baf5602a92`, 0 P1 / 0 P2 / 0 P3.

Conclusion: accepted. M5B-02 is closed. M5B-01 remains open for its explicit
Task 13 reliability evidence and must not be represented as complete by this
tiering gate alone.

## M5B-01b (Task 13 evidence runner and nightly collection)

- Source commits `0c90fbc`, `90679b9`, and `9d249ea` add a non-promotional
  source acceptance bundle and a Windows-native Task 13 report. They pin the
  actual extension payload, approved profile registry, frozen-golden verify
  runs, locked Cargo checks, bounded native rejection/cancellation, a
  million-symbol observation, synthetic child failure, optional remote-wheel
  validation, and fresh-child end-to-end elapsed/RSS observations. No
  numerical algorithm, resolver, golden, or default route changed.
- `e438f5e` adds `.github/workflows/task13-nightly.yml`: a Windows scheduled
  and manual-dispatch workflow that builds the locked release extension,
  records source acceptance plus five-repeat Task 13 evidence, and retains the
  reports for 35 days. Optional wheel inputs are explicit job environment
  values and must be supplied as an HTTP(S) URL/SHA-256 pair; a missing pair
  fails closed. Scheduled runs leave the remote wheel lane unavailable.
- Verification: the Task 13 focused source tests passed 4/4; the workflow
  parses as YAML, the one-sided wheel-input guard was exercised locally, and
  the source evidence reports remain observation-only. A local SIPI root-suite
  rerun was not recorded for this documentation-only target update because its
  temporary dependency install stalled without CPU progress; this has no
  bearing on the independently executed source gates.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, responses
  `msg_50ea08a02d50`, `msg_beb065d78934`, and `msg_f772b4b3a7c5`, 0 P1 / 0 P2.
  OMP was unavailable due to subscription quota; the user explicitly
  authorized a one-agent acceptance decision on 2026-08-09. Its pending
  workflow request is `msg_01c4e679f608`.

Conclusion: accepted as the Task 13 evidence collection mechanism, not as
Task 13/M5B-01 completion. It cannot create 30 days of results, approve or
meet the required end-to-end performance budget, exercise an authorized real
Agent-Spice executable, or substitute bounded input rejection for panic/OOM
and real child-process fault evidence.

## M5B-01c (staged native-wheel integrity observation)

- Source commit `c045d8e` extends the Task 13 runner in a new clean PyBERT
  worktree from `e438f5e`. An explicit external `--wheel-file` route is
  mutually exclusive with the remote URL route and requires the same pinned
  lowercase SHA-256. The artifact is copied into a private temporary directory
  and verified before the runner creates an isolated venv or invokes `pip`.
- A successful staged wheel receives the existing fresh-venv, `--no-deps`,
  `-I` native import smoke. A one-byte-corrupted copy must fail at the digest
  gate; its report says `installer_invoked=false` and the test verifies no
  corrupt venv directory was created. The observed Windows wheel hash was
  `19e7193f...fd468`; its isolated import passed, while its corrupted copy was
  rejected before installation.
- Verification: focused `tests/test_task13_reliability.py` passed 6/6, and the
  external staged artifact exercise passed. No numerical algorithm, resolver,
  golden, profile admission, production lock, or backend default changed.
- Independent audit: OMP `msg_677f77326ebf`, 0 P1 / 0 P2. The user permits a
  usable independent conclusion when the second reviewer is unavailable or
  delayed; OpenCode was also asked on this same boundary.

Conclusion: accepted as a Windows staged-wheel integrity and isolated-import
observation. It is not remote-wheel certification, a performance/RSS budget,
Task 13/M5B-01 completion, a final clean source tag, or a history-import
unlock.

## M5B-01d (locked Agent-Spice real-child output-commit fault drill)

- Source commit: clean PyBERT worktree
  `codex/m5b-task13-wheel-observation@0e6d3ad`
  (`test(m5b): drill locked Agent-Spice child failure`). The Task 13 runner
  accepts this lane only with all three explicit inputs: an absolute
  executable, its lowercase SHA-256, and an absolute `engine.lock`.
- The runner checks Windows platform, exact executable hash and size, exact
  `build-info`, and `rfmDirectResponse` before running. It materializes the
  RFM only from `f6ba031:tests/golden/rust_migration/agent_spice_rfm/block_2.rfm`
  Git blob `9a087a9f...`, verifies SHA-256 `5716f691...`, then performs a
  normal `AgentSpiceChannelRunner` control request before the fault request.
- A private test diagnostic subclass only changes the two existing runner
  output destinations: their parent is a regular file. The locked real child
  therefore returns exit 1 with Windows `ERROR_PATH_NOT_FOUND` (`os error 3`)
  at output commit; the runner returns `AgentSpiceChannelError`, publishes no
  consumable response, and records a scrubbed command template plus logical
  source/engine identities. It does not use a shell, a synthetic Python
  child, a second resolver, a changed golden, or a rebuilt executable.
- `AgentSpiceChannelRunner._run_process` now consumes child output as UTF-8
  with replacement decoding. This prevents an undecodable diagnostic from
  escaping its reader thread and masking the nonzero fail-closed result; a
  focused invalid-UTF-8 stderr test covers it.
- Verification: with `PYTHONPATH=src`, focused PyBERT tests passed 19/19
  (3 real-engine/Ngspice opt-in tests skipped). The external Windows report
  records the normal control response shape `[17, 2, 2]`, exact lock-pinned
  executable identity, fault exit 1, `response_published=false`, and the
  output-I/O error classification.
- Cross-audit: OMP `msg_5489a72da428` and OpenCode `msg_028c550955ba` both
  report 0 P1 / 0 P2. Both independently reproduced the focused test gate and
  the exact locked-executable drill.

Conclusion: accepted only as a Windows locked-real-child output-commit failure
propagation drill. It does not establish RFM numerical correctness, Link or
receiver parity, timeout/cancel behavior, panic/OOM resilience, general
Agent-Spice reliability, a production route, or M5B-01/Task 13 completion.

## M5B-01e (restricted Rust fault-worker containment drill)

- Source commit: clean PyBERT worktree
  `codex/m5b-task13-fault-worker@8eb62be`
  (`test(m5b): contain restricted Rust fault worker`). The standalone
  `native/task13-fault-worker` binary has
  `required-features = ["task13-fault-injection"]`; it is not built by default
  and has no wheel, resolver, default backend, or runtime-registry integration.
- `tools/build_task13_fault_worker.py` writes all build products outside the
  source tree and emits a v1 manifest binding the executable name/SHA/size,
  source commit, Cargo lock, worker/core manifests, feature, platform, and
  Cargo version. The Task 13 runner requires the explicit worker path, SHA, and
  manifest together, then rejects any mismatch before execution.
- The worker emits exactly one ready JSON record and waits for literal stdin
  `go`. Before sending that release, the parent assigns the child to a Windows
  Job Object with active-process limit one, 64 MiB process/job memory limits,
  10-second wall timeout, and kill-on-close. It then exercises either an
  intentional Rust panic or one bounded 128 MiB allocation; no arbitrary code,
  path, loop, or unbounded allocation is accepted.
- A passing panic lane requires ready then abnormal exit (observed exit 101),
  complete Job accounting, and no private-directory artifact. The bounded
  allocation lane additionally requires Rust's allocation-failed diagnostic
  and peak process/job memory at or below the configured limit; the final
  external rebuild/replay bound source commit `8eb62be` and observed
  `memory_limit_observed=true` with no success artifact.
- Verification: focused PyBERT tests passed 22/22 with three explicit
  real-engine/Ngspice tests skipped; default and feature-enabled locked Cargo
  checks passed. The external Windows build and full two-lane replay passed.
  OMP `msg_c58bddbf36e4` and OpenCode `msg_0db9cc9c708d` independently found
  0 P1 / 0 P2. OpenCode's pre-commit build artifact note was superseded by the
  final post-commit external rebuild/replay recorded above.

Conclusion: accepted only as a Windows restricted project-worker
panic/contained-allocation process drill. It does not establish production
`pybert_native` extension fault parity, unbounded OOM safety, a remote wheel,
RSS/performance budget, nightly history, Agent-Spice behavior, numerical
correctness, a production route, or M5B-01/Task 13 completion.

## M5B-03a (RFM current-drive artifact bridge)

- Scope: SIPI `876d380` and `ce3069a`; clean PyBERT source worktree
  `codex/m5b-source-gates` commits `7fb6182` and `93c8c9e`. The user-owned
  dirty PyBERT main worktree was not used.
- The platform materializes and hash-verifies the existing Agent-Spice
  `agent-spice.rfm-response.v1` metadata and binary response artifacts before
  the strict PyBERT CLI consumes them. The source path has an explicit
  `rfm_request` xor `rfm_response` boundary; the response path therefore
  enters the existing Rust receiver without rerunning Agent-Spice.
- This preserves the current-domain impedance contract
  `V(f)=sign*H(f)@I(f)` rather than coercing it to `ChannelResponseV1`. The
  M4 production resolver remains the only platform S2P/S4P resolver. Calibrated
  sign, explicit port intent, exact sample interval, one-sided FFT dimensions,
  finite values, response byte size, and provenance all fail closed.
- Verification: SIPI adapter tests 7/7 and root suite 56/56; PyBERT channel
  tests 9 passed with 3 opt-in skips and current-drive contract tests 3/3,
  using an audited native extension. The no-rerun test injects a runner that
  raises on any Agent-Spice invocation and still obtains the expected receiver
  result from the supplied artifact.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_0ad1e6785cf8`, 0 P1 / 0 P2 / 0 P3.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_acedd84f2ef2`, 0 P1 / 0 P2 / 0 P3.

Conclusion: accepted for the RFM current-drive bridge only. M5B-03 remains
open for the S2P/Python-external-boundary to sole-M4-resolver path and its
required parity compare report.

## M5B-03b (Python S2P external-boundary admission)

- Scope: SIPI `029c09d`, `58971aa`, `677f8af`, and `437d8bc`; clean PyBERT
  source worktree `codex/m5b-source-gates` commits `244f6db` and `f6ba031`.
  The user-owned PyBERT main worktree was not used.
- `export_external_channel_resolution()` calls existing
  `PyBERT.calc_chnl_h()` once and only serializes that result. The legacy
  discrete `V/sample` taps are divided by `dt` exactly once at the DTO
  boundary; the native `ChannelResponseV1` consumer multiplies by `dt` and
  therefore restores the same discrete taps. No S-parameter conditioning,
  termination, IFFT, resampling, trim, or sign algorithm was copied into SIPI.
- The SIPI adapter requires the injection sentinel and checks canonical
  network/policy/impulse hashes, finite samples and impedances, exact timebase
  agreement, port and policy intent, and source provenance. It calls the
  existing M4 `resolve_channel` exactly once; unsupported numerical transforms
  fail closed. On-die/IBIS-modified source channels are rejected by the
  exporter.
- Verification: SIPI focused adapter suites passed (13 tests plus 5
  subtests); clean-source external-resolution tests passed (3 tests). The
  earlier contract-error category regression was fixed in `437d8bc`; OMP
  independently reports the root suite at 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_14c2c2edc93b`, 0 P1 / 0 P2 / 0 P3.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_fe0137aaaf4f`, 0 P1 / 0 P2. Its non-blocking note is that the source
  channel/config hashes are provenance identities at this boundary, not
  independently materialized by the SIPI adapter.

Conclusion: accepted for external-boundary admission and unit/lineage
verification only. This does not claim that M4 resolves S2P or that S2P
numerical parity is proven. M5B-03 remains open for a same-config Python
reference-to-native Link array and metric compare report.

## M5B-03c (fixed S2P Link handoff parity)

- Commits: `221ac0c` (`test(m5b): verify S2P Link handoff parity`) and
  `2adbcc3` (`fix(m5b): resolve cross-repo S2P fixture paths`). The committed
  cross-repository gate is
  `tools/verify_m5b_s2p_link_parity.py`; it is intentionally not part of the
  hermetic root suite and requires the explicitly supplied clean PyBERT source
  worktree and its native extension.
- It runs the authorized tracked fixture
  `models/sp_file/channel_16ghz_3db.s2p` at source `f6ba031`
  (`sha256=6263c436bdc05150ac4263945abac44542f2f59daa33a6be2aa41e446345cea2`)
  with a deterministic NRZ/PRBS-7 configuration. The only S-parameter numeric
  producer is legacy `PyBERT.calc_chnl_h`; the gate exports its DTO, sends it
  through `PyBertResolvedChannelAdapter` and its sole M4 resolver call, then
  invokes the real PyBERT `sim-native` CLI in a subprocess.
- The emitted report pins source/config/network/policy/producer identities and
  verifies the DTO unit round trip `(V/s) * dt ~= V/sample` at
  `rtol=1e-15`, with a serialization-rounding maximum of
  `6.94e-18`. It reuses PyBERT's structured comparator for the seven Link
  arrays: channel impulse/output, RX input, CTLE output, DFE output, DFE
  decisions, and DFE clocks. BER, sampled-eye height, jitter, and integer error
  count are also compared; all checks passed in the recorded invocation.
- The report explicitly excludes legacy visual-eye versus native
  statistical-eye width, which are distinct M5B-05 product-presentation
  contracts. It also explicitly excludes any claim that M4 performs or
  independently validates S-parameter conditioning, termination, IFFT, or
  resampling, or that this certifies ADS/AMI/S4P/general S-parameter parity.
  The `NetworkTensor` in this identity-admission gate is a structural
  descriptor with a placeholder data artifact hash; it is not independent S2P
  matrix evidence and remains a non-blocking limitation for any future M4
  numeric S-parameter implementation.
- Verification: the cross-repository invocation passed for both absolute and
  `--pybert-root`-relative fixture paths; its resolved-input, fixture-request,
  and M4-output hashes were identical. Project
  `.venv` `tools/run_all_tests.py` passed 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_f355f8f9b021`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, responses
  `msg_d86556e9b3e4` and `msg_e09811d1e784`, 0 P1 / 0 P2.

Conclusion: accepted for the fixed external S2P fixture's Link handoff parity.
This closes the S2P evidence left by M5B-03b, but does not close M5B-03's
separate RFM current-domain reference-to-native compare obligation.

## M5B-03d (RFM reference baseline identity blocker)

- Scope: read-only preflight against clean PyBERT source worktree
  `codex/m5b-source-gates@f6ba031`, its tracked
  `tests/golden/rust_migration/agent_spice_rfm/block_2.rfm`, and the
  lock-pinned Agent-Spice executable. No golden or numerical algorithm was
  changed.
- The tracked RFM has SHA-256
  `894b5da3aba954d3d288689458be99d7434db966b4df4675db52165c4db2b55d`, but
  its immutable `block_2_response_v1.json` declares
  `5716f691a69174cfe88980ffd96bfc867f30f9ee92b6b85d3fb6b8c8b3636a53`.
  The mismatch exists in the introducing source commit `1d6565e`; no object
  or file with the declared bytes was found in the clean source or Agent-Spice
  sources, and the golden retains no `response.bin`.
- The packaged Agent-Spice executable SHA-256
  `111ff6e1a1a8f7f39782a355722c56f4d8e7392bff6d389445ba87dee73928dc` matches
  `native/crates/sipi-circuit/engine.lock`. Running the tracked RFM through it
  reproduces the golden response hash
  `ce80d68600319a4d1b8788e40f88221d1c231ba157bf2b0949bd14ab63a12ddc`, but
  cannot repair the contradictory RFM identity. The source opt-in real-engine
  golden therefore fails at the identity assertion before response comparison.
- Reproduction observation: the lock-matching artifact was read from
  `C:\\Users\\z3312\\code\\agent-spice\\src\\agent_spice\\lib\\native\\win-x64\\agent-spice-sim.exe`;
  its SHA-256 and byte length (`4604416`) were checked before invoking the
  clean-source `AgentSpiceChannelRunner` with the golden's fixed 32-point,
  `1e-12`-second, ports `[1,2] -> [1,2]` request. This records the otherwise
  non-obvious diagnostic artifact source without treating the user worktree as
  source code evidence.
- Independent audit: OMP `msg_27de4d4fbc16`, 0 P1 / 0 P2 / 0 P3.
- Cross-audit: independent review agent `msg_7facce6a6e01`, 0 P1 / 0 P2.
  Its P3 asked that the diagnostic artifact source be recorded; the preceding
  reproduction observation closes that documentation gap.

Conclusion: blocked for acceptance. A current NumPy-to-native FFT transfer
comparison can only be labelled diagnostic until the original RFM bytes are
recovered or the user explicitly approves a provenance-preserving golden
replacement. It must not close M5B-03, represent RFM Link parity, or be used
to alter the retained golden implicitly.

## M5B-03e (Git-object RFM baseline correction and transfer/handoff evidence)

- Source commit: `2b92b80` (`test(m5b): verify RFM transfer handoff`), based
  on `f6ba031`. The gate uses `git cat-file blob`, never a filtered checkout,
  to materialize the RFM and its response contract from the same source commit.
- This corrects M5B-03d's baseline assessment. The `894b5d...` value was the
  Windows CRLF-transformed worktree content, including an earlier audit's
  PowerShell text-pipeline reproduction. The authoritative Git blob is
  `9a087a9f75c58d3d1fb15efee2628001a601a546`; its raw SHA-256 is
  `5716f691a69174cfe88980ffd96bfc867f30f9ee92b6b85d3fb6b8c8b3636a53`, exactly
  matching `block_2_response_v1.json`. The response JSON is likewise bound to
  blob `9315911e83e13f95e9aff255f52716d339e9ad35`.
- With the `engine.lock`-pinned `111ff6...` executable, the gate verifies the
  raw RFM identity, executable size/hash/build-info/capability, response
  binary SHA-256 `ce80d686...`, shape/finite state, and its three immutable
  probes. It then compares existing NumPy and native FFT callbacks on the
  same frozen two-port current drive, exact ports/dt and calibrated `-1` sign.
  Finally the pre-produced response is handed to the existing Rust receiver
  with a runner that raises if Agent-Spice is invoked again; its RX input hash
  equals the verified native voltage waveform hash.
- Verification: the cross-repository gate emitted
  `C:\Users\z3312\AppData\Local\Temp\m5b-rfm-transfer-handoff.json` with
  `acceptance=true`; the focused source suite passed 31 tests with 3 existing
  opt-in skips. The verifier's raw-Git unit tests also passed when copied out
  of the repository, so they do not accidentally derive their Git root from
  the test file location.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, responses
  `msg_25a6ad518cfe` and `msg_c325613631c9`, 0 P1 / 0 P2. OMP was unavailable
  due to its subscription quota; the user explicitly authorized a one-agent
  acceptance decision on 2026-08-09. Its pending request remains recorded as
  `msg_d154580e431e`.

Conclusion: accepted only for Git-object-backed RFM response golden
reproduction plus current-domain NumPy/native FFT transfer and one native
receiver handoff. It clears the false "missing original RFM" blocker but does
not establish independent RFM Link receiver/BER parity, general RFM/S-parameter
parity, or full M5B-03/M3-08b closure.

## M5B-05 (GUI/optimizer backend support gate)

- Scope: SIPI `1a46727`, specifically `docs/m5b-backend-support.v1.md` and
  the M5B-05 plan row, cross-checked against clean PyBERT source worktree
  `codex/m5b-source-gates@f6ba031`. The user-owned PyBERT main worktree was
  not used.
- The matrix accurately records the explicit Web registry, strict seven-profile
  Rust admission, observable `auto` fallback, Python-hosted AMI hybrid, and
  the GUI optimizer's Python-only rejection. Unsupported routes fail closed;
  the matrix does not create a production route or introduce a silent fallback.
- It accurately keeps the clean-room Rust AMI host at lifecycle-contract scope
  only. It makes no GUI, optimizer, AMI numerical-parity, or Linux/macOS
  certification claim, and does not close the separate M5B-01, M5B-03, or
  M5B-04 gates.
- Verification: OMP independently ran `tools/run_all_tests.py` at 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_c415ffabea79`, 0 P1 / 0 P2 / 0 P3.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_80d9fd6b73be`, 0 P1 / 0 P2. Its non-blocking P3 recommends a future
  documentation-to-registry consistency check to prevent drift.

Conclusion: accepted as a support/non-support governance gate only. It does
not alter any backend's numerical acceptance status.

## M5B-04 (Rust AMI host route preflight)

- Scope: SIPI `16db13a`, `docs/m5b-ami-host-preflight.v1.md`, and the M5B-04
  plan row, checked against clean PyBERT source
  `codex/m5b-source-gates@f6ba031`, `sipi-ami` host code, and the engine lock.
- The current `sipi-ami` API is correctly retained as a clean-room library
  lifecycle contract, not a lock-constrained process route. The only locked
  Windows executable is `111ff6...` (4,604,416 bytes) and declares only
  `build-info` and `rfm-response`; it cannot honestly host a versioned AMI
  request today.
- The accepted preflight prohibits substitution by a local rebuild or the
  Python host. It records the required future process contract and preserves
  explicit `rust-host`, executable/DLL/model identity, Init/GetWave/Close,
  lock/cancel/timeout/partial-output failure handling, and existing Rust
  receiver handoff requirements. It makes no AMI parity, default, GUI,
  optimizer, Linux, or macOS claim.
- Verification: OMP independently ran `tools/run_all_tests.py` at 56/56.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_9e2b50c17592`, 0 P1 / 0 P2 / 0 P3.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_a0a312a5409a`, 0 P1 / 0 P2. Its P3 asked for a pointer to the existing
  hybrid-AMI fixture-distribution compliance blocker; the preflight now points
  to `docs/baselines/capability-inventory.yaml` and explicitly leaves it open.

Conclusion: M5B-04 remains blocked pending a separately versioned host process
contract, candidate bundle, lock promotion, and source-side thin process
adapter. The preflight itself is accepted.

## M5B-04a (AMI metadata prerequisite blocker)

- Scope: SIPI `57161b9`, the M5B-04 plan row, and
  `docs/m5b-ami-host-preflight.v1.md`, checked against clean PyBERT source
  `codex/m5b-source-gates@f6ba031` and the pre-fix `ami-inspect` behavior.
- The recorded failure was reproduced: the public wrapper-style
  `example_rx.ami` placed required values under `Reserved_Parameters`, while
  the original typed metadata lookup searched only the top-level tree and
  failed closed with `missing AMI parameter AMI_Version`.
- The record correctly treated wrapper parsing as a prerequisite, not an AMI
  runtime, parity, candidate-bundle, or PyBERT-route claim. M5B-04 remained
  open.
- Independent audit: OMP `msg_a12ee4eb927d`, 0 P1 / 0 P2.
- Cross-audit: independent review agent `msg_90b40009583c`, 0 P1 / 0 P2.

Conclusion: accepted as an accurate blocker record only.

## M5A-09i (Reserved_Parameters host metadata)

- Commit: `e204d0b` (`fix(m5a): parse reserved AMI host metadata`). The
  clean-room parser now collects only the three typed host metadata records
  directly inside standard `Reserved_Parameters`, without changing the public
  top-level `parameters()` / `parameter()` API.
- The wrapper fixture intentionally contains a same-named
  `Model_Specific` `AMI_Version` with a conflicting Float value. Metadata
  extraction selects the reserved String value (`5.1`), proving that model
  parameters cannot override host metadata. Malformed reserved records still
  propagate the existing parse failure, and typed semantic checks remain
  fail-closed.
- Verification: `cargo fmt --check`, `cargo clippy -- -D warnings`, and
  `cargo test` for `sipi-ami` passed (18/18); the public
  `example_rx.ibs` / `example_rx.ami` now passes `ami-inspect` with IBIS 7.1,
  AMI 5.1, `Init_Returns_Impulse=true`, and `GetWave_Exists=true`.
  `tools/run_all_tests.py` passed 56/56.
- Independent audit: OMP `msg_5cbd6c6bd68e`, 0 P1 / 0 P2.
- Cross-audit: independent review agent `msg_bc9c7a1ac800`, 0 P1 / 0 P2.

Conclusion: accepted for metadata admission. It does not demonstrate DLL
loading, `AMI_Init`/`AMI_GetWave` execution, candidate-bundle promotion,
PyBERT routing, or AMI numerical parity. M5B-04 remains open.

## M5B-04b (candidate AMI host transport)

- Commit: `c51029d` (`feat(m5b): add AMI host candidate transport`). The
  Windows-only, explicitly invoked `ami-host-candidate` command introduces
  `agent-spice.ami-host-request.v1` and `agent-spice.ami-host-result.v1`.
  It remains outside `engine.lock`, production resolver discovery, defaults,
  fallback, GUI, optimizer, and PyBERT routing.
- The command accepts explicit request-relative `.ibs`, `.ami`, and `.dll`
  identities plus `f64le` ABI sidecars. It validates schema, unknown fields,
  hashes, byte and element counts, finite values, metadata/capability
  agreement, and path traversal before invoking the sole clean-room host path:
  `AmiDll::load` followed by `AmiModel::initialize`, optional `get_wave`, and
  `close`. It performs no receiver, resampling, equalization, unit, or sign
  calculation; v1 records primary column zero only.
- Outputs are written to a sibling staging directory and renamed only after
  Init, optional GetWave, and Close all succeed. The Windows dynamic ABI stub
  integration test covers Init-only, Init+GetWave raw waveform/clock buffers,
  bad hashes, unknown fields, path traversal, metadata mismatch, non-finite
  input, and Close failure with no published output.
- Verification: `cargo fmt --check`, `cargo clippy --all-targets -- -D
  warnings`, and `cargo test --all-targets --locked` passed. OMP additionally
  ran the root `tools/run_all_tests.py` suite at 56/56; its timing review found
  the apparent no-output interval came from temporary-venv wheel tests, not a
  `test_supervisor.py` deadlock.
- Independent audit: OMP `msg_681bb805f087`, 0 P1 / 0 P2 / 0 P3.
- Cross-audit: independent review agent `msg_6c88a2451f5a`, 0 P1 / 0 P2.
  Its two P3 observations are intentionally retained: the candidate has no
  promoted bundle or loader dependency-closure hash, and the initial local
  root-suite observation needed the timing clarification above.

Conclusion: accepted as candidate transport and lifecycle evidence only. It
does not demonstrate a licensed real-DLL runtime, candidate bundle or lock
promotion, PyBERT process routing, timeout/cancellation/lock handling, AMI
waveform/BER/eye parity, GUI/optimizer support, or Linux/macOS certification.
M5B-04 remains open.

## M5B-04c (candidate request provenance)

- Commit: `a14120b` (`fix(m5b): bind AMI candidate result to request`). The
  candidate now calculates `requestSha256` from the exact manifest bytes before
  deserializing them, and publishes that digest at the top level of a successful
  v1 result. This binds the model and sidecar identities already pinned inside
  the manifest to the output that a later process adapter consumes.
- Verification: the dynamic stub integration test rereads the manifest and
  compares its independently calculated SHA-256 to `result.json`.
  `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and
  `cargo test --all-targets --locked` passed. OMP additionally reran the root
  suite at 56/56.
- Independent audit: OMP `msg_d7dc6b2cccb8`, 0 P1 / 0 P2.
- Cross-audit: independent review agent `msg_b3f4a53f44d7`, 0 P1 / 0 P2.
  Its P3 records the intentional division of responsibility: the manifest hash
  binds its exact bytes, while each referenced model/sidecar remains bound by
  its own SHA-256 and byte length.

Conclusion: accepted provenance completion for the candidate transport. It
does not promote the candidate or alter the remaining M5B-04 limitations.

## M5B-04d (PyBERT candidate process adapter)

- Commit: clean PyBERT worktree `codex/m5b-task13@d4af5e0`
  (`feat(m5b): add Rust AMI host candidate adapter`), reviewed together with
  SIPI `a14120b`/`04f2dc6`.
- `RustAmiHostCandidate` is an explicit Windows-only, unpromoted adapter. It
  pins an absolute executable and SHA-256, its `build-info` result, the exact
  raw-manifest digest, result echo, and copied `.ibs`/`.ami`/`.dll` identities.
  It uses a private temporary directory and f64le sidecars, rejects malformed,
  partial, non-finite, or mismatched output, and kills and waits for timeout or
  cancellation. It has no `ctypes`, Python DLL-host, numerical conversion, or
  fallback path.
- The adapter does not infer host placement: the caller must assert the
  existing `post_rx_ffe` handoff. It returns the validated, unchanged raw
  waveform through `HostDrivenDfeInputV1`; the test proves the already existing
  native DFE receiver is called exactly once and is not called on rejection.
  It is not registered in Web, default, auto, or compare routing.
- Verification: `tests/test_rust_ami_host_candidate.py`,
  `tests/test_hybrid_ami_pipeline.py`, and
  `tests/test_engine_python_backend.py` passed 75 tests with 1 pre-existing
  skip. The synthetic child is transport evidence only; it is not a real DLL
  runtime or AMI numerical-parity result.
- Independent audit: OMP `msg_28c75f0e5a27`, 0 P1 / 0 P2 / 0 P3.
- Cross-audit: independent review agent `msg_c61ba4387555`, 0 P1 / 0 P2. Its
  P3 notes that output-file checking does not separately reject a nested extra
  directory; the enclosing temporary directory is always removed, so it does
  not create a consumable artifact or block acceptance.

Conclusion: accepted as the candidate-only PyBERT process-adapter contract.
It does not promote a bundle or lock, certify a real DLL runtime, create a
default route, establish AMI waveform/BER/eye parity, or certify Linux/macOS.
M5B-04 remains open.

## M5B-04e (candidate bundle and license preflight)

- Commit: `c1d4d97` (`feat(m5b): add AMI candidate bundle preflight`). The
  candidate-only builder copies a caller-selected executable to a new external
  artifact directory and emits `sipi.m5b-ami-candidate-bundle.v1`; Git contains
  only the builder, verifier, tests, and policy record, never an executable or
  vendor fixture.
- The manifest requires a clean SIPI source commit, `sipi-circuit` Cargo.lock,
  clean-room `sipi-ami` lineage, candidate build-info v1 capability, executable
  hash/size, and binary-safe Git-object evidence for
  `agent-spice@2cc92316` `LICENSE`/`LICENSE-MANIFEST.md`. Vendor DLL/AMI/IBIS
  assets and their loader closure are caller-supplied `blocked_unknown` and
  forbidden in the bundle. The verifier rejects their suffixes, a third-party
  closure, hash/size/schema drift, and any candidate command or hash appearing
  in `engine.lock`.
- External clean release observation: artifact manifest SHA-256
  `50f4a4f2d4aa12abbcc9cbb7e5fa30c2d5fc104ee1d43174f6e5682cf0ae00cb`,
  candidate executable SHA-256
  `8fcbe4c059875286b6a2f325b3442f19d39d6be78bc44dc198a2f9b1d165bb1a`,
  source commit `c1d4d97`, Windows x64, `cargo 1.88.0`. The observed manifest
  deliberately says `input-pinned; executable reproducibility must be
  independently observed`; it is not a release or promotion evidence.
- Verification: Python bundle tests 7/7 passed; `cargo fmt --check`,
  `cargo clippy --all-targets -- -D warnings`, and
  `cargo test --all-targets --locked` passed (21 Rust tests). The existing
  Windows synthetic ABI-stub transport test remains part of that Rust run.
- Cross-audit: OpenCode `msg_7949d9ab636f`, 0 P1 / 0 P2. P3 records two
  intentional, documented limits: `--source-root` is an operator trust
  boundary and the system-DLL closure is operator-declared, not a PE import
  scan. OMP request `msg_d9d9f01901e0` is pending provider recovery; under the
  user's 2026-08-09 direction, the available independent 0 P1 / 0 P2 review is
  sufficient to record this candidate-policy slice.

Conclusion: accepted as an explicit candidate packaging and license-preflight
policy only. It does not promote `engine.lock`, register a default/auto/Web/
GUI/optimizer backend, authorize a vendor runtime, prove loader closure or
binary reproducibility, or establish AMI numerical parity. M5B-04 remains
open.

## M5B-04f (PE closure and twin-build assurance)

- Commit: `405dfb1` (`feat(m5b): measure AMI candidate PE closure`). A
  fail-closed, dependency-free PE32+ reader records machine/subsystem,
  timestamp, section hashes, normal imports, and delay imports. It rejects
  malformed offsets, unsupported delay attributes, non-x64 executables,
  changed twin import closure, and non-System32 non-API-set imports. Vendor
  DLL closure remains caller-supplied `blocked_unknown`.
- Two new external target directories built `sipi-circuit` at `405dfb1` with
  `cargo build --locked --release` and `CARGO_INCREMENTAL=0`. Their executable
  hashes differ (`f1feed1f...a790` vs `f4c0acef...36e8`) while size, import
  closure, `.text`, `.data`, `.pdata`, and `.reloc` hashes match; COFF
  timestamps and `.rdata` differ. The machine-readable report
  `m5b-ami-assurance-405dfb1.json` therefore records
  `byteReproducible=false` and hard-codes `promotionEligible=false`.
- Verification: assurance tests plus bundle tests passed 10/10. A real
  candidate executable parsed as PE32+ with five sections, eleven normal
  imports, no delay imports, System32 hashes for concrete imports, and API-set
  contract records. This measures the candidate executable only; it does not
  invoke a vendor DLL.
- Cross-audit: OpenCode `msg_0a88f7642e48`, 0 P1 / 0 P2. P3 records strict
  ASCII DLL-name decoding and a theoretical section-slice edge; both fail
  closed or are guarded by PE bounds and do not change the acceptance scope.
  OMP request `msg_1f30b1467f95` remains pending provider recovery under the
  user's single-available-auditor continuation decision.

Conclusion: accepted as Windows candidate build-assurance evidence. The
observed non-reproducibility blocks promotion; it does not certify a bundle,
real AMI DLL lifecycle, default routing, or AMI numerical parity. M5B-04
remains open.

## M5B-04 Candidate Packaging Aggregate

- Scope: candidate transport/provenance, PyBERT explicit adapter, bundle and
  license preflight, PE closure, and twin-build evidence. The `/Brepro` follow-
  up used two new external targets with `CARGO_INCREMENTAL=0`; both produced
  the same 4,776,448-byte executable SHA-256
  `a01e5076fe5dea3dba6f05780ee7d8c22a2dd65b232aef55656e5375336fc759`.
  It remedies the previously observed timestamp/`.rdata` drift but does not
  change `promotionEligible=false` or add an engine lock entry.
- Cross-audit: OpenCode aggregate review reports 0 P1 / 0 P2. Its P3 is
  retained: the `/Brepro` report is an external evidence artifact and must be
  retained/repeated by any future promotion review. It does not alter the
  preflight's vendor `blocked_unknown` state.

Conclusion: the whole candidate packaging boundary is accepted as a necessary
Windows-only pre-promotion evidence chain. It remains non-production: no lock
promotion, default/auto route, vendor DLL runtime, or AMI numerical-parity
claim is authorized.

## M5B-04g (authorized exact-fixture runtime contract)

- Commit: `614a328` (`docs(m5b): authorize exact AMI fixture runtime`). The
  machine-readable preflight binds the user-authorized, external-only Windows
  fixture to PyBERT Git objects at `f6ba031`: `example_rx.ibs`
  (`37b7bf1b...f39fbe9`), `example_rx.ami`
  (`6d4038a1...6ef80c38`), and `example_rx.dll`
  (`60e29f16...8c1ff46c`). It keeps the assets out of Git and limits use to an
  explicit candidate runtime; redistribution, `engine.lock` promotion, and
  default/auto routing remain prohibited.
- Verification: the Git-object-only preflight verifier passed against the
  dirty PyBERT worktree without treating its worktree bytes as authority. The
  externally materialized exact blobs were then passed explicitly to the
  `/Brepro` candidate (`a01e5076...336fc759`, build revision `7990fc7`). An
  empty AMI parameter string returned ABI status `0` and published no output
  directory. A separate diagnostic `ctypes` probe using the standard Windows
  `c_long` ABI showed that `(example_rx)` returns Init status `1` and Close
  status `1`; the candidate preserves that strict status contract.
- The candidate `init` and `init-get-wave` requests with `(example_rx)` both
  reported `initSucceeded=true` and `closeSucceeded=true`; the latter also
  reported `getWaveAttempted=true`. Result manifests bind the raw requests
  (`90e8fae6...9667cd` and `ca37415...7fd874`), model identities, schema, and
  candidate executable. The observed GetWave sidecars are a two-sample
  waveform (`b74fce6c...4146633`) and an explicitly empty clock vector
  (`e3b0c442...b7852b855`). These are lifecycle/transport observations, not a
  numerical comparison.
- Cross-audit: OpenCode `msg_239b906d9789`, 0 P1 / 0 P2. P3 records that the
  external Temp evidence should be retained or made replayable before any
  later promotion review. OMP `msg_9c9e9f744eef` independently reports 0 P1 /
  0 P2 / 0 P3.

Conclusion: accepted as an explicit, Windows-only real-DLL Init/GetWave/Close
contract observation for this exact authorized fixture. It does not grant
redistribution, prove the caller-supplied DLL dependency closure, promote a
bundle or lock, route PyBERT/default/auto/Web/GUI/optimizer execution, certify
Linux/macOS, or establish AMI waveform/BER/eye numerical parity.

## M5B-04h (authorized runtime replay gate)

- Commit: `f09052a` (`feat(m5b): add authorized AMI runtime replay gate`).
  `replay_m5b_ami_authorized_runtime.py` turns the prior one-off observation
  into a Windows-only replay gate. It requires an explicit external candidate
  bundle and report path, verifies the candidate manifest/build-info/executable
  identity, then materializes only the exact `f6ba031` Git-object fixture into
  a fresh private temporary directory.
- It produces the fixed `init-get-wave` v1 request with `(example_rx)`, no
  caller-selectable model or parameter path, and validates the request echo,
  candidate capability, all model identities, exact metadata, Init/GetWave/
  Close success, output-file set, f64le encodings, hashes, sizes, and finite
  values. The external report records logical Git identities and hashes only;
  `finally` removes all materialized vendor assets and sidecars.
- Verification: an external bundle (`50dd4b35...396b4`) containing the
  `/Brepro` candidate (`a01e5076...6fc759`) replayed successfully. The report
  records request `dbf88752...9a4a3`, result `19881308...2605e`, two-sample
  Init/GetWave hash `b74fce6c...46633`, and an empty clock vector. Focused
  tests passed 3/3; the replay intentionally has no `ctypes`/Python-host lane
  and makes no numerical comparison.
- Independent audit: OpenCode `msg_12f96080b263` and OMP
  `msg_2617d1872130` both report 0 P1 / 0 P2; both independently replayed the
  gate, and OMP reports the root suite at 55/55. P3: a future operator must
  supply a separately verified external bundle; this is intentional and does
  not relax the bundle verifier or create a promotion path.

Conclusion: accepted as a replayable exact-fixture candidate lifecycle gate.
It remains Windows-only and non-production; it does not establish DLL closure,
vendor redistribution, candidate promotion, AMI numerical parity, or a default
or auto route.

## M5B-04i (exact PyAMI/Rust raw host-output equivalence)

- Commit: `e82236c` (`feat(m5b): compare exact AMI host outputs`). The
  Windows-only comparison gate fixes one canonical raw ABI call for the
  authorized `example_rx` fixture: two input samples `[1.0, -1.0]`, row size
  two, zero aggressors, `dt=1e-12`, bit time `1e-10`, raw UTF-8 parameter
  bytes `(example_rx)`, and clock capacity eight with `-1.0` sentinel. It
  materializes both the three fixture assets and the 33-entry retained PyAMI
  source tree only from `f6ba031` Git objects into a private temporary
  directory.
- The retained PyAMI reference directly invokes `_amiInit`, `_amiGetWave`, and
  `_amiClose` with Windows `c_long` return types; Init, GetWave, and Close must
  all return `1`, and Close runs exactly once. It does not use
  `run_ami_model` or perform any high-level conversion, convolution, FFT,
  resampling, or unit conversion. The candidate remains the explicit external
  `/Brepro` bundle (`50dd4b35...396b4`) and executable
  (`a01e5076...6fc759`), with `productionResolvable=false`.
- Verification: the external report
  `m5b-ami-exact-host-equivalence-f090.json` was accepted. Init and GetWave
  raw f64le outputs match byte-for-byte (`b74fce6c...46633`), clocks are both
  the explicitly empty `e3b0c442...b855`, and all three observable UTF-8 text
  outputs match exactly. The focused tests passed 3/3. The temporary
  materialization is removed in `finally`; the report contains hashes,
  lengths, and logical Git identities rather than vendor bytes or absolute
  paths.
- Independent audit: OpenCode `msg_c77a3738f99a` and OMP
  `msg_e9d222e8622b` both report 0 P1 / 0 P2. OpenCode P3 notes that an
  after-the-fact byte inspection requires a replay and that the explicit
  reference interpreter remains an operator-supplied environment prerequisite.
- Follow-up test-gate repair: `4e9875d` removes an accidental `pytest`
  runtime dependency from the pre-existing candidate-bundle and PE-assurance
  test files, because the repository's root gate invokes each file directly.
  Their former `pytest` functions were not executed by that runner and the
  locked root environment lacked that dependency. Equivalent fail-closed cases
  now run through explicit temporary-directory `main()` entry points. OMP
  `msg_4011e18dd283` reports 0 P1 / 0 P2 and independently verifies the root
  gate at 61/61; no production candidate policy changed.

Conclusion: accepted only as `example_rx` exact-fixture raw ABI-observable
PyAMI/Rust host-output equivalence on Windows. It is not general AMI parity,
AMI Link BER/eye parity, a Python high-level pipeline comparison, DLL dependency
closure, vendor redistribution, candidate promotion, a default/auto route, or
Linux/macOS certification. Python remains the reference host and M5B-04 remains
open.

## M5B-04j (stateful exact PyAMI/Rust raw host-output matrix)

- Commit: `ee7b5f5` (`feat(m5b): add stateful AMI host matrix`). Candidate-only
  request v2 accepts either Init-only or a non-empty ordered GetWave sequence.
  It reuses the sole `sipi-ami` DLL host, validates all sidecars before Init,
  performs every GetWave call against one model handle, requires Close, and
  atomically publishes only a complete result.
- The external Windows report `m5b-ami-stateful-equivalence-ee7b5f5.json`
  uses the authorized `f6ba031` `example_rx` Git objects and a new external
  candidate bundle. It compares retained PyAMI direct `c_long` ABI calls with
  candidate raw f64le sidecars, clock sentinels, and observable UTF-8 texts for
  Init-only, one GetWave, and two sequential GetWave blocks of different
  lengths. Every observed Init/GetWave/Close status is `1` and all compared
  bytes are exact.
- This is one Windows fixture and fixed raw ABI call family only. It is not
  general AMI semantics, Link BER/eye parity, a high-level Python comparison,
  DLL closure, redistribution, candidate promotion/default routing, or
  Linux/macOS certification.

Conclusion: OMP `msg_26c293b208eb` independently reports 0 P1 / 0 P2. Under
the user-approved one-review continuation rule, this bounded M5B-04j gate is
accepted; the requested OpenCode cross-review remains pending and will be
recorded when delivered.

## M5B-06/07 (PyBERT history-import preflight only)

- Commit: `eebdd54` (`feat(m5b): add PyBERT history preflight gate`). The
  preflight fixes a candidate-only source identity at clean detached
  `pybert-core-rust-migration@b3d072e` (tree `6428fa66...416ea`) and recreates
  a complete 521-path policy directly from Git blobs: 268 retain, 250 drop,
  and 3 external-reference entries. The canonical entry digest is
  `79165e39...241f8f`; each entry carries its path, mode, blob, decision,
  reason, owner, license status, import target, and (for `pybert-core`) future
  `sipi-link` target.
- The verifier requires a clean detached source at that exact commit/tree,
  rejects local `filter-repo` metadata and either prospective destination
  directory, and rejects drift in any entry/digest. Legacy `PyAMI`, agent and
  experiment state, generated artifacts, and user input are dropped; vendor
  AMI fixtures are external references. Native-core retain entries carry the
  BSD-derived attribution/Cargo-MIT consistency decision as pending rather
  than resolving it by assertion.
- `mode=preflight_only`, `migrationEligible=false`, `M5B-01=pending`, and
  `historyMapStatus=pending` are hard requirements. No filter run, destination
  copy, future destination commit, or synthetic history map exists. The future
  filter contract requires a new final clean accepted source tag plus pinned
  `git-filter-repo` identity, old-to-new commit map, and preservation counts.
- Cross-audit: OpenCode initially found P2 in the test's expected JSON hash;
  the one-character semantic repair (adding the missing outer array) was
  amended into `eebdd54`. Recheck `msg_f75c1b4c1c2f` reports 0 P1 / 0 P2 and
  3/3 tests passed. OMP `msg_987b8ea3d442` and repair recheck
  `msg_ffe14c9062ed` both report 0 P1 / 0 P2; the latter independently ran the
  root suite at 55/55.

Conclusion: accepted only as reproducible migration-input policy and source
provenance. It does not execute or authorize a history rewrite/import, make
the candidate anchor final, resolve licensing, or close M5B-01, M5B-03, or
M5B-06--09 implementation work.

## M5B-01f (production-wheel memory-containment drill)

- Source commit: clean PyBERT worktree
  `codex/m5b-task13-production-wheel-memory@b6fd1ef`
  (`test(m5b): contain production wheel memory fault`). It adds an explicit
  Windows-only `--exercise-production-wheel-memory` route to the existing
  Task 13 runner. The route requires a caller-supplied wheel and lowercase
  SHA-256, validates it before venv creation, and installs it only alongside
  the exact NumPy wheel selected for the running interpreter from `uv.lock`
  using `pip --require-hashes`.
- The isolated child runs the existing approved RLGC native input (profile key
  `260f509b...f0c9`) through the public `simulate_native_v1` API. It starts
  with `-I`, clears `PYTHONPATH`, and verifies both package facade and native
  payload are under the temporary venv site-packages directory. The parent
  records an unbounded baseline/control run, then assigns the fault child to a
  Windows Job before releasing its literal `go` barrier. That Job has
  active-process limit one, kill-on-close, and equal process/job memory caps
  derived from the observed baseline but strictly below the control peak.
- Verification: a post-commit locked release wheel was built outside the
  source tree (`08438587...60269e`) and the external report
  `pybert-production-wheel-memory-b6fd1ef/task13-reliability.json` records a
  completed control peak of 527,179,776 bytes. The fault lane loaded the same
  native contract and entered the public workload, then ended nonzero
  (`3221226505`) with a 15,556,608-byte cap, 15,319,040-byte Job peak, one
  total process, zero active processes, and no success artifact. Focused
  source tests passed 17/17.
- Independent audit: OpenCode `msg_e1e196e7147e` reports 0 P1 / 0 P2. Its P3
  records the intentionally explicit wheel/flag and baseline-derived fault
  threshold. OMP request `msg_506a1680f3b9` was delivered but had no returned
  conclusion at this checkpoint; per the user-approved one-review continuation
  rule, it remains a pending supplemental review rather than fabricated dual
  acceptance.

Conclusion: accepted only as one Windows public-production-wheel hard-memory
containment path. It does not add a production panic trigger, establish
production native-extension panic/OOM parity, approve an RSS/performance
budget, certify a remote PyBERT wheel, alter the default route, prove RFM or
Link parity, or close Task 13/M5B-01.
