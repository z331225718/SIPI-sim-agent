# M5 Audit Record

## M5A-06a (Agent-Spice migration preflight)

- Commit: `db3889a` (`feat(m5a): lock agent-spice migration preflight`).
- Source anchor: `agent-spice@2cc92316c2fb89a159f18fcb1ff2ba249f0e22f5`, tree
  `b6bde97128030d6cea0d68b2f0a35d807be8c402`; baseline tag
  `sipi-baseline/agent-spice/20260807.2` resolves to
  `80f66a384b1b4b52b91390dc40f12a7af6cf0e2a` and is an ancestor.
- `docs/baselines/migrations/m5a-agent-spice-preflight.v1.json` fixes the
  target prefix and keep/drop policy. It retains the production Python/Rust
  sources, build metadata, docs, tests, and C# reference while excluding
  `third_party`, external-reference assets, local native binaries, generated
  output, and unrelated tooling history.
- `tools/verify_m5a_agent_spice_preflight.py` validates source commit/tree,
  baseline commit/tree/ancestry, evidence SHA-256 values, path policy, and
  restricted-path exclusion. The real source report was ready with 371 retained
  paths and 651 dropped paths. `tools/test_m5a_agent_spice_preflight.py` covers
  ready, missing restricted drop, and source tree mismatch cases.
- `license-manifest.v1.yaml` records the M5A source anchor, aligns the
  production source scope, refreshes the Rust crate evidence hash, and marks
  locally built native binaries `external_reference_only`.
- Verification: `tools/verify_m5a_agent_spice_preflight.py --source
  C:\Users\z3312\code\agent-spice`, `tools/verify_license_manifest.py`, and
  `tools/run_all_tests.py` (55/55) passed.
- Independent audit: OMP `term_e75ea1bb-ada5-4b90-8ad1-3b6c7c6eaa32`, response
  `msg_681a317f48a4`, 0 P1 / 0 P2.
- Cross-audit: independent review agent
  `term_b0c8b68d-db95-478e-b221-f3394dfef1fc`, response
  `msg_668804ab5a9b`, 0 P1 / 0 P2.

Conclusion: accepted. M5A-06b may create and filter its temporary clone, but
must rerun this preflight before merging the rewritten history.
