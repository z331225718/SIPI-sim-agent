# P5-06 Original-13 Scalar Reference Custody

This additive record accepts only the current, user-selected original-13 final scalar surface. It binds two fresh MATLAB R2024b engine replays and two fresh public-root Rust `sipi com run` replays already captured by the v6 matrix.

The record reconstructs the exact normalized workbook/channel/parameter materialization identity from the MATLAB reports and encodes each scalar slot in explicit `(workbook_index, case_index, metric_name, value_tag, f64le)` order. MATLAB and Rust each have a repeat-stable bundle; Rust is compared to MATLAB without alignment at an absolute `1e-9` tolerance. The v6 aggregate separately proves Rust is not slower for every workbook or in total.

This does not change `com-r480-acceptance.v1`, does not claim its historical three-metric contract, and does not accept arrays, intermediate checkpoints, warnings, a full result graph, generalized COM, IEEE certification, or release.
