# SPEC v0.2 原生 MIT Rust 重基线审计

- 日期：2026-08-09
- 被审计提交：`e6e13e1cd06539e9ae7d29b5b951169aef272bef`
- 父提交：`9f9c76a`
- 范围：`SPEC.md`、`PLAN.md`、`README.md`、`docs/adr/ADR-011-native-mit-rust-product-boundary.md`
- 结论：accepted，0 P1 / 0 P2

## 变更边界

该提交将产品终态从“Python 控制面 + 旧引擎渐进迁入”重基线为：

- 第一方可发布源码 MIT，第三方 compatible 依赖保留自身许可证/NOTICE/SBOM。
- Rust-only 产品 runtime、领域内核、contracts、artifacts、pipeline 和单一公开 `sipi` CLI。
- 非 MIT/BSD 第一方实现只作隔离 black-box oracle，由独立规格驱动 clean-room Rust 实现。
- 供应商 DLL/IBIS/AMI/私有 corpus 默认 external-only，不进入 Git 或发行包。
- 三个旧项目的准确性按 profile/stage/tolerance/provenance/non-claims 认证。
- 旧 Python adapters、bundles、nightly 和历史迁入工具降级为迁移/验证设施。

## 本地验证

```text
uv run python -B tools/run_all_tests.py
63 runs, 0 failed

git diff --check
通过（仅 Git 的 LF/CRLF 工作树提示）
```

测试过程中 `uv run` 会按当前 `pyproject.toml` 尝试同步 `uv.lock` 的 editable dependency 元数据；该非任务副作用已恢复，审计锚点和最终工作树均干净。

## OMP 独立审计

- 请求消息：`msg_d3e1ef6baab9`
- 结论消息：`msg_da2172e91f17`
- 结论：0 P1 / 0 P2

OMP 核验结论：

1. 第一方 MIT 与 MIT-compatible 第三方、外部 vendor 资产的边界诚实，未把第三方许可证改写为 MIT。
2. 终止非 MIT/BSD 历史 filter-repo 迁入，并定义观察侧/实现侧 clean-room 隔离，没有伪许可升级。
3. `sipi` 单一公开 CLI 与 P0-P7 依赖/退出条件一致、可执行。
4. 旧 M3-M5 进程封装和 adapter 被诚实重分类，未写成 Rust-only 产品能力。
5. 准确性声明绑定 profile/stage/tolerance/non-claims，并保持唯一 production crate/resolver。
6. 四份文档无 P1/P2 级矛盾或虚假能力宣称；全量测试通过。

## OpenCode 交叉审计状态

- 请求消息：`msg_e876e0a9b126`
- 补充消息：`msg_50460c3b82b9`
- 落库时状态：not awaited per user decision；未返回的结论不视为通过。

用户已明确允许在两个审计方中任一可用方给出 0 P1/0 P2 后继续，并在本轮进一步确认无需等待 OpenCode。其后若返回 P1/P2 finding，仍需单独核验、修复并补审。

## 验收与非宣称

本次接受的是产品规格、路线和迁移处置的一致性，不表示：

- 根 MIT 许可证、product-boundary manifest 或 clean-room role register 已完成。
- 现有 `sipi-circuit`/`sipi-ami` 已取得最终产品资格。
- TRAN、Channel、IBIS-AMI、COM 已达到 Rust-only certified 状态。
- vendor asset 可公开分发，或 Linux/macOS 已认证。
- 任一旧 profile 的证据可外推为通用数值能力。

下一实施边界是 PLAN P0-A：根 MIT、tracked-path product boundary 和 fail-closed verifier。
