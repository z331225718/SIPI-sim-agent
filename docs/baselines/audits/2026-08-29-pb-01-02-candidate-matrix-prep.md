# PB-01/PB-02 Candidate Matrix Preparation Audit

Status: preparation only. No formal replay report, aggregate, reciprocal audit,
or parity claim is generated by this preparation.

## Boundary

This gate prepares a future two-run replay of the already implemented
`crates/sipi-pybert-direct` PB-01 and PB-02 branches. It does not add a
simulation feature, change production Rust, close a migration row, admit a
product capability, or make a release or license decision. The fixed
production candidate is commit `0d57b36f965588bed3393d2a0a529e4d573a493c`.
The interposed mainline checkpoint is commit
`2dea3bbe6039224704f1e07aebc3ebd6c178abed`; its exact 0d57-to-checkpoint
name-status and protected PB blob equality are part of the machine gate.

The preparation commit must be a direct single-parent child of that checkpoint and
must introduce exactly the five manifest-listed `100644` blobs: the candidate
matrix runner, this preparation verifier and test, and the preparation
manifest/audit. The verifier checks first introduction, raw Git blob bytes,
live-file equality, bounded archive bytes, and absence of the future formal
record paths. Unrelated dirty files are not part of this PB gate and are not
silently treated as PB inputs.

## Provenance

The candidate and the pinned PyBERT source are materialized only from bounded
Git archives. The historical Stage-1 commit is retained as the source of the
existing patch, process-capture, and strict comparator primitives; it is not
used as the production candidate. Both base fixtures and every derived matrix
input are content-addressed to the candidate archive and to the exact patch
from `pb-01-02-portable-matrix-inputs.v1.json`.

PB-01 Duo-binary analytic-line selected arrays passed the diagnostic comparison
across the dictionary/class codec boundary; class-pickle byte parity is not
claimed. PB-02 NRZ/PAM4/Duo retained strict metadata blockers despite matching
logical arrays. The equalization case retains the strict fixed NPZ member-set
blocker, the CTLE case retains the local impulse-response extension and output
drift blockers, and the 16-bit PRBS-7 jitter case retains the invalid-span and
missing-output blockers. These diagnostic observations are not formal evidence.

## Future Gate

The runner is bound to the fixed IDs
`pb-01-02-candidate-run-01` and `pb-01-02-candidate-run-02`. Each future report
must carry the matrix challenge ID, run index/count, a fresh 32-byte nonce, and
its own report digest. The two runs must use fresh archive roots, identical
path-free tool identities, archive custody, and the strict PB-01/PB-02
comparators. Formal report/aggregate/audit paths are intentionally absent in
this preparation batch; a later record gate must be a separate reviewed step
and must establish its own reciprocal bindings.

The preparation manifest and verifier are the machine-readable source of these
constraints. The verifier must pass the complete synthetic baseline and its
coordination mutations before the preparation commit is eligible for review.
