# COM ERL exact-profile replay v3 preparation

Status: preparation only. No formal replay report, aggregate, or parity claim is generated by this preparation.

The successor harness is locked to candidate `955835369c28aacb3f49bd0cf95f3660587739d8` (tree `fbb415e5582ccf3ee70e4d0d472f1fc6856e0e26`, archive SHA256 `005e06d1ab680a7ddf41a2110c5e8ea9e44b020e04c5d4369a78f05c15ea79f0`) and upstream `5272ffe74702cd585054d975559b06f8afae7b6e` (tree `7094ab6e84989b218730c52432c70da10261f8ea`, archive SHA256 `a7bbe0e019d5ce4d7b47246b6f0daccdd3cfc8f27a471b03eb50e8c751082ccf`). Both are materialized with the resolved `git archive` binary and no working-tree overlay.

The fixture gate is exact: `crates/sipi-agent-com-direct/tests/fixtures/erl_s2p_10db_at_26p56ghz.s2p`, 830969 bytes, 8001 rows, SHA256 `7a59b41a385a95752d2d1159aab7772a10f7bd2c07e5122d770493151853c7a3`. Candidate and upstream receive independent read-only copies. The channel policy is raw S11 FD-to-TD impulse; S-parameter fitting is forbidden.

The runtime gate is release/locked candidate build with explicit `RUSTC`, cleared `RUSTC_WRAPPER`, offline `uv run --frozen` against the pinned `uv.lock`, and 180-second runtime limits. Cold release build has a 900-second bound. Tool identities include path-free basename, file SHA256, version-output SHA256, exit status, timeout, and redaction state.

The upstream ERL-only API exposes `ptdr_gated` but not raw `ptdr` in its public diagnostics surface. Raw PTDR is therefore diagnostic-only and never guessed; the only stage comparison admitted by the schema is upstream `ptdr_gated` to candidate `gated` with count and little-endian f64 SHA256. Numeric parity remains open until both metric and stage payloads match under the successor verifier.

The upstream control payload is emitted from `config.materialize()` and retained as raw source fields plus an explicit runtime crosswalk. Worksheet fields such as `Z0`, `T_r_filter_type`, `T_r_meas_point`, `T_k`, `TDR_duration`, and `Tukey_Window` are not silently relabeled as executed candidate controls.

Binary custody requires raw SHA256, canonical SHA256, shape, and REPRO binding. Aggregation is fail-closed on identity drift, tool drift, nonce/report collisions, path leakage, fixture/control/stage mismatch, or missing custody. A future harness-prep commit/tree/archive must be bound before formal replay.
