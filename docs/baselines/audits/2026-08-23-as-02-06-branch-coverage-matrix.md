# AS-02..AS-06 reachable branch coverage matrix

Authority: Agent-Spice commit `2cc92316c2fb89a159f18fcb1ff2ba249f0e22f5`,
tree `b6bde97128030d6cea0d68b2f0a35d807be8c402`, MIT.  This matrix is the
post-second-pass inventory. `implemented` means the Rust leaf executes the
portable branch locally; `external-only` means only the caller-owned solver
process/runtime is outside the leaf. Actual external execution is currently
fail-closed before PATH/alias lookup. Any numerical parity mismatch remains
open rather than being narrowed into a claim.

| Row | Upstream reachable path | Implemented portable branches | External-only branches | Missing portable branches |
| --- | --- | --- | --- |
| AS-02 | `sparam/cascade.py`: ordered `.s2p` block admission, shared frequency intersection, priority-band-only/full-band gates, passivity adjustment, bounded cascade RMS refit, per-block S/RFM/wrapper/HTML artifacts | two-port admission boundary, native vector-fitting pole relocation/residue refit, priority/full gates, Hamiltonian crossover and adaptive violation-band sampling, constant/residue/pole-damping coordinate optimization, block/cascade RMS guards, non-blocking full-band postcheck, impact-ranked refit with accepted/rejected history and artifact restore, Z0 renormalization, physical-frequency RFM and row-major-safe Touchstone/SPICE/RFM/wrapper/HTML artifacts | none | none; continuous Hamiltonian/numeric parity is explicitly open evidence, not a hidden claim |
| AS-03 | `sparam/yparam.py`: arbitrary `nports`, strict S/Y conversion, auto-order search, sampled Y positive-real check, Y SPICE/HTML/derived-S outputs; `native_vf.py`/`pole_relocation.py`; `rational_lft.py` and CLI exact S RFM/wrapper delivery | bounded n-port conversion/fit/check, column-major-to-row-major delivery remap, native vector-fitting pole relocation/residue refit, common-ground Y SPICE with physical-frequency de-normalization and split complex residues, HTML, derived S, proper-rational state-space LFT, native real KYP-LMI P solve/full matrix audit with bounded C/D correction, exact S Touchstone/RFM/wrapper | none for the portable branch; native solver identity is not invoked | none in the reachable CLI input contract; descriptor/proportional exact input is rejected by upstream and remains fail-closed |
| AS-04 | `sparam/y_tran_tuning.py`: response grouping, static gates, exact token replacement, bounded trial artifacts, SciPy Nelder-Mead, measure parsing and best-RFM publication | deterministic bounded Nelder-Mead/simplex, response grouping, static gates, exact replacement, measure parsing and trial history | commercial HSPICE executable/measure runtime | none |
| AS-05 | `cli.run_hspice`: `.alter`, quote-safe audit/converter, recursive dependency staging, touchstone S-element admission boundary, compat manifest, `project.py` manifest/run directories, native/ngspice/Xyce/XDM dispatch, backend logs/result/measure/waveform summaries | bounded project YAML parse and `<root>/<deck>/<case>` layout, case/dependency staging with budgets, v2 compat schema, nonblocking `.model`/`.endcomment` audit, explicit unsupported S-element path when no pinned S-domain exporter exists (original line preserved, no AS-03 Y substitution), conversion/probe/measure inventory, native result validation, XDM plan/log contracts | native/ngspice/Xyce/`xdm_bdl` processes, all actual external execution pending executable custody | S-domain S-element exporter is unavailable in pinned source; unsupported deck semantics and missing custody fail closed |
| AS-06 | `sparam/rfm.py` + `rfm_ngspice.py`: RFM parser/normalizer, native/ngspice wrapper, recursive dependency staging, injected converted deck, manifest, waveform/result summaries | conjugate-aware RFM evaluation, pole union/normalization, Cadence/XSPICE wrapper, deterministic ngspice deck conversion, injected deck including terminal `.end*comment` handling, recursive dependency staging, SHA-256 manifest, result-contract parsing | native engine and ngspice XSPICE code-model process; actual execution fail-closed before PATH/alias lookup | none in portable parse/convert/dispatch-plan branches |

Numerical parity against the pinned Python/NumPy/SciPy/scikit-rf implementation
is not inferred from structural coverage.  Any replay mismatch remains open.
