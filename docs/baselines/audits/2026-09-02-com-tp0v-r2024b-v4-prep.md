# COM TP0V R2024b v4 preparation audit

This is an additive preparation gate. It does not create, bless, or replace
the v3 exact preparation or the historical exact ERL record.

## Frozen inputs

The v4 candidate is intentionally unbound until the additive gate commit is
created. Its required gate parent is commit
`bd9a26195a6c204e31b2601b138da66e7ecad150`, tree
`b12a799cb97a065b9f6d896ace4c40d7a3b35bac`; a later candidate receipt must
bind the archive to that gate and is not represented by the old v3 candidate.
The upstream Agent-COM archive is pinned to commit
`5272ffe74702cd585054d975559b06f8afae7b6e`, tree
`7094ab6e84989b218730c52432c70da10261f8ea`, and archive SHA-256
`a7bbe0e019d5ce4d7b47246b6f0daccdd3cfc8f27a471b03eb50e8c751082ccf`.

Each future acceptance run must use a fresh archive root, build/output root,
and 64-hex nonce. The four fixed replay identities are `matlab-01`,
`matlab-02`, `rust-01`, and `rust-02`; each replay itself runs two exact
repeats. Reports and the aggregate remain outside the repository until all
gates are closed.

## Comparison contract

The current TP0V profile compares the ordered scalar surface for the selected
package case (case index `0`) with the existing `1e-9` finite absolute
tolerance. The trace source proves package testcase index `0` only. Its two
entries are physical ports 1 and 2, not two package cases; therefore v4 binds
the one selected package case to port 1 and does not copy port 2 into case 1.
Vector comparison is one-dimensional, element-by-element, with exact shape,
order, count, first-sample presence, finite values, and strictly increasing
`time_s`. The only tolerances are:

| Vector | Absolute | Relative | Unit |
| --- | ---: | ---: | --- |
| `time_s` | `5e-24` | `0` | s |
| `impedance_ohm` | `2e-12` | `2e-14` | ohm |
| `ptdr` | `5e-17` | `2e-14` | linear |
| `gated` | `5e-17` | `2e-14` | linear |

The limit is `absolute + relative * abs(reference)`. No alignment,
resampling, interpolation, truncation, or delay correction is permitted.
Each engine must reproduce its own payload exactly on the second repeat before
cross-engine comparison is considered.

## Runtime and D3 policy

MATLAB is explicit R2024b only, launched with `-noFigureWindows
-singleCompThread`, a fresh `MATLAB_PREFDIR`, and `MW_DISABLE_CONNECTOR=1`.
The workbook-to-MAT bridge is materialized and reloaded by MATLAB; its logical
shape, row, column, kind, and value projection is exact. Rust must use the
public `sipi com run` route and the `com-direct-integration` feature. Raw
normal-ERL vectors are diagnostic sidecars only: Rust uses the opt-in
`normal-erl-diagnostic-sidecar` feature and its
`sipi.com.normal-erl-array-sidecar.v1` f64le receipt; MATLAB uses the existing
archive-local trace wrapper/sink and checks the uninstrumented versus
instrumented scalar/warning semantics before reading the trace vectors. The
The legacy MATLAB sink writes raw f64 files and the v4 runner adds a SHA-256
receipt over those unchanged bytes before consuming them. The selected port
is port 1. Neither trace is allowed to alter the semantic run,
and trace work is excluded from the performance measurement. Semantic timing
is recorded only around the uninstrumented MATLAB core invocation and the
sidecar-disabled Rust root invocation; the diagnostic trace timings are
recorded separately and never used for the performance gate. Runtime
performance is a gate: Rust wall time must be strictly lower than MATLAB wall
time for the selected case. Instrumented traces are excluded from timing.

All selected configurations set `COMPUTE_TDILN=0`. D3 remains globally
enabled, but its selected configuration is deliberately not evaluated; the
recorded status is `not_evaluated_configuration_disables_tdiln`. This is not a
substitute for a TDILN result and does not close global D3.

The v4 runner and aggregate are reusable diagnostic tools. The v4 manifest is
`preparation_only_pending_candidate_gate_commit`; no formal reports or
acceptance claim are present. A future candidate gate must first bind the
candidate archive; only then may a formal step create the four independent
replays, run the aggregate, and pass the verifier without changing this
contract.
