# P1-04B Comparison Permission Reconciliation

Owner 已明确外部 fixtures 可被信任用于 comparison，因此“不允许比较”不再是
P1-04B 的 owner blocker。本记录只迁移 blocker 分类：14 项资产仍全部
`blocked_unknown`，required profile、外部 custody 与消费/分发权利均没有被该决定补齐。

`trusted for comparison` 不等于把 bytes 纳入产品、runtime、发布包或获得再分发许可。
P1-04B 保持 open，并由 `external_asset_oracle` 类外部事实继续阻塞。
