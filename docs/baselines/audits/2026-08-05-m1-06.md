# M1-06 External Evidence Audit Handoff

功能审计对象：SIPI 提交 `aa21d17`，外部源快照见 `docs/baselines/source-snapshot.v1.json`。复验工具以包含本记录的 handoff commit 为准；相对 `aa21d17` 的 executable delta 仅为 PyBERT probe 的运行后 source recheck，不改变 contract、ledger 或矩阵。本交接记录仅接受固定快照中可达的 producer/consumer 边界证据；不会将仅有 synthetic struct/fixture round-trip、且不存在可达生产路径的类型提升为 covered。

| Check | Result | Evidence |
| --- | --- | --- |
| 外部义务清单完整 | pass | `tools/verify_m1_rule_ledger.py` 无 missing obligation、unexpected obligation 或 orphan case |
| 已覆盖外部契约 | pass | PyBERT SimulationInputV1、PyBERT RunEventV1、agent-com ProgressEvent、agent-com result v1 的 producer/consumer 均通过固定快照运行器与同字节 lineage 复验 |
| 来源绑定 | pass | 每个外部运行器均验证 repo HEAD/tree、关键 lock hash、依赖路径及运行后工作树状态 |
| 本地运行器隔离 | pass | suite schema 与 verifier 对 external runner 和 external contract 双重 fail-closed 绑定 |
| 完整 conformance | pass | `python -B tools/verify_m1_conformance.py` 无 failures；矩阵为 `11 covered / 30 partial / 10 gap` |
| M1-07 生命周期边界 | pass | 外部 probes 不调用 publish、artifact staging、HTML/NPZ 渲染或数值仿真；agent-com result 仅使用纯内存 payload |
| M1-06 全部外部义务覆盖 | open | 10 个 gap 仍有明确的真实生产/消费路径或方向模型缺口，见下表 |

## Declared Gaps

| External obligation | Missing role / evidence | Owner | Release condition |
| --- | --- | --- | --- |
| PyBERT ChannelResponseV1 producer/consumer (Rust) | `validate()` 私有；源码声明 JSON 仅 fixture/IPC，生产数组经 PyO3 buffer；没有独立真实 producer | PyBERT | 找到固定快照中的真实 buffer producer/consumer 边界并为其建立可复验探针 |
| PyBERT ArtifactRefV1 producer/consumer (Rust) | `validate()` 私有；simulation 当前固定输出空 artifacts，非空手工构造不是生产证据 | PyBERT / M1-07 | 实现并固定真实 artifact producer 后，在生命周期边界完成消费证据 |
| Agent-Spice RFM producer/consumer (Python/Rust) | authority 当前按 bindings 笛卡尔积要求 Python producer/Rust consumer；真实路径仅 Rust producer -> Python consumer，且 native source 非 clean snapshot | Agent-Spice / authority audit | 独立批准方向化 binding 模型，并提供 clean pinned native snapshot 后重建同字节 Rust->Python probe |
| agent-com behavior registry producer/consumer (Python) | 有 `BehaviorProfile.from_name()` consumer，无 registry writer/producer | agent-com | 引入并固定真实 registry 生成路径，或在 authority 中将 producer 标明为不存在 |

## Conclusion

建议独立审计结论：**conditional pass / checkpoint accepted**。M1-06 的外部取证清单已完整，现有 covered evidence 通过复验，剩余缺口已被 ledger 如实声明；`suite.status` 保持 `incremental_partial`，不得据此宣称完整 conformance 或 capability advertisement。下一步由 M1 外部独立审计复跑本记录和固定快照；签核后再进入 M1-07。
