# AS-02/AS-03 numeric bound v2 audit

Each row has two fresh runs from the immutable 9914a23dc747d94405f2fed9227538ae7aea8629 candidate archive and pinned Agent-Spice archive. The upstream CLI and candidate binaries both executed successfully. Numeric mismatches remain open; no parity or acceptance claim is made. Execution diagnostics are content-addressed (SHA-256 and byte counts only); stdout/stderr tails are intentionally omitted so temporary host paths cannot enter the evidence.
