# P5-06 Current Rust Performance Rebind

Current candidate `db025081` preserves source-order floating-point evaluation while
skipping zero-mass PDF terms. Two fresh public-root `sipi com run` original-13
replays completed all 13 workbooks, 28 package cases, and 509 scalar slots.

The two current Rust reports reproduce the prior fixed Rust scalar surface exactly.
The reference MATLAB R2024b reports are immutable upstream-oracle measurements: the
candidate change is Rust-only and does not alter their inputs, archive, MATLAB release,
or engine launch contract. The verifier checks both current Rust reports against both
fixed MATLAB reports at `1e-9`, and requires Rust's worst time not to exceed MATLAB's
best time for every workbook and for the whole matrix.

This accepts only the owner-selected original-13 scalar/performance route. It does not
claim broader upstream workflow completion, arrays/checkpoints, S-parameter fitting,
release readiness, or IEEE certification.
