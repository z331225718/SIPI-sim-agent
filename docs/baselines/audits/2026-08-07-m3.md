# M3 Platform MVP Audit Handoff

审计对象：SIPI M3（Platform MVP）实现切片。本记录只接受每切片经独立审计（CodeBuddy/Orca run）的提交作为证据；每个切片单独提交、单独审计。

| 切片 | 提交 | 独立审计（Orca run） | 结论 |
| --- | --- | --- | --- |
| M3-01 定义 `sipi.project.v1` | `b06c051` | `run_53bb61a24504` | 0 P1 + 2 P2，已修复（`152d54a`，复审 0 P2） |
| M3-02 路径解析 + 输入 hash + resolved request | `70c5fbf` | `run_7809e14ded18` | 0 P1 + 1 P2，已修复（`ccbfa28`，复审 0 P2） |
| M3-03 本地分析 DAG（拓扑/环、effective_required、cache identity） | `fbab7d4` | `run_d2d6c5e7b137` | 0 P1 + 1 P2，已修复（`03b976d`，复审 0 P2） |
| M3-04a SQLite 单写者 supervisor registry（CAS/idempotency/lease） | `cff34f3` | `run_4a1efd58c95e` | 0 P1 + 1 P2，已修复（`00dfcd0`，复审 0 P2） |
| M3-04b supervisor 协调核心（singleton 锁 + cancel/publish 协议） | `3275a37` | `run_6ce2d5e4e41a` | 0 P1 / 0 P2（首轮通过） |
| M3-04b3 supervisor IPC + restart reconciliation | `d553c31` | `run_67941c98c228` | 0 P1 + 1 P2，已修复（`938c462`，复审 0 P2） |
| M3-05a `sipi validate/run/status/cancel/retry` CLI + supervisor daemon | `11831b3` | `run_58105df98106` | 0 P1 + 2 P2，已修复（`8a46d30`，复审 0 P2） |
| M3-05b 本地执行驱动（DAG 调度、bound_inputs、publish、aggregation） | `77ffc9d` | `run_220bbd6562a3` | 0 P1 + 2 P2，已修复（`ab1286a`，复审 0 P2） |
| M3-06 运行报告 + compare 组装（`sipi report`） | `ad6dcdc` | `run_a2bdc8bb1d8b` | 0 P1 + 1 P2，已修复（`935183a`，复审 0 P2） |
| M3-09a supervisor 循环接入 ExecutionDriver（submit→后台执行） | `9f0906e` | `run_42dc2aa9d2b8` | 1 P1 + 1 P2，已修复（`c44c37b`/`b5ef31b`，复审 0 P2） |
| M3-09b 内容寻址 cache reuse（CacheStore + driver 接入） | `4d50596` | `run_e2636dca21ab` | 0 P1 + 2 P2，已修复（`0150b91`，复审 0 P2） |
| M3-09c 进程树管理 + cancel 终止 + reconcile child identity | `ab1b4c7` | `run_8bb0b5a84297` | 1 P1 + 2 P2，已修复（`e1110a3`，复审 0 P2） |
| M3-07/08a 三引擎示例项目 + RFM→Link 绑定示例 | `405c291` | `run_675b2e5395d0` | 0 P1 / 0 P2（首轮通过） |

## M3-01（`sipi.project.v1`）

- `schemas/project.v1.schema.json`：严格项目输入；analysis 使用 discriminated operation；payload 与 payload_artifact 互斥；路径相对且不可逃逸；inputs 绑定 `from_analysis + artifact_role + expected_schema`；`backend_selection` 可省略并回退 `runtime.backend_defaults`（完整判别对象，无继承歧义）。
- `ProjectV1` 冻结模型 + `parse_project`；语义校验：analysis id 唯一、depends_on 已声明且非自引用、binding 上游已声明、`artifact_role` 命中上游 export、`expected_schema` 与上游 export schema 一致、export role 唯一、缺 selection 时报 `missing_selection`。
- authority.v1.yaml 登记为 `local_json_schema`；rule-ledger `authority_sha256` 同步（M1-06 义务集不受影响）。
- 证据：`tests/contract/test_project.py` 11 项；全量 26/26。

## M3-02（路径解析与输入 hash）

- `packages/sipi-runtime/src/sipi_runtime/resolution.py`：`resolve_project(project, root)` 将 engine_lock/payload_artifact 经 `resolve_artifact_path` 解析并哈希（缺失/逃逸/符号链接逃逸拒绝）；内联 payload 取 canonical JSON sha256；每个 analysis 落定完整 `backend_selection`（analysis 优先、runtime 默认兜底，记录 `selection_source`）；`project_hash` 定义为不含自身字段的 content wire sha256（可下游复算）。
- 未改写领域 payload；未物化 bound_inputs（M3-03）；未做 DAG 拓扑（M3-03）。
- 证据：`tests/runtime/test_resolution.py` 9 项；全量 27/27；`sipi doctor` schemas=ok。

## M3-03（本地分析 DAG）

- `packages/sipi-runtime/src/sipi_runtime/dag.py`：`plan_dag(resolved, registry)` 产出确定性执行计划——Kahn 拓扑序与长链环检测（`DagCycleError`）、`effective_required` 闭包（required 节点的完整依赖闭包，optional 不削弱必需上游）、每节点经 `resolve_selection` 落定选择/`selection_hash`/`execution_modes`、预绑定 cache key（`cache_key_prebound`）。
- `cache_identity` 为内容寻址：canonical payload、完整 selection、bundle hashes（顺序无关）、声明式 input binding；执行期再绑定上游 artifact hash、resource_policy、randomness（M3-05）；执行层 ID 与 lineage 一律排除。
- 未越界 M3-04（SQLite/supervisor）与 M1-09 生命周期；bound_inputs 物化留执行层。
- 证据：`tests/runtime/test_dag.py` 9 项；全量 28/28。

## M3-04a（SQLite supervisor registry）

- `packages/sipi-runtime/src/sipi_runtime/supervisor_registry.py`：四层表 executions/nodes/attempts/backend_executions（run_id+project_hash+retry_of+version+submission key、node 复合唯一、attempt 全局唯一、backend 绑定 role+instance+child identity）；`BEGIN IMMEDIATE` 单写者事务；所有变更 `WHERE version = expected` CAS（含复合键路径）；终态行拒绝一切变更（terminal 后 cancel = already-terminal）；submission 幂等（同 key+同 hash 重放原 run_id、异 hash 拒绝）；lease/heartbeat 与跨重开持久化。
- 未越界 M3-04b（singleton/IPC/supervisor 循环/reconciliation）。
- 证据：`tests/runtime/test_supervisor_registry.py` 8 项；全量 29/29。

## M3-04b（supervisor 协调核心）

- `packages/sipi-runtime/src/sipi_runtime/supervisor.py`：`SupervisorLock`（Windows `msvcrt.locking(LK_NBLCK)` / POSIX `flock(LOCK_EX|LOCK_NB)`，第二实例 `SupervisorBusy`，释放幂等）；`Supervisor` 组合 registry 并提供 `publish_success`/`request_cancel`。
- registry 扩展：`attempt_publish_cas`（prepare/commit 线性化：attempt 必须 `publishing`、version CAS、execution/node/attempt 无已接受 cancel 才提交 success+manifest，SQLite commit 为成功线性化点；五态返回机）；`cancel_scope`（单写者事务内对 execution/node/active attempt CAS 写 `cancel_requested`，run_id 或 analysis 作用域，含 unknown/already_terminal/already_requested）。
- 未越界 M3-04b3（IPC/进程树/supervisor 循环/reconciliation）。
- 证据：`tests/runtime/test_supervisor.py` 8 项；全量 30/30。

## M3-04b3（IPC + reconciliation）

- `packages/sipi-runtime/src/sipi_runtime/supervisor_ipc.py`：`SupervisorServer`/`SupervisorClient`——Windows `AF_PIPE`（当前用户 named pipe）/POSIX `AF_UNIX`，地址按 data_dir 确定性生成、无网络端口；消息为 JSON bytes（非 pickle）；服务器先持 singleton 锁再绑定端点（第二实例 `SupervisorBusy`）；客户端无服务 `SupervisorUnavailable`；命令面 ping/submit/status/cancel，异常仅回 `{category, message}`。
- `Supervisor.reconcile()`：单写者事务内过期 lease 非终态 execution 按 `cancel_requested` 判 cancelled/failed（版本 CAS、幂等），publishing+cancel 的 attempt 归 cancelled；EngineProtocolFailure 与 child identity 匹配明确 defer 至 M3-09 失败注入。
- 进程树/Job Object/信号留 M3-09（本切片不越界声称）；close 释放 listener/lock/registry。
- 证据：`tests/runtime/test_supervisor_ipc.py` 9 项；全量 31/31。

## M3-05a（CLI 接线 + supervisor daemon）

- `packages/sipi-runtime/src/sipi_runtime/supervisor_main.py`：`python -m sipi_runtime.supervisor_main --data-dir <dir>` 守护进程（singleton 锁 + reconcile + IPC 绑定 + SIGINT/SIGTERM/shutdown 优雅退出）；`spawn_supervisor` 以 `CREATE_NO_WINDOW` 拉起独立进程并等 ping。
- `sipi validate`：只读 parse+resolve+engine.lock 加载+plan_dag 预检，失败 exit 1 且不覆盖原文件；`run`（默认连守护进程、`--foreground` 进程内 server、`--detach` 立即返回 run_id、非 detach 轮询终态且 Ctrl-C cancel、`--wait-timeout-s` 有界等待）；`status`/`cancel`/`retry`（新 run_id+`retry_of`+新 submission key）均经 IPC。
- run 默认 submission key=project_hash（幂等重放），retry 用新 key；SupervisorUnavailable 一律 exit 1 干净退出。
- 未执行引擎数值（run 仅提交 registry 行，驱动属后续切片）；未实现 M3-06+。
- 证据：`tests/runtime/test_cli_m3.py` 7 项（含 daemon spawn/shutdown）；全量 32/32。

## M3-05b（本地执行驱动）

- `packages/sipi-runtime/src/sipi_runtime/driver.py`：`ExecutionDriver` 按拓扑序驱动节点——node pending/ready/attempt/backend 状态经 registry CAS；上游 failed/cancelled/blocked 按 failure policy 阻塞下游；auto retry 受 `max_attempts` 约束（耗尽后 node 终态）；attempt 循环开始前重查 cancel。
- bound_inputs：上游 success 产物按 `from_analysis+artifact_role` 物化，`relative_path` 重写为运行目录相对路径且 sha256 保留；publish：attempt→publishing→`attempt_publish_cas`（cancel 栅栏）→success-manifest 落盘（manifest sha 与 registry 同源；顶层 run_record/checksums digest 占位，M3-09 接线真实值）。
- aggregation：cancel 优先 → cancelled；required failed/blocked → failed；optional 失败不 fail execution。
- 未越界 cache reuse/进程树（M3-09）；`sipi-runtime` 新增 `sipi-adapters` 依赖（adapter SPI 调用）。
- 证据：`tests/runtime/test_driver.py` 6 项（成功/阻塞/cancel 前置/cancel 执行中/retry/optional）；全量 33/33。

## M3-06（运行报告与 compare）

- `packages/sipi-runtime/src/sipi_runtime/report.py`：`build_run_report` 按 analysis 分区输出 status/attempt_count/artifacts/success_manifest_sha256 + provenance 摘要（project_hash、engine_lock sha）；compare 组装仅在 compare 模式且 node succeeded 时执行，从 `{attempt_id}-reference/-candidate/out/meta.json` 读取并按 profile（默认 `effective_input.sample_count`）经 `compare_results` 判定；domain_result_schema 从 meta.json 派生，bundle_hash 合成值注明 M3-07/08 从真实 backend 记录派生。
- `sipi report <run_id>`：只读输出报告，失败 exit 1。
- 未越界 M3-07+（示例项目/桥接），不改写领域数据。
- 证据：`tests/runtime/test_report.py` 2 项（compare 匹配、非 compare 分区）；全量 34/34。

## M3-09a（supervisor 执行循环）

- `SupervisorServer.submit` 收到 `project_root`/`project_path` 且服务端 `created=True`（`submit_execution_record` 单写者事务内权威标志）时，以 daemon 后台线程调用 runner，IPC 立即应答；submission 重放（含显式复用同一 run_id）不 re-drive。
- `supervisor_main.build_runner`：resolve+plan+`ExecutionDriver` 驱动；先校验 `resolved.project_hash == execution.project_hash`，不一致则 cas failed 不驱动；异常 best-effort 将 execution 置 failed。
- CLI `run`/`retry` 随 submit 携带 project_root/project_path；daemon `serve` 默认接 runner。
- 未越界 cache reuse/进程树（后续）；守护进程重启恢复依赖 M3-04b3 reconcile。
- 证据：`tests/runtime/test_cli_m3.py` 11 项（含 runner 到 succeeded、replay 不 rerun、hash 不一致、显式 run_id 重投）；全量 34/34。

## M3-09b（cache reuse）

- `packages/sipi-runtime/src/sipi_runtime/cache.py`：`CacheStore` 按 key 的 sha256 摘要落盘（原始 key 不入目录名，防路径注入）；`_check_relative` 可移植路径守卫（store/materialize 前校验，越界抛 `CachePathEscape`）；`lookup/store/materialize` 以 success-manifest + artifacts 为单元。
- driver 接入：命中时先 `materialize`（失败回落到正常执行）→ append attempt → `attempt_publish_cas(require_publishing=False)` 以原 manifest sha 落 succeeded（cancel 栅栏保留），不跑引擎且物化产物供下游 bound_inputs；未命中正常执行并把 manifest+artifacts 存入。
- cache identity 参与项：payload/selection/bundle/上游 artifact hash/resource/randomness；执行层 ID 与 lineage 排除；跨 run 复用经共享 cache_root（CountingAdapter 验证二次运行跳过引擎），payload 变化重新执行。
- 未越界进程树/restart（后续）。
- 证据：`tests/runtime/test_driver.py` 9 项（命中计数、payload 变化、逃逸守卫）；全量 34/34。

## M3-09c（进程树与恢复）

- `packages/sipi-runtime/src/sipi_runtime/process_tree.py`：Windows Job Object（`KILL_ON_JOB_CLOSE`）+ `taskkill /T` 回退 + POSIX `killpg`；`is_process_alive`（`STILL_ACTIVE=259`/`os.kill`）；`child_identity`（`GetProcessTimes` 起始时间 / `/proc/pid/stat`）；`matches_identity` 防 PID 重用误杀（start time 为 None 时保守返回 False）。
- `sipi-adapters.process.run_process` 改为 `Popen+communicate` 并支持 `on_start(pid)` 回调（timeout 语义保留），`execute_backend` 透传 `on_child_start`；driver 在 backend 启动时以 uuid run_token + bundle sha 记录真实 child identity 到 `backend_executions`。
- `Supervisor.request_cancel`：身份匹配才 `terminate_tree` 并 cas cancelled；`reconcile`：dead→failed、alive+匹配→reap+failed、alive+不匹配→`unmatched_alive_backends` warn-only 不误杀。
- 未越界 full managed worker（adapter 仍 process 隔离）。
- 证据：`tests/runtime/test_process_tree.py` 5 项 + `test_driver_records_backend_child_identity`；全量 35/35。

## M3-07/08a（示例项目与绑定）

- `examples/`：`circuit/rfm-deck`（`circuit.solve.v1`）、`channel/rfm-to-link`（`circuit.solve.v1`→`link.simulate.v1`，`rfm-response` 导出经 `from_analysis+artifact_role+expected_schema` 绑定）、`com/r480`（`com.r480.run.v1`）；三项目均 `sipi validate` 通过（1/2/1 分析），engine.lock 哈希/长度与 bundle 文件一致。
- bundle 为 stub（agent-spice/agent-com validate-only，pybert 可 stub 执行）；`examples/README.md` 诚实声明各执行状态与 M2 deferred 触发条件（managed lock + parity gate approved + 许可分类），未越界声称真实引擎可运行。
- M3-08 绑定语义由 `rfm-to-link` 示例固化（上游导出 role/schema 与下游 input binding 内部一致）。
- 证据：`tests/runtime/test_examples.py` 3 项；全量 36/36。

## Next

- M3 收尾：restart recovery 演练（daemon kill→reconcile→状态恢复）、`sipi run --foreground` 完善；M3-07b/08b：真实引擎示例（managed lock + 许可分类后替换 stub）。
