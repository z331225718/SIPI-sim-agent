# P3C-04x Owner C4 COM Metric-Profile Core - Audit Record

- Date (UTC): 2026-08-16
- Scope: P3C-03 sub-slice 04x (owner C4 COM metric-profile definition core)
- Status: delivered and cross-checked against an independent reference.
  Freezes the owner C4 decision (COM_dB / ICN_mV / ERL, relative tolerance
  < 1%) as a named, compare-engine-compatible profile definition.

## Method

Implement c4_metric_profile_v1.rs: C4_METRICS (COM_dB / ICN_mV / ERL with
unit tokens) and C4_RELATIVE_TOLERANCE_V1 = 0.01; c4_metric_specs_v1 consumes
a caller-supplied finite reference map and returns the P3C-03c compare-engine
MetricSpecV1 list. The owner provided the metric set and tolerance but not
per-metric references, so references are caller/oracle-supplied (B4) and
must be finite; a missing or non-finite reference for any of the three
metrics is a hard error (fail closed, no guessing). Cross-check compares the
product path (runner binary) against an independent reference across 6
scenarios, matching ok/error and full spec+d spec details.

## Result

- 6/6 matched_hash_bound, exercising BOTH happy paths (full-references,
  realistic-values, single-ERL) and failure paths (missing-ERL, missing-ICN,
  empty -> missing-COM).
- sipi-compare unit suite 32 tests green (5 new c4 profile tests).
- C4 metric set and tolerance frozen; reference values intentionally not
  hard-coded (oracle-driven), so no PARITY or acceptance is implied.

## Binding

- Verifier verify_p3c_04x_c4_profile.py + 6 tests; crosscheck evidence
  docs/baselines/p3c-04x-c4-profile-crosscheck-evidence.v1.yaml.
- Charter p3c-04x-c4-profile-stage.v1.yaml; source map
  p3c-04x-mit-source-map.v1.yaml.
- PLAN **P3C-04x**; owner decision C4 recorded in
  owner-decision-checklist.v1.md; ledger/gate under P3C-03; coverage gates
  112 -> 113.
