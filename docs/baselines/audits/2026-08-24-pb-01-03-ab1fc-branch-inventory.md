# PB-01..03 additive branch inventory

This is an additive, scoped record for candidate `ab1fc77024383ab6dafed3df96babced420c830d`. It does not rewrite historical PB manifests or promote a row. The record is an environment-local numeric observation, not a cross-machine reproducibility claim: the reports bind cargo/rustc/uv identities but do not bind every host linker/native environment variable.

PB-01's two fresh candidate/upstream archive replays passed the scoped NRZ metallic-line payload comparison. The aggregate is `docs/baselines/pb-01-legacy-leaf-ab1fc-current-aggregate.json`; it binds distinct run IDs/nonces, candidate/upstream archive identities, fixture bytes, toolchain, and runner/helper hashes.

PB-02 candidate execution exited zero, but the pinned Python `sim-native` oracle exited nonzero before producing arrays. That one diagnostic was discarded and is not formal evidence. The row is therefore `not_evaluated`, not parity. PB-03 has Rust reachability tests and the typed AMI materializer, but no independent numeric payload in this slice; its external AMI/IBIS/DLL/TS4 branches remain fail-closed.

The machine-readable branch/status matrix is [pb-01-03-branch-inventory-ab1fc.v1.yaml](../pb-01-03-branch-inventory-ab1fc.v1.yaml). It lists exact Rust reachability cases rather than presenting a category Cartesian product as branch-complete. Historical v1/v2 evidence remains untouched.
