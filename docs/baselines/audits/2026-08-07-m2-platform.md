# M2 Platform Audit Handoff

审计对象：SIPI M2 平台侧实现，范围提交 `0d8bed7`（adapter SPI 脚手架）至 `59e0305`（端到端覆盖补强）。本记录只接受每切片经独立审计（CodeBuddy/Orca run）的提交作为证据；不把 fixture 引擎上的平台闭环等同于真实引擎双跑等价。

| 切片 | 提交 | 独立审计（Orca run） | 结论 |
| --- | --- | --- | --- |
| strict adapter SPI | `0d8bed7` | `run_42637f339985` 系列（首切片） | 0 P1 + 5 P2，已修复（`b4fd28c`） |
| process adapter + agent-spice hspice | `de6a9f0` | `run_42637f339985` | 1 P1 + 5 P2，已修复（`b4fd28c`） |
| pybert strict adapter + artifact 交接 | `b8574b4` | `run_ded503073ee4` | 0 P1 + 2 P2，已修复（`4a0b191`） |
| agent-com strict adapter | `b996fef` | `run_1312c27ef154` | 0 P1 / 0 切片内 P2 |
| runtime selection（M2-07/M2-10） | `0f0f9e4` | `run_ddddf87111b0` | 0 P1 + 2 P2 测试缺口，已补（`f115baf`） |
| execution assembly | `a9925fd` | `run_6e634099a8bf` | 0 P1 + 2 P2 测试缺口，已补（`f61a761`） |
| capabilities 预检（M2-11 前置） | `93605e8` | `run_455a956bc20d` | 0 P1 + 3 P2，已修复（`14eda93`） |
| clean-install smoke（M2-08） | `34d1a9d` | `run_4f0541cbcf94` | 1 P1 + 3 P2，已修复（`5a2ad40`） |
| wheel attestation（M2-09） | `39fa071` | `run_01e73e42b2d9` | 0 P1 + 4 P2，已加固（`65b09c4`） |
| certified catalog（M2-11） | `1acf4ab` | `run_f41678a9ab84` | 2 P1 + 2 P2，已修复（`2bbca8a`） |
| 双跑比较器（M2-05 shell） | `4a4deb6` | `run_cca59060dd96` | 0 P1 + 3 P2，已处理（`4fd45d2`/`0154f25`） |
| wheel venv 执行 | `8265f22` | `run_08fff5c11df6` | 0 P1 + 3 P2，已修复（`c786016`） |
| 端到端纵向集成 | `26c93a9`/`59e0305` | `run_bf6cc4bccf58` | 0 P1 + 3 覆盖缺口，已补（`59e0305`） |

## 验证证据

- 单元门禁：`python -B tools/run_all_tests.py` 25/25 通过（含 M1 rule ledger 回归；2026-08-07 收尾复跑）。
- 干净安装：`python -B tools/clean_install_smoke.py` 5/5 通过（wheel 构建 + 隔离 venv + 打包 schema 完整）。
- `git diff --check` 干净；工作树干净。
- M1 authority 冻结未被破坏：`capabilities-certified.v1` 以 M2-11 conformance 注册，未进入 M1-06 ledger 义务。

## Declared Gap（外部 gate，非平台缺口）

真实 bundle 双跑等价（M2-05 引擎级）要求三仓具备 annotated clean tag（`sipi-baseline/<engine>/<YYYYMMDD>.<n>`）、许可非 `blocked_unknown`、required fixtures 可用。机器检查：

```powershell
python -B tools/check_m2_external_gate.py
```

初始输出 `ready: false`：三仓均无 baseline tag、许可全部 blocked_unknown，agent-com 另有 required fixture partial。外部就绪后该工具返回 0，即可注册真实 engine.lock 并完成双跑等价验收。

## 2026-08-07 更新（三仓 agent 协作）

三个引擎 agent（Orca run `run_bfb108517607`）已完成：

| 引擎 | 结果 |
| --- | --- |
| agent-spice | 8 项 keep 已提交（`84c638b9`），annotated tag `sipi-baseline/agent-spice/20260807.1`（`e7918aee`）指向 main HEAD；跟踪树 clean；无顶层 LICENSE/NOTICE，Cargo.toml 声明 MIT，第三方 mft-nnls 许可文本 2 份 |
| pybert | 自 worktree（与主仓共享 .git）创建 annotated tag `sipi-baseline/pybert/20260807.1`（`f89867f9`）指向 `5bf6d7e`；主仓仍有 19 个本地修改 + 2 个未跟踪文件（agent 元数据/日志），未代判处理；许可证据：根 LICENSE（BSD-3-Clause，David Banas 2014）、PyAMI/LICENSE、native Cargo.toml 声明 MIT、pyproject 无 license 字段 |
| agent-com | annotated tag `sipi-baseline/agent-com/20260807.1`（`f8a8f245`）指向 `034b21b2`；仓库无 LICENSE/NOTICE；MATLAB oracle 仅本地 ignored，未入库 |

门禁语义更新：baseline tag 的“clean”指命名的是 committed 快照（默认分支 HEAD 指向 tag），任何 checkout 的未提交本地文件不使 tag 失效；`check_m2_external_gate.py` 相应不再把工作树脏改动当作 blocker。

剩余 blocker（法律/资产，超出 agent 授权，无法由代码消除）：三仓许可仍全部 `blocked_unknown`（需正式授权证据后更新 license-manifest）；agent-com MATLAB golden oracle 未入库/`partial`。tag 部分已就绪。

## 2026-08-07 授权更新（license owner 决策：全部 MIT）

license owner（`z331225718`）授权：agent-spice 与 agent-com 源码主体采用 MIT（两仓已补 MIT LICENSE 并打 `.2` baseline tag，提交 `80f66a38`/`8c6ffa40`）；pybert 保持其现有 BSD-3-Clause / PyAMI two-clause 文本并授权为 `authorized_public`（未重标为 MIT，避免歪曲现有版权文件；如需正式 relicense 需单独确认权利主体）。`license-manifest.v1.yaml` 已更新并新增 `tools/verify_license_manifest.py` 机器校验（25/25 门禁通过，`verify_license_manifest.py` 返回 valid）。

当前 gate：pybert `ready: true`；agent-spice 仅剩 `agent-spice-third-party`（第三方 solver 组件需逐组件分类）；agent-com 仅剩 MATLAB oracle fixture `partial`。

## 2026-08-07 收尾（license owner 决策：许可项延后至 cleanroom）

license owner（`z331225718`）决定：M2 收尾不等待许可项，`agent-spice-third-party`（第三方 solver 逐组件分类）与 agent-com oracle/数据资产（`external_reference_only` + fixture `partial`）延后至 pybert clean-room 完成后重新打开 M0-09/G0b/G0c。该决策只改变排期与“就绪”判断，不改变 `license-manifest.v1.yaml` 分类：仅上述两项维持 `blocked_unknown`/`external_reference_only`，其余 subject（agent-spice 源码主体、agent-com 源码、pybert 主体）保持 `authorized_public`，机器拒绝语义不变。

M2-05 引擎级真实双跑状态（deferred，附证据）：

- pybert：真实 CLI `pybert sim-native`（input.json + `--output-dir` → `meta.json`/`arrays.npz`）已确认存在且与 adapter 契约一致；但平台 wheel 执行当前仅支持单 wheel `--no-deps`（`sipi.m2.console-script`，见 `packages/sipi-adapters/src/sipi_adapters/venv.py`），pybert-rust 依赖独立扩展包 `pybert-native`，真实双跑需 managed dependency lock（M2-08 后续 slice），故平台内真实双跑 deferred。
- agent-spice / agent-com：真实产物与 fixture/license 前置未满足，随许可项一并延后。

收尾复跑证据：单元门禁 `tools/run_all_tests.py` 25/25 通过；`tools/verify_license_manifest.py` `valid: true`；`tools/clean_install_smoke.py` 5/5 通过；`docs/baselines/capabilities.certified.v1.json` 保持 absent（无 certified 组合，不 advertise）；`tools/check_m2_external_gate.py` 输出 `ready: false`（agent-spice license `blocked_unknown`、agent-com fixture `partial`），与用户延后决策一致。

## Conclusion

**checkpoint accepted（M2 平台侧完成，2026-08-07 收尾）**。平台控制链（selection → assembly → 三个 strict adapter → artifact 交接 → 比较 → 晋级/门禁）已实现并经逐片独立审计闭环；M2 收尾按 owner 决策将许可项与引擎级真实双跑延后（触发：cleanroom 完成后重开许可项；managed dependency lock 后重开 pybert 真实双跑），不据此宣称 M2 完整完成或能力可 advertise。下一步：进入 M3。
