# TP0V R2024b v3 preparation

This is additive and separate from v2. It has no candidate and no formal
record. MATLAB is accepted only through a caller-supplied executable whose
runtime reports exactly `version('-release') == '2024b'`; PATH and Engine
selection are forbidden. Semantic timing is uninstrumented; trace capture is
diagnostic only. A future aggregate requires the two MATLAB runtime identities
to be exact and rejects any Rust case that is not strictly faster.

## Runtime smoke diagnostic

This is not numerical, performance, or acceptance evidence. With an explicit
`C:\Program Files\MATLAB\R2024b\bin\matlab.exe`, a fresh `MATLAB_PREFDIR`,
`MW_DISABLE_CONNECTOR=1`, and `-batch`, the probe exited 0 and returned release
`R2024b`/raw `2024b`, arch `win64`, executable bytes `458288`, executable SHA-256
`4b0fcf8112211ad1ae6ef5e51df0801d7afbe89c4daaac45473a46e1de16e633`, and
full-version-output SHA-256 `42234314c46548012617d2926969237373aa04d6fe5e94d8ecd9f30e463df109`.
