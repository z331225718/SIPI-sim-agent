# M4 Shared Data DTO Audit Handoff

审计对象：SIPI M4（共享语义 DTO）实现切片。每切片单独提交、单独独立审计（CodeBuddy/Orca run）。

| 切片 | 提交 | 独立审计（Orca run） | 结论 |
| --- | --- | --- | --- |
| M4-01 `sipi.axis.v1` schema + 解析模型 + 语义校验 | `5d2b09f` | `run_ddddd9c5930d` | 0 P1 / 0 P2（首轮通过；首次 worker_done 被拒后重发确认闭环） |
| M4-02 `sipi.port-map.v1` schema + 解析模型 + 语义校验 | `378631d` | `run_6c84a40778a4` | 0 P1 / 0 P2（首轮通过） |
| M4-03 `sipi.network-tensor.v1` schema + 解析模型 + 语义校验 | `519c5ea` | `run_7154341511de` | 0 P1 / 0 P2（首轮通过） |
| M4-04 `sipi.waveform.v1`/`sipi.spectrum.v1` schema + 模型 + 语义校验 | `2e9b53b` | `run_efd7f48a59f3` | 0 P1 / 0 P2（首轮通过） |
| M4-05a `sipi.channel-resolution-policy.v1`/`report.v1` 契约 | `d171a0f` | `run_17f7ecf2e973` | 0 P1 / 0 P2（首轮通过） |
| M4-05b 唯一 channel resolver（契约钉定 + fail-closed 数值变换） | `8769184` | `run_9b9dd2da4618` | 0 P1 / 0 P2（首轮通过） |
| M4-06a M4 DTO conformance 框架 + 记录（Python covered / Rust deferred） | `7c5c6d3` | `run_3fd1c17d8a32` | 0 P1 + 2 P2（strictness 声明、direction 引用），已修复（`eddd8ab`/`0c32b95`，复审 0 P2） |
| M4-07/08a profile readers + cross-profile fixture 矩阵 | `a46a646` | `run_173ce66909f2` | 0 P1 / 0 P2（首轮通过） |
| M4-09 显式 transform policies（实现名 + 契约 + 语义校验） | `14f92d6` + P1 修复 `d9f22ed` | `run_429daf4b418c` | 首轮 1 P1（ledger sha 失配），已修复并经复审 0 P1 / 0 P2 |
| M4-10 cache identity（语义字段 + producer 进 key） | `06bd5f6` | `run_d62f0d526276` | 0 P1 / 0 P2（首轮通过） |
| M4 收尾 checkpoint（任务表/退出条件/必测矩阵核验） | `8283b09` | `run_8d03802f84f9` | 0 P1 / 0 P2（首轮通过） |

## M4-01（AxisV1）

- `schemas/axis.v1.schema.json`：kind（time/frequency/phase/index）、SI 单位、dtype、长度、单调性、均匀性、sample_location（sample/bin_center/bin_edge）、频谱元数据（sidedness/has_dc/has_nyquist）；oneOf 强制 start/step、values、values_artifact 互斥。
- `validate_axis_intrinsic`：frequency 必带 spectrum、非 frequency 禁 spectrum、step!=0、uniform 声明一致性、values 长度/单调性校验、artifact-ref 校验；`AxisV1` 冻结模型 + `parse_axis`。
- authority 登记 `local_json_schema` + rule-ledger sha 同步（`54816fb6...`）。
- 未越界 NetworkTensor/PortMap 语义。
- 证据：`tests/contract/test_axis.py` 6 项；全量 39/39。

## M4-02（PortMapV1）

- `schemas/port-map.v1.schema.json`：basis（single_ended/mixed_mode）、index_base（0/1）、稳定 id/label/external_index、polarity、差分对 `pair_with`、common_mode 与 reference 节点。
- `validate_port_map_intrinsic`：id/index 唯一、reference 已声明且非自引用、`pair_with` 仅 mixed_mode 且仅 signal、对称性、自配对/未知配对拒绝；`PortMapV1` 冻结模型 + `parse_port_map`。
- authority 登记 `local_json_schema` + rule-ledger sha 同步（`df4e40e6...`）；未混入 NetworkTensor/数据引用。
- 证据：`tests/contract/test_port_map.py` 4 项；全量 40/40。

## M4-03（NetworkTensorV1）

- `schemas/network-tensor.v1.schema.json`：parameter_kind（S/Y/Z/H/EXT + EXT 名称）、`[frequency, output_port, input_port]` shape、complex_encoding/dtype/byte_order/layout、z0（scalar/per_frequency/per_port）、wave_definition（power/pseudo/traveling）、内嵌 Axis+PortMap、数据 `SipiArtifactRefV1`、reader/port_reorder。
- `validate_network_tensor_intrinsic`：axis 必为 frequency、shape.frequency==axis 长度、S/Y/Z/H 方形且等于非 reference 端口数、EXT 需 parameter_kind_name、complex dtype→interleaved、z0 与 port_reorder 规则；`NetworkTensorV1` 冻结模型 + `parse_network_tensor`。
- authority 登记 `local_json_schema` + rule-ledger sha 同步（`aa6ed1d5...`）；未混入 resolver/policy 语义。
- 证据：`tests/contract/test_network_tensor.py` 6 项；全量 41/41。

## M4-04（WaveformV1 / SpectrumV1）

- `schemas/waveform.v1.schema.json`：内嵌 Axis+PortMap、signal_kind（voltage/current + measurement/sign convention）、单位、data artifact、shape（samples+channels）、显式 channel entry（port_id/polarity/reference，不靠 shape 推断）、sample_scaling/effective_interval/warmup_samples/trimming/initial_state。
- `schemas/spectrum.v1.schema.json`：同上 + fft（normalization/window/scaling）；axis 必为 frequency。
- 语义校验：axis kind、shape 与 axis/channels 长度、channel port 已声明且极性一致、reference 已声明且非自引用、voltage/current 条件字段、interval/warmup+trimming 边界、fft 未知字段拒绝。
- authority 登记 `local_json_schema` + rule-ledger sha 同步（`5497a0a3...`）；未混入 resolver/policy 语义。
- 证据：`tests/contract/test_signal.py` 8 项；全量 42/42。

## M4-05a（channel resolution 契约）

- `schemas/channel-resolution-policy.v1.schema.json`：reader_semantics、port_selection/termination、interpolation（kind/fft_size/sample_interval_s）、dc、causality、ifft（window/zero_pad_factor/trim）、normalization、output（signal_intent/current_to_voltage_sign/reference_port）。
- `schemas/channel-resolution-report.v1.schema.json`：additionalProperties 容忍未知字段，固定 producer/source_network_hash/policy_hash/output_hash（64-hex）+ transforms/warnings。
- 语义校验：port_selection 唯一、voltage 输出必带 sign、reference_port 必须已选、report transform kind 唯一/hash 格式；模型冻结 + parse。
- authority 登记 `local_json_schema` + rule-ledger sha 同步（`99e922d3...`）；未实现 resolver 主体（留 M4-05b）。
- 证据：`tests/contract/test_channel_resolution.py` 4 项；全量 43/43。

## M4-05b（channel resolver）

- `packages/sipi-adapters/src/sipi_adapters/channel_resolver.py`：`resolve_channel` 为唯一 production producer——NetworkTensor（S/Z）+ policy → PyBERT `ChannelResponseV1`（camelCase 字段经 `CHANNEL_RESPONSE_CONTRACT` 钉定到 `native/pybert-core/src/input.rs`）+ report（source/policy/output hash、transforms/warnings、sign 应用）。
- 数值变换（interpolation/dc/causality/ifft window/normalization 非 none）fail-closed 拒绝并记录 applied 如实；normalization=amplitude 仅警告不谎称已应用。
- 全仓仅此一处实现（无第二 producer）；导出与测试覆盖契约钉定、identity 解析+哈希、失败关闭、缺 impulse 拒绝。
- 证据：`tests/adapters/test_channel_resolver.py` 4 项；全量 44/44。

## M4-06a（DTO conformance）

- `tools/verify_m4_conformance.py`：五 DTO（axis/port-map/network-tensor/waveform/spectrum）固定用例矩阵——producer/consumer good、producer/consumer unknown reject（双向 strict）、consumer version reject；输出 python=covered / rust=deferred 记录。
- `docs/baselines/conformance/m4-dto-conformance.json`：提交记录，与工具实时计算一致（测试断言）；notes 精确声明权威 direction（axis/port-map=bidirectional、network-tensor=reader_to_platform、waveform/spectrum=engine_to_platform）且 6.7 未知字段容忍仅适用于 result 信封。
- Rust 侧 deferred（M5C workspace 建立后补）；未越界。
- 证据：`tools/test_m4_conformance.py` 2 项（含记录一致性）；全量 45/45。

## M4-07/08a（profile readers）

- `packages/sipi-adapters/src/sipi_adapters/readers.py`：`read_network(profile, document)`——com-r480/com-standard/pybert-standard/agent-spice-standard 四 profile 将预提取构件组装为 `NetworkTensorV1` 并经 parse 校验，如实记录 `reader.source_reader/reader_semantics`。
- `cross_profile_fixture_matrix()`（M4-08）：同一语义 2-port 文档按 profile 复制，data sha/shape 跨 profile 一致、semantics 各异；未断言数值相等（符合 SPEC 7.3“不宣称数值相同”）。
- 未知 profile / 缺失字段 fail-closed；原生格式（Touchstone/workbook/RFM 二进制）解析明确 defer 给 domain owner，未谎称完成。
- 证据：`tests/adapters/test_readers.py` 3 项；全量 46/46。

## M4-09（显式 transform policies）

- `schemas/transform-policy.v1.schema.json`：`sipi.transform-policy.v1`——`reader_semantics` + 有序 `transforms` 链；每项带命名空间化 `name`（实现名，`^[A-Za-z0-9_-]+(\.[A-Za-z0-9_-]+)+$`）、`kind`（port/interpolation/dc/causality/passivity）、`parameters`（公共语义字段类型钉定：operation/ports/method/fft_size/sample_interval_s/extrapolate/enforce/tolerance；`additionalProperties: true` 允许实现私有参数）。
- `validate_transform_policy_intrinsic`：数值变换 kind（interpolation/dc/causality/passivity）每链至多一次（顺序无歧义）；port 变换可多次成序列；port 必带显式 `operation`，非 identity 必带受影响 `ports`；interpolation/dc/causality 必带 `method`。顶层与每项 strict（additionalProperties:false），parameters 私有字段由 domain owner 负责（格式解析仍归 domain owner，未谎称解析 Touchstone/workbook/RFM 二进制）。
- authority 登记 `local_json_schema`（`sipi.transform-policy.v1`，direction=resolver_input_strict）+ rule-ledger `authority_sha256` 同步（git blob LF 值 `a3f70547...`）。
- 未越界：不修改已冻结的 M4-05a `channel-resolution-policy.v1`；未接入 resolver 消费（留 M4-10 cache identity）。
- 证据：`tests/contract/test_transform_policy.py` 9 项；全量 47/47。

## M4-09 审计（run_429daf4b418c）

- CodeBuddy 首轮：1 P1——rule-ledger `authority_sha256` 与 git blob（LF 规范化）不符（提交值基于 CRLF 工作区字节，LF/CI 检出会失败），其余 0 P2。
- 修复 `d9f22ed`：`tools/verify_m1_rule_ledger.py` 计算前对 authority 字节做 LF 规范化（CRLF→LF），使 CRLF/LF 检出与 git blob 一致；`rule-ledger.json` 重算为 blob sha256 `a3f70547...`。本地验证：`verify_m1_rule_ledger` 不再报 `ledger:authority_sha256`（仅剩既有的 agent-com external_snapshot 漂移 4 项，与本次无关）；`test_m1_rule_ledger.py` 10/10、全量 47/47。
- CodeBuddy 复审终端会话异常（只规划不执行），改用 OMP（`term_e75ea1bb`）独立复审 `d9f22ed`：0 P1 / 0 P2，通过（`msg_577e38940b86`）。

## M4-10（cache identity）

- `packages/sipi-runtime/src/sipi_runtime/dag.py`：`cache_identity` 新增显式 `producers`（role + instance_id + bundle_hash，按 resolved selection 顺序进入 content），`DagNode` 增加 `producers` 字段，`plan_dag` 从 `trace.resolved` 构造并传入；`driver.py` 执行时透传 `node.producers`。
- key 语义字段全集：operation、payload_schema、payload_sha256、backend_selection、selection_hash、bundle_hashes、producers、inputs（from/role/expected_schema/content_sha256）、resource_policy、randomness；有意排除执行 ID、lineage、project_hash、selection_source（依赖语义由 payload/inputs 内容 hash 与 selection 覆盖）。
- 相同 bundle hash 不同 producer instance 不误命中；compare 的 reference/candidate 角色顺序敏感（语义序）；未传 producers 时行为兼容不变。
- 证据：`tests/runtime/test_dag.py` 11 项（新增 producer 参与/顺序/兼容 3 项）、`tests/runtime/test_driver.py` 10 项；全量 47/47。

## M4-10 审计（run_d62f0d526276）

- 新 CodeBuddy/Codex 终端启动卡死（node_repl MCP boot 无进展），已停；改用 OMP 独立审计 `06bd5f6`：0 P1 / 0 P2，通过（`msg_a06784b0bb11`）。

## M4 收尾 checkpoint（2026-08-07）

### 任务表状态

M4-01 ~ M4-10 全部实现并经独立审计闭环（上表）。唯一未落地代码切片为 M4-06b（Python/Rust conformance 的 Rust 侧），前置为 M5C Rust workspace，显式 deferred。

### 退出条件核验（SPEC 8.4）

| 退出条件 | 状态 | 证据/依赖 |
| --- | --- | --- |
| DTO schema 有清晰物理含义 + 双语言 consumer conformance | 部分满足 | Python 侧 covered（M4-06a 记录 `docs/baselines/conformance/m4-dto-conformance.json`）；Rust 侧 deferred 至 M5C workspace（M4-06b），未谎称双语言 |
| 三个 reader/transform profile 可共存且结果可追踪到 producer/policy | 满足 | M4-07/08a（com-r480/com-standard/pybert-standard/agent-spice-standard + cross-profile fixture 矩阵）、M4-09（实现名 transform-policy + report 哈希） |
| RFM -> Channel 切片改用 DTO 后不退化 | 平台侧未接线（deferred） | resolver 契约/编排就绪（M4-05b、M4-09）；RFM response -> NetworkTensorV1 -> resolver -> ChannelResponseV1 的生产接线依赖 S2P 数值边界（pybert G4 决策：S2P 留 Python 外部边界）与 M5B-03 拓扑决策（禁止第二套 resolver），留 M5 集成 |
| 没有把 COM 行为默认值变成平台默认，也没有把 PyBERT conditioning 隐式施加给 COM | 满足 | M4-09 命名空间化实现名 + 每 profile 独立 reader 语义；平台只做契约与语义校验，不设数值默认 |

### 必测矩阵覆盖（SPEC 8.3）

- S/Y/Z/EXT、方形 shape、z0 标量/逐频点/逐端口（`tests/contract/test_network_tensor.py` 6 项）。
- 单端、差分对/混合模式、reference、极性、端口置换（`tests/contract/test_port_map.py` 4 项 + network-tensor port_reorder）。
- 非均匀轴、缺 DC、存在/缺失 Nyquist、单双边谱、sample/bin 语义（`tests/contract/test_axis.py` 6 项）。
- C/F layout、大小端、complex128 与 dtype 拒绝（network-tensor/artifact 校验）。
- 电压/电流、测量/符号约定、FFT scaling/window、warm-up/trimming、channel entry（`tests/contract/test_signal.py` 8 项）。
- 解析策略/变换链语义与 fail-closed 边界（`tests/contract/test_channel_resolution.py` 4 项、`test_transform_policy.py` 9 项）。
- 数据布局/数值相等的物理断言归领域测试（跨仓 golden/parity），平台 DTO 层不谎称数值等价（M4-08）。

### 结论

M4 Shared Data MVP 的平台侧契约与编排完成（schema、语义校验、唯一 resolver 契约、reader/transform profile、cache identity）；引擎级 DTO 消费（Rust conformance）与 RFM->Channel 数值接线按依赖 deferred，不据此宣称 M4 完整完成或可 advertise。下一步：M5（Circuit/Link 内核收敛）或按 PLAN 13.4 依赖顺序推进。
