# Owner Recommended-Bundle Authorization

The owner replied `全部按推荐` to the current 22-item explanation and response
form. This record freezes the selected policy options without inventing the
external facts that the form deliberately left open.

The authorization selects D1 through D6, E3, E4, E7, and the proposed Windows
release target. It permits read-only external inspection and clean or isolated
execution with hash-only repository evidence. It does not create vendor runtime
rights, redistribution rights, an IBIS model/corner selection, a unique PyBERT
equalizer profile, or an Agent-COM checkpoint and TD-ILN reference.

Workers must therefore proceed in this order: source and license audit,
external custody, narrow prerequisite implementation, and only then a
current-candidate comparison. If an exact value or profile is not mechanically
unique, the result is a blocker observation rather than a guessed product
policy. P7 remains blocked until one final committed candidate is rebuilt and
the full release chain is replayed.
