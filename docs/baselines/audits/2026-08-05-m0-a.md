# Independent Audit: M0-A

审计对象：M0-A evidence 提交 `083e69107e70b1f7ed49713757bb4c1fd3c64c25`。审计结论不将未运行的测试、未知许可或未签核的 dirty disposition 误报为通过。

| Check | Result | Evidence |
| --- | --- | --- |
| Target repository initialized before governance evidence | pass | root commit `d32df26` only contains README/SPEC/PLAN |
| No source code imported from sibling repositories | pass | target index contains only governance/design files in this round |
| Dirty source state protected non-destructively | pass | external backup manifest and all three scratch-clone restore digests match |
| Source capture stable during backup | pass | each manifest `head_before == head_after` and status digest matches |
| Every dirty source path recorded for a migration decision | pass | 50 Agent-Spice + 21 PyBERT entries; all deliberately remain `pending` |
| Every dirty source path assigned a migration decision | open | all entries explicitly remain `pending`, owner `unassigned` |
| Toolchains, domain baselines, fixtures, licenses and capability inventory completed | not_run | out of M0-A scope |

审计备注：第一次备份尝试因 PowerShell 文本管道改变 patch 末尾换行，恢复演练失败；该工件没有被引用。第二次以 raw Git stdout 捕获 patch，并在全新 clone 中成功恢复。独立复核还发现 M0-01 inventory 漏掉构建输入和 remote cache evidence；该问题由下一提交补齐，但 M0-01/M0-02 仍不因此关闭。后续 rehearsal 还应将 patch hash 和逐文件 untracked hash 直接写入 rehearsal record。

Delta 审计（提交 `2400305`）：独立审计确认 Agent-Spice、PyBERT 的遗漏项和三仓 remote cache evidence 已补齐；COM 仍遗漏 `setup.py` 和 `MANIFEST.in`，这两个 hash 已在随后的微小修正中加入。最终结论为 `conditional pass / checkpoint accepted`，不是 M0-01 或 M0-02 完成。
