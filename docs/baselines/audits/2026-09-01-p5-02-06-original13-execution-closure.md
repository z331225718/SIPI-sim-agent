# P5-02/P5-06 Original-13 Execution Closure

The owner-selected COM acceptance surface is the pinned Agent-COM
original-13 workbook corpus, run against MATLAB R2024b and the Rust public
root route.  This record joins four independently verified records rather
than inventing a new output contract.

For each of the 13 workbooks, the MATLAB worker records the complete workbook
cell-matrix slot digest and materializes the exact runtime-consumed parameter
and option projection.  The Rust materializer has the same canonical
projection.  The two fresh root replays then compare 303 final scalar slots;
the separate TDILN record compares six named intermediate vectors.  All
per-workbook and aggregate performance gates require Rust not to be slower
than MATLAB; the weakest bound remains 9.237x in Rust's favour.

Warning coverage is intentionally precise.  The source `MaxFreqTooLow`
callsite at line 9715 has exact occurrence mapping.  MATLAB's line-6337
anti-causal message is triggered on a 2.27e-17 to 4.55e-17 residual while the
corresponding Rust trace is exact zero.  It is recorded as a platform-roundoff
diagnostic, not fabricated into a source-equivalent warning and not treated as
a functional execution mismatch.

This closes the selected original-13 parameter/default/consumption and MATLAB
execution surface only.  It does not claim an unbounded warning catalog, a
serializable full MATLAB results graph, generalized COM conformance, IEEE
certification, or release readiness.
