{
  "schema": "sipi.p4b-02b180.mit-source-map.v1",
  "status": "parameter_list_prevalence_ratio_source_mapped",
  "source": {
    "repo": "product-owned",
    "license": "clean-room",
    "path": "sipi-ami-text parameter_list_prevalence_ratio_v1.rs"
  },
  "observation": {
    "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml",
    "note": "prevalence ratio (max count / total) of the distinct trimmed items of a validated AMI parameter list value"
  },
  "mapping": [
    {
      "concept": "prevalence ratio",
      "rust": "parameter_list_prevalence_ratio_v1",
      "notes": "max occurrence count over distinct items by raw byte equality divided by list length; f64 in (0, 1]"
    },
    {
      "concept": "prevalence-ratio errors",
      "rust": "ParameterListPrevalenceRatioErrorV1",
      "notes": "NotAList for non-List types, defensive MalformedList otherwise"
    }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
