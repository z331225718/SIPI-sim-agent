{
  "schema": "sipi.p4b-02b107.mit-source-map.v1",
  "status": "parameter_list_distinct_count_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_distinct_count_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "distinct list item counting over validated AMI parameter values" },
  "mapping": [
    { "concept": "distinct item count", "rust": "count_distinct_parameter_list_items_v1", "notes": "counts distinct trimmed items by raw byte equality, first-occurrence membership" },
    { "concept": "count errors", "rust": "ParameterListDistinctCountErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
