{
  "schema": "sipi.p4b-02b141.mit-source-map.v1",
  "status": "parameter_list_multi_keep_only_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_multi_keep_only_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "batch retain by query set over validated AMI parameter list values" },
  "mapping": [
    { "concept": "batch retain", "rust": "keep_only_parameter_list_items_multi_v1", "notes": "keeps trimmed items equal to any raw query item, re-joined with ', '" },
    { "concept": "retain errors", "rust": "ParameterListMultiKeepOnlyErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
