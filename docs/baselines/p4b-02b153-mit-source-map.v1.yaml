{
  "schema": "sipi.p4b-02b153.mit-source-map.v1",
  "status": "parameter_list_contains_multiset_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_contains_multiset_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "multiset sub-multiset check over validated AMI parameter list values" },
  "mapping": [
    { "concept": "multiset containment", "rust": "list_contains_multiset_v1", "notes": "reports whether every query item count is at most the host count by byte equality" },
    { "concept": "containment errors", "rust": "ParameterListContainsMultisetErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
