{
  "schema": "sipi.p4b-02b135.mit-source-map.v1",
  "status": "parameter_list_distinct_pair_count_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_distinct_pair_count_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "distinct unordered pair counting over validated AMI parameter list values" },
  "mapping": [
    { "concept": "distinct pair count", "rust": "parameter_list_distinct_pair_count_v1", "notes": "counts index pairs (i < j) whose trimmed items are unequal by byte equality" },
    { "concept": "pair errors", "rust": "ParameterListDistinctPairCountErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
