{
  "schema": "sipi.p4b-02b127.mit-source-map.v1",
  "status": "parameter_list_contains_sublist_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_contains_sublist_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "contiguous sublist membership over validated AMI parameter list values" },
  "mapping": [
    { "concept": "contiguous sublist", "rust": "parameter_list_contains_sublist_v1", "notes": "reports whether a window equals the query sublist element-wise by byte equality" },
    { "concept": "sequence errors", "rust": "ParameterListContainsSublistErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
