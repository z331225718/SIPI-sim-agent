schema: sipi.p4b-02b188.mit-source-map.v1
status: parameter_list_occurrence_indices_map_source_mapped
source:
  repo: product-owned
  license: clean-room
  path: sipi-ami-text parameter_list_occurrence_indices_map_v1.rs
observation:
  ref: docs/baselines/p4b-raw-text-binding-foundation.v1.yaml
  note: BTreeMap mapping each distinct trimmed item to every 0-based occurrence index in ascending position order
mapping:
  - concept: occurrence-indices map
    rust: parameter_list_occurrence_indices_map_v1
    notes: returns BTreeMap<String, Vec<usize>> mapping each distinct trimmed item to every 0-based occurrence index in ascending position order
  - concept: occurrence-indices map errors
    rust: ParameterListOccurrenceIndicesMapErrorV1
    notes: NotAList for non-List types, defensive MalformedList otherwise
not_ported:
  - full reserved-name AMI parameter catalog
  - model-populated Out/Info runtime execution
  - release certification
