schema: sipi.p4b-02b190.mit-source-map.v1
status: parameter_list_frequency_map_source_mapped
source:
  repo: product-owned
  license: clean-room
  path: sipi-ami-text parameter_list_frequency_map_v1.rs
observation:
  ref: docs/baselines/p4b-raw-text-binding-foundation.v1.yaml
  note: BTreeMap mapping each distinct trimmed item to its total occurrence count
mapping:
  - concept: frequency map
    rust: parameter_list_frequency_map_v1
    notes: returns BTreeMap<String, usize> mapping each distinct trimmed item to its total occurrence count
  - concept: frequency map errors
    rust: ParameterListFrequencyMapErrorV1
    notes: NotAList for non-List types, defensive MalformedList otherwise
not_ported:
  - full reserved-name AMI parameter catalog
  - model-populated Out/Info runtime execution
  - release certification
