{
  "schema": "sipi.p4b-02b176.mit-source-map.v1",
  "status": "parameter_list_entropy_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_entropy_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "Shannon entropy in bits over the empirical frequency distribution of a validated AMI parameter list value" },
  "mapping": [
    { "concept": "shannon entropy", "rust": "parameter_list_entropy_v1", "notes": "-sum(p_i * log2(p_i)) over distinct trimmed items by raw byte equality, f64 in [0, log2(len)]" },
    { "concept": "entropy errors", "rust": "ParameterListEntropyErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}