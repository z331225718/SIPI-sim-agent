{
  "schema": "sipi.p4b-02b157.mit-source-map.v1",
  "status": "parameter_list_contains_sequence_source_mapped",
  "source": { "repo": "product-owned", "license": "clean-room", "path": "sipi-ami-text parameter_list_contains_sequence_v1.rs" },
  "observation": { "ref": "docs/baselines/p4b-raw-text-binding-foundation.v1.yaml", "note": "value-level contiguous sequence containment over validated AMI parameter list values" },
  "mapping": [
    { "concept": "sequence containment", "rust": "list_contains_sequence_v1", "notes": "reports whether the host has a window equal to the query items element-wise by byte equality" },
    { "concept": "sequence errors", "rust": "ParameterListContainsSequenceErrorV1", "notes": "NotAList for non-List types, defensive MalformedList otherwise" }
  ],
  "not_ported": [
    "full reserved-name AMI parameter catalog",
    "model-populated Out/Info runtime execution",
    "release certification"
  ]
}
