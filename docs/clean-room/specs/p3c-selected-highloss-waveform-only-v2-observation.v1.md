# P3C Selected High-Loss Waveform-Only v2 Observation

This external-only runner repeats the exact sealed selected S4P intake,
interpolation, bounded causality, fixed truncation, and owner-confirmed
finite-edge PRBS9 source projection. It binds the resulting strict-grid
candidate to the existing immutable ADS RX payload through the fixed v3
waveform-only CLI. Two fresh runs must agree, while either numerical result is
recorded without changing the one-percent policy or any receiver/release gate.
