# P3C Selected S4P Raw-Periodic Observation Evidence v1

## Purpose

This evidence retains only the hash-only result of two fresh external custody
runs through the committed sealed-admission, selected IEEE BSD interpolation,
and raw-periodic inverse-transform leaves.

## Required Facts

The report must bind the exact selected S4P identity, a clean archive commit,
the listed product-source inventory, two distinct manifest identities, and
identical admitted outcomes. The outcome includes the uniform-spectrum and raw
response domain-separated digests, sample/grid facts, residues, and exact
residual-bound bit patterns. It never retains a path, S4P byte, spectrum value,
or response sample.

## Boundary

An observed result proves only repeatable invocation under the ArtifactRoot
non-hostile-writer assumption. It does not admit causality, delay, passivity,
truncation, convolution, a candidate waveform, external-reference binding, a
receiver, P5, AMI, or release capability. Any later product-source change makes
this historical evidence source-drift rather than rewriting it.
