# P3C External ADS Selected High-Loss Waveform-Only Evidence v3

This hash-only evidence records two fresh executions of the v3 selected
waveform-only runner. Both must bind the immutable selected S4P and ADS RX
payload identities, use distinct sealed manifests, and agree on the candidate
and metric facts. A finite NRMSE above one percent is retained as observed
non-acceptance, not discarded as a harness failure.

The verifier binds the clean-archive source inventory and report digest. It
rejects altered counts, digests, acceptance booleans, gate promotions, leaked
paths/arrays, or source drift. The evidence is limited to the selected v3
waveform result; it neither reclassifies v2 eye/TIE rejection nor accepts a
receiver, physical channel semantics, P4B/P5, or release.
