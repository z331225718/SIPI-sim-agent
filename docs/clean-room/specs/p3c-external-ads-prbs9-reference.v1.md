# P3C External ADS PRBS9 Reference v1

This evidence binds two fresh ADS runs made from the clean archive of the v2
contract amendment. Each run uses only the explicitly sequenced PRBS9 source,
the authorized four-port S-parameter channel, and ideal 50-ohm source/load
topology. The finite source edge is exactly 100 as on rise and fall.

The evidence can set only the external ADS oracle reference observation. It
requires matching canonical waveform and third-period hashes across both runs,
and it binds the previous v1 rejected attempt rather than rewriting it. The
same waveform hashes may be recognized as historical identity only after the
v2 fresh runs reproduce them.

No candidate comparator has run. Receiver acceptance, AMI/P4B execution,
statistical eye contour, product runtime, release compare, and release
promotion remain blocked.
