# P3C Selected S4P Candidate Observation v1

This external-only runner executes the complete fixed selected route twice in
fresh custody: sealed v2 S4P admission, fixed IEEE BSD interpolation, bounded
inline causality, fixed truncation, and the fixed PRBS9 right-continuous direct
linear-convolution bridge. Each run starts from the source independently and
must use a distinct temporary `ArtifactRoot` and manifest.

The report retains only exact source identity, stage-qualified status, counts,
fixed diagnostics, and domain-separated digests for the truncated kernel and
the full, strict-grid-prefix, and third-period candidate responses. It may not
retain a path, asset byte, spectrum, response sample, temporary-root name, or
waveform payload. Source identity must remain equal before staging and after
staging; both roots must clean up successfully.

Two matching admitted runs establish fresh-custody repeatability for this
current product chain only. They do not establish independent toolchain
reproduction, physical causal-FIR admission, delay/passivity treatment, 100 as
sub-strobe or ADS parity, external-reference binding, metric acceptance,
receiver acceptance, P4B/P5, public runtime, or release capability.
