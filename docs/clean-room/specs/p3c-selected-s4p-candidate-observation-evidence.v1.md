# P3C Selected S4P Candidate Observation Evidence v1

This evidence binds two matching, fresh-custody external invocations of the
fixed selected S4P candidate route to a clean archive and a complete compiled
source inventory. The repository retains only hashes, counts, bounded
diagnostics, and release-build identity; it retains no external asset, report,
path, or waveform payload.

It records the predecessor truncation observation as historical source drift
when its old verifier rejects against the current product archive. That history
is not rewritten or used as current evidence. This evidence observes the
candidate waveform only; it cannot admit a physical causal FIR, reference
binding, metric/receiver acceptance, or release capability.
