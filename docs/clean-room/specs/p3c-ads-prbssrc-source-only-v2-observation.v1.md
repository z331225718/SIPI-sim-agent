# P3C ADS Source-Only v2 Observation

This external-only observer replays the approved matched-load ADS PRBSsrc
topology and compares its canonical half-open loaded differential samples to
one fixed expected projection: half the product v2 finite-edge source level.
It retains the existing ADS topology and explicit PRBS9 sequence. It makes no
product policy change, channel claim, alignment, gain fit, or waveform
acceptance decision.
