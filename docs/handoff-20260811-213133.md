# Handoff: SIPI clean-room PLAN continuation

## Current state

- The active objective was to execute `PLAN.md` in order, consult the configured `advisor` for difficult architectural decisions, and use one Orca-hosted reviewer at a time. The work is now correctly marked **blocked**, not complete: remaining domain work needs external assets, authoritative reference outputs, or owner-defined metric semantics that cannot be safely invented.
- Latest committed maintenance: `c8e2484 test: reconcile current evidence gates`.
  - Rebound three stale contract-schema inventory hashes and the release-license manifest's product-boundary hash.
  - Updated stale P4 static-IBIS acceptance assertions.
  - Preserved historical S2P evidence drift while normalizing the imported core evidence exception into the CLI evidence module's stable error type.
  - Split RFM historical readiness/decision tests from the current delegated-policy state and added a guard that the live profile remains `oracle_only`, required, and explicitly not lock/parity/acceptance.
- The immediately preceding evidence commits are `a366d6b` (current fixed-TRAN evidence v2) and `49979df` (matched-S2P CLI evidence v3 at its bound candidate). Historical evidence is intentionally retained and fails current-source attestation after relevant product changes. P3A-12 v3 remains historical because the `c8e2484` contract-schema inventory change drifted its bound `sipi-contracts` tree; P3A-14 now supplies a v4 current-candidate record for the same exact external S2P object and restores only its selected matched-kernel observed evidence.
- Verification completed:
  - `python -m unittest discover -s tools -p "test_*.py"`: 302 tests passed when run in four bounded groups.
  - `cargo test --workspace --all-targets --locked` and `cargo clippy --workspace --all-targets --locked -- -D warnings` passed using `C:\Users\z3312\.cargo\bin\cargo.exe`.
- Product boundary, clean-room register, Rust source map, release-license preflight, and current S2P CLI evidence v4 verifiers passed. S2P CLI v1/v2/v3 correctly report source drift and are retained only as historical evidence.
  - One Orca OpenCode reviewer audited the gate-consistency change and reported `0 P1 / 0 P2`; its only nonblocking suggestions were addressed (exact schema inventory assertion and narrower exception handling).
- Do not touch the unrelated user change `uv.lock`; it remains the sole dirty worktree file.

## Next steps

1. Begin by reading `PLAN.md`, `docs/baselines/release-capability-publication.v1.yaml`, and the current Git status. Keep the present blocked state honest rather than creating a generic solver/executor/host shell.
2. Resume P3C only after a metric charter supplies the eye/jitter/bathtub definitions, observables, tolerances, and an accepted receiver stage. Do not invent PRBS, clock, bins, or BER estimator defaults.
3. Resume P4B only after an exact authorized AMI DLL/IBIS/AMI-text asset set and non-system dependency closure are admitted. The product mock ABI/worker coverage is not vendor interoperability.
4. Resume P5 only after a safe, authoritative R480 reference-generation contract provides normalized input, defaults, runtime/provenance, outputs, and comparison tolerance. MATLAB source, data, and workbooks remain external oracle material.
5. Add generic P6 execution only when a second real product topology has an admitted contract; the existing fixed TRAN-to-causal-FIR route is deliberately the only project topology.
6. Keep P7 release blocked until license/NOTICE decisions, fresh-machine evidence, and required-domain/profile gates exist. Do not create an RC tag or remove legacy evidence.

## Relevant artifacts

- `PLAN.md` — authoritative roadmap, completed narrow slices, and explicit blockers.
- `docs/baselines/release-capability-publication.v1.yaml` — current command/product/evidence status and global release blockers.
- `docs/baselines/tran-rc-pulse-current-external-compare-evidence.v2.yaml` — current fixed-TRAN external evidence.
- `docs/baselines/channel-s2p-matched-cli-current-external-compare-evidence.v3.yaml` — current matched-S2P CLI external evidence.
- `docs/baselines/channel-s2p-matched-cli-current-external-compare-evidence.v4.yaml` — current matched-S2P CLI external evidence for the current product candidate.
- `docs/baselines/p3b-link-stage-capability-ledger.v1.yaml` — current causal-FIR and diagnostic-receiver non-claims.
- `docs/baselines/p4b-external-ami-asset-set.v1.yaml` — AMI asset admission blocker.
- `docs/baselines/com-r480-acceptance.v1.yaml` and `docs/baselines/com-r480-reference-availability-preflight.v1.yaml` — COM required-profile/reference blocker.
- `docs/baselines/audits/2026-08-11-p2-fixed-tran-current-evidence-rebinding-v2.md` — latest fixed-TRAN evidence rationale.
- `docs/baselines/audits/2026-08-11-p3a-matched-s2p-cli-current-evidence-rebinding-v3.md` — latest channel CLI evidence rationale.

## Suggested skills

- `orca-cli` — obtain exactly one independent Orca-hosted OpenCode audit after a substantive future slice.
- `handoff` — prepare another concise continuation document when a new external decision or asset unblocks work.
