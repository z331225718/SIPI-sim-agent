# Handoff: M5B-03 external boundaries

## Current state

- Target `main` is at `2adbcc3`. Clean PyBERT source worktree
  `C:\Users\z3312\code\.m5b-pybert-source` is at `f6ba031` on
  `codex/m5b-source-gates`; it remains based on candidate `b3d072e`.
  Do not use the user-owned dirty PyBERT main worktree as source evidence.
- Source commit `7fb6182` adds a strict loader for an already-produced
  `agent-spice.rfm-response.v1` meta/binary pair and routes it into the
  existing Rust current-drive receiver without rerunning Agent-Spice. It does
  not alter `V(f)=sign*H(f)@I(f)`, its FFT/IFFT implementation, or a golden.
- Source commit `93c8c9e` requires explicit input/output port intent for that
  CLI and rejects a response artifact whose ports do not match the request.
  Exact `dtS`, sign, output port index, binary shape, layout, and finite
  values remain fail-closed checks.
- Target commit `876d380` adds `PyBertAgentSpiceResponseAdapter`. It accepts
  only a materialized, hash-verified RFM metadata/binary pair, invokes the new
  strict CLI, and records both platform artifact refs in output provenance.
  It deliberately does not create a `ChannelResponseV1` or a second resolver.
  `ce3069a` retains explicit port intent in the adapter fixture.
- Local focused gates passed: source channel/backend tests (28 passed, 3
  opt-in skips before the port-intent follow-up; then channel 9 passed, 3
  skips) and target adapter tests (7 passed). The OMP audit independently
  reports root suite 56/56 and source channel suite 8/8 passed.
- OMP `msg_0ad1e6785cf8` and independent OpenCode review
  `msg_acedd84f2ef2` both conclude 0 P1 / 0 P2 / 0 P3. The bridge is accepted
  and recorded in the audit log, but M5B-03 remains open for the S2P branch.
- SIPI `029c09d`, `58971aa`, `677f8af`, and `437d8bc`, together with source
  `244f6db` and `f6ba031`, add the constrained Python S2P external boundary.
  Existing `calc_chnl_h` remains the only S2P numerical resolver. Its
  discrete `V/sample` taps are serialized as `V/s` by dividing by `dt`; the
  native consumer multiplies by `dt`, restoring those taps. SIPI admits the
  DTO through exactly one M4 resolver call and records fail-closed lineage.
- `221ac0c` adds the non-hermetic cross-repository gate
  `tools/verify_m5b_s2p_link_parity.py`; `2adbcc3` makes its relative `--s2p`
  path resolve from the explicitly supplied clean source root. The gate runs
  the real legacy Python S2P producer, one M4 identity/sign admission, and the
  real `sim-native` CLI for tracked
  `models/sp_file/channel_16ghz_3db.s2p`
  (`sha256=6263c436bdc05150ac4263945abac44542f2f59daa33a6be2aa41e446345cea2`).
  Its seven Link arrays plus BER/sampled-eye/jitter/error-count checks passed;
  it records provenance and the V/sample <-> V/s round trip without changing a
  golden or channel algorithm. The root suite also passed 56/56.
- OMP `msg_f355f8f9b021` and independent OpenCode reviews
  `msg_d86556e9b3e4`/`msg_e09811d1e784` all conclude 0 P1 / 0 P2. This accepts
  only the fixed external S2P fixture's Link handoff parity. It does not claim
  that M4 resolves S2P or independently validates conditioning/IFFT/termination,
  nor any ADS/AMI/S4P/general S-parameter parity. The structural NetworkTensor
  still has a placeholder data-artifact hash and is not independent S2P matrix
  evidence.

## Next steps

1. Complete the remaining M5B-03 RFM evidence: run one same-configuration
   current-domain RFM reference-to-native Link compare with current sign,
   explicit ports, FFT mapping, and provenance. Do not coerce impedance-domain
   RFM into the voltage-transfer DTO or add another resolver.
2. M5B-04 retains the Python AMI host as reference. M5A's Rust ABI host is not
   AMI parity. M5B-05 needs a separate GUI/optimizer unsupported matrix.
3. Keep M5B-01 Task 13 open (nightly/RSS/perf/remote and corrupted wheel,
   panic/OOM, and real child-process faults). Filter-repo remains user-gated
   and last.
