# Handoff：P4B-02 AMI 参数列表家族交接（round 240 完成 02b187 last-occurrence-map，未提交）

## 目标背景
- 本轮交付 **02b187 parameter-list-last-occurrence-map**（item→末次出现下标映射表），未提交。
- 02b187 是 02b186 first-occurrence-map 的**镜像伴侣**：相同 BTreeMap 结构、相同键集，值取末次下标（02b186 取首现）；单次出现项上 `last == first`。

## 当前健康态（round 240 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=339, VALID_KEY=315, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=409, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=357**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：**185**（02b003..02b187，与 runner 文件数一致 185）
  - `parameter_list_*.rs` 模块：**93**
  - `cargo test -p sipi-ami-text --lib`：**1073 全绿**
  - PLAN.md：661 行 CRLF（659 基线 + 2 行叙事），0 bare LF / 0 lone CR；ledger 527 CRLF

## 本会话交付：02b187 parameter-list-last-occurrence-map
- **语义**：`parameter_list_last_occurrence_map_v1` 返回 `BTreeMap<String, usize>`，键为每个 distinct trimmed item，值为该项**末次**出现的 0-based 下标。02b186 的镜像伴侣（同一键集，值互补：逐项 `last >= first`，等号恰在单次出现项）。
- **工件**：Rust 模块（6 单测）+ runner（单值 JSON 输入模式）+ crosscheck 4/4 + verifier + unittest 6 + charter（based_on=186）+ source map（mapping×2）+ audit + PLAN 叙事 + ledger note+gate。
- **修复**：发现 round 239 的 ledger gates 列表漏加 02b186 gate 条目（列表停在 02b185），本轮一并补上 02b186 + 02b187 两条 gate，恢复 ledger 与 coverage 工具一致。

## 未提交文件清单（30 新 + 6 改，含 rounds 238/239 未提交项）
- 新增：02b185（is-balanced）、02b186（first-occurrence-map）、02b187（last-occurrence-map）三套工件（模块+runner+crosscheck+verifier+unittest+charter+source map+evidence+audit）+ handoff round234/238/239/240
- 修改：lib.rs（+3 mod +3 pub use）、Cargo.toml（+3 [[test]]，185 段）、PLAN.md（+3 叙事，661 CRLF）、ledger（note+gate×3）、coverage gate（+3）、test 锚点 354→357

## 建议接续动作
1. **先提交 02b185 + 02b186 + 02b187**（30 新 + 6 改），排除 `_` 前缀临时脚本（25 个历史遗留，未触碰）。
2. **继续 02b188+**：候选包括 occurrence-indices-map（item→全部出现下标 Vec，02b186/187 的富化形态）或 profile 层操作。
3. 或转向其它主项（先读 charter/evidence 现状）。

## 便于接续的最小复核命令
- `git status --short`（30 新 + 6 改）
- `python -B tools/verify_session_health.py`（6/6：TOTAL=339, VALID_KEY=315, gates=357, links=409）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（gates=357 锚点）
- `cargo test -p sipi-ami-text --lib`（1073 全绿）

## 风险与注意事项（历史踩坑，务必遵守）
- **updater 必须用二进制模式读写 PLAN.md/ledger**：文本模式 universal newlines 会把 CRLF 归一化为 LF（round 240 首版 updater 即踩坑，修复为 `read_bytes`/`write_bytes` 并断言 `bare == 0`）。
- 插入叙事前先确认目标文件没有重复叙事（重复插入会产生孤立 CR，导致 git diff 全文件误报）。
- 命名约定：charter/evidence schema 用连字符 `sipi.p4b-02bNNN-parameter-...`；policy 常量用点号 `sipi.p4b-02bNNN.parameter-...`。
- ledger 是 JSON（非 YAML），note 追加需修改 P4B-02 item 的 "note" 字段值，不能在文件末尾追加行。
- Cargo.toml 追加 `[[test]]` 前用 `tail -c 120 | od -c` 确认尾部换行，避免 `harness = false[[test]]` 粘连。
