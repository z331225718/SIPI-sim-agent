# Handoff：P4B-02 AMI 参数列表家族交接（round 244 完成 02b191 pairwise-equal-adjacent，未提交）

## 目标背景
- 本轮交付 **02b191 parameter-list-pairwise-equal-adjacent**（相邻等值对列表），未提交。
- 02b191 是 02b184 pairwise-distinct-adjacent 的**等值镜像**：同窗口（长度-2）、互补谓词（`==` vs `!=`）；对数恒等于 02b134 equal-adjacent count。

## 当前健康态（round 244 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=343, VALID_KEY=319, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=413, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=361**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：**189**（02b003..02b191，与 runner 文件数一致 189）
  - `parameter_list_*.rs` 模块：**97**
  - `cargo test -p sipi-ami-text --lib`：**1097 全绿**
  - PLAN.md：669 行 CRLF（667 基线 + 2 行叙事），0 bare LF / 0 lone CR；ledger 531 CRLF

## 本会话交付：02b191 parameter-list-pairwise-equal-adjacent
- **语义**：`parameter_list_pairwise_equal_adjacent_v1` 返回 `Vec<(String, String)>`，对每个相邻下标 i 且 `item[i] == item[i+1]` 返回 `(item[i], item[i+1])`，保序含重复；单项目/全不同列表返回空列表（空结果合法）。
- **工件**：Rust 模块（6 单测）+ runner（单值 JSON 输入模式）+ crosscheck 4/4 + verifier + unittest 6 + charter（based_on=190）+ source map（mapping×2）+ audit + PLAN 叙事 + ledger note+gate。
- **零踩坑**：updater 沿用二进制 + CRLF 感知 line_end，一次通过。

## 未提交文件清单（11 新 + 6 改，本轮增量）
- 新增：`src/parameter_list_pairwise_equal_adjacent_v1.rs`、runner、crosscheck、verifier、unittest、charter、source map、evidence、audit、handoff
- 修改：lib.rs（+mod+pub use）、Cargo.toml（+[[test]]，189 段）、PLAN.md（+叙事，669 CRLF）、ledger（note+gate）、coverage gate 360→361、test 锚点 360→361

## 建议接续动作
1. **先提交 02b190 + 02b191**（22 新 + 6 改），排除 `_` 前缀临时脚本。
2. **继续 02b192+**：候选包括 relative-frequency-map（02b181 的 map 形态）或 profile 层操作。
3. 或转向其它主项（先读 charter/evidence 现状）。

## 便于接续的最小复核命令
- `git status --short`（22 新 + 6 改）
- `python -B tools/verify_session_health.py`（6/6：TOTAL=343, VALID_KEY=319, gates=361, links=413）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（gates=361 锚点）
- `cargo test -p sipi-ami-text --lib`（1097 全绿）

## 风险与注意事项（历史踩坑，务必遵守）
- **updater 必须二进制模式 + CRLF 感知 line_end**：`read_bytes`/`write_bytes`；`line_end = data.index(b"\r\n", idx) + 2`；插入前断言 anchor 唯一 + 目标叙事不存在。
- **ledger 是 CRLF**：gate 尾部 anchor 用 `\r\n`；coverage 工具（.py）是 LF，anchor 用 `\n`。
- 命名约定：charter/evidence schema 用连字符；policy 常量用点号。
- ledger 是 JSON（非 YAML），note 追加需修改 "note" 字段值，不能文件末尾追加行。
- Cargo.toml 追加 `[[test]]` 前用 `tail -c 50 | od -c` 确认尾部换行。
