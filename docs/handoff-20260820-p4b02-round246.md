# Handoff：P4B-02 AMI 参数列表家族交接（round 246 完成 02b193 prevalence-map，未提交）

## 目标背景
- 本轮交付 **02b193 parameter-list-prevalence-map**（mode 子集比例 map），未提交。
- 02b193 是 02b180 prevalence-ratio（单值 f64）的 map 形态：BTreeMap 仅含达到最大计数的 item（mode 集合），值 = 共享比例 max_count/total（每个 mode item 相同，== 02b180 单值）。**语义经 owner B2 确认**（02b180 无列表形态、02b192 已覆盖全 item 比例，需区分；选择「mode 子集比例 map」）。

## 当前健康态（round 246 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=345, VALID_KEY=321, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=415, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=363**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：**191**（02b003..02b193，与 runner 文件数一致 191）
  - `parameter_list_*.rs` 模块：**99**
  - `cargo test -p sipi-ami-text --lib`：**1109 全绿**
  - PLAN.md：673 行 CRLF（671 基线 + 2 行叙事），0 bare LF / 0 lone CR；ledger 533 CRLF

## 本会话交付：02b193 parameter-list-prevalence-map
- **语义**：`parameter_list_prevalence_map_v1` 返回 `BTreeMap<String, f64>`，仅含 count == max_count 的 mode items，值为 max_count/total（f64，(0,1] 内）。全等列表 → 单条目 1.0；全不同 → n 条目各 1/n；并列 mode → 多条目同值。
- **f64 位精确**：crosscheck 以 12 位小数字符串逐位比较（产品 `format!("{x:.12}")` vs Python `f"{x:.12f}"`），4/4 matched_hash_bound，含 tied_mode 用例。
- **工件**：Rust 模块（6 单测，含并列 mode）+ runner（12 位小数字符串输出）+ crosscheck 4/4 + verifier + unittest 6 + charter（based_on=192）+ source map（mapping×2）+ audit + PLAN 叙事 + ledger note+gate（note 记录 owner 确认）。
- **零踩坑**：updater 沿用二进制 + CRLF 感知 line_end，一次通过。

## 未提交文件清单（11 新 + 6 改，本轮增量）
- 新增：`src/parameter_list_prevalence_map_v1.rs`、runner、crosscheck、verifier、unittest、charter、source map、evidence、audit、handoff
- 修改：lib.rs（+mod+pub use）、Cargo.toml（+[[test]]，191 段）、PLAN.md（+叙事，673 CRLF）、ledger（note+gate）、coverage gate 362→363、test 锚点 362→363

## 建议接续动作
1. **先提交 02b190..02b193**（44 新 + 6 改），排除 `_` 前缀临时脚本。
2. **继续 02b194+**：候选包括 longest-run-length-map（02b119/02b120 的 map 形态）或 profile 层操作。
3. 或转向其它主项（先读 charter/evidence 现状）。

## 便于接续的最小复核命令
- `git status --short`（44 新 + 6 改）
- `python -B tools/verify_session_health.py`（6/6：TOTAL=345, VALID_KEY=321, gates=363, links=415）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（gates=363 锚点）
- `cargo test -p sipi-ami-text --lib`（1109 全绿）

## 风险与注意事项（历史踩坑，务必遵守）
- **updater 必须二进制模式 + CRLF 感知 line_end**：`read_bytes`/`write_bytes`；`line_end = data.index(b"\r\n", idx) + 2`；插入前断言 anchor 唯一 + 目标叙事不存在。
- **f64 切片**：产品 runner 用 `format!("{x:.12}")` 字符串输出，Python 参考用 `f"{x:.12f}"`，逐位字符串比较（12 位小数）。
- **含歧义切片先问 owner**：02b180 无列表形态、与 02b192 重叠风险，本轮先经 owner 确认「mode 子集比例 map」再实现（避免返工）；owner 确认已记入 ledger note。
- **ledger 是 CRLF**：gate 尾部 anchor 用 `\r\n`；coverage 工具（.py）是 LF，anchor 用 `\n`。
- 命名约定：charter/evidence schema 用连字符；policy 常量用点号。
- ledger 是 JSON（非 YAML），note 追加需修改 "note" 字段值，不能文件末尾追加行。
- Cargo.toml 追加 `[[test]]` 前用 `tail -c 50 | od -c` 确认尾部换行。
