# Handoff：P4B-02 AMI 参数列表家族交接（round 236 完成 02b184）

## 目标背景
- 本次交接从当前工作树状态继续，goal 为「推进剩下所有未完成的plan」（P4B-02 纵向能力切片节奏）。
- 核心工作仍在 **P4B-02 typed AMI 参数语义家族**（sipi-ami-text crate），按「小而完整的纵向能力切片」推进：每个切片自带 charter / source map / verifier / unittest / crosscheck / audit / PLAN 行 / ledger gate。
- round 234 交付 02b182 first-occurrence-indices；round 235 交付 02b183 run-boundaries；**本轮（236）交付 02b184 pairwise-distinct-adjacent**（经 owner 确认语义：相邻不同对列表——每对是 (item[i], item[i+1])，i 处两项不等，保序含重复）。

## 当前健康态（round 236 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=336, VALID_KEY=312, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=406, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=354**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：**182**（02b003..02b184，harness=false，与 runner 文件数一致 182）
  - `crates/sipi-ami-text/src/parameter_list_*.rs` 模块：**90**
  - evidence 文件（p4b-02b*-crosscheck-evidence.v1.yaml）：**182**
  - ledger P4B-02 verifier gate 条目：**184**
  - PLAN.md：655 行，CRLF 保持（655 CRLF / 0 bare LF）；ledger 525 CRLF / 0 bare LF
  - `cargo test -p sipi-ami-text --lib`：**1055 全绿**

## 本轮交付：02b184 pairwise-distinct-adjacent（owner 确认为「相邻不同对列表」）
- **语义**：`parameter_list_pairwise_distinct_adjacent_v1` 返回每个相邻下标 i 处两项不等（原始字节相等，02b0 绑定）的 (item[i], item[i + 1]) 对，按列表顺序保留重复对；对数恒等于 02b172 相邻变化计数，每对是 02b157 序列包含的长度-2 窗口；单项目/全等列表为空结果（空结果合法，镜像 02b40 空搜索）。fail-closed：NotAList / MalformedList。6 Rust 单测 + crosscheck **4/4 matched_hash_bound**（mixed / duplicate_pairs / all_equal_empty / non_list）。
- 治理工件全套：charter（based_on=183 列表+上一 charter 路径，183 条）、source map（mapping 长度 2）、audit、verifier + 6 单测（含证据篡改拒绝）、crosscheck（evidence CRLF，schema 连字符）、PLAN 叙事（插在 02b183 audit 链接后，CRLF 无损断言通过）、ledger gate+note、coverage gate 353→354。

## 语义锚点提醒（延续）
- **命名约定两类**：schema 用连字符（`sipi.p4b-02bNNN-<name>-stage.v1` / `-crosscheck-evidence.v1`；source map 例外 `sipi.p4b-02bNNN.mit-source-map.v1`），policy 常量用点号（`sipi.p4b-02bNNN.<name>-v1.<short>`）。02b183 曾踩坑（evidence schema 点号），02b184 一次到位。
- NotAList / MalformedList 防御 + 每切片自有 Error enum；查询项不 trim、列表项 trim；空 token `()` 语义 token 层全函数；空结果合法形态已在 02b40/02b184 确立。
- 跨切片共享类型勿改。

## 当前阻塞 / 不可推进项（勿重复实现）
- 同前轮：P3C-02 剩余 eye folding/bins、P3B-05 jitter observable、P3C-03/P5-02/P5-06 剩余、P4B-08/09（ADS 资产）、P1-04B/P2-06/P3B-02/P3C-01/P4A-01/02（owner 决策/契约）。
- P4B-02 主项仍 open：reserved-name catalog 与 profile 规则 pending。

## 建议接续动作（优先级）
1. 继续 P4B-02 值级列表族（02b185 起）候选：**is-balanced（全游程等长，值无关位置统计）**、**first-occurrence-map（首现序→首现下标映射表）** 等；每个仍按「模块+注册→runner+crosscheck→verifier+unittest→charter+source map+audit→updater（PLAN/ledger/coverage gate）→verify+pytest+session health」模板执行，完成后纳入同一提交或单独提交。
2. 或转向 profile 层操作（02b44/47/48/64/69/74/76/77/79-82 已有基础）。
3. 若继续其它主项，先读对应 charter/evidence 现状再动手（fail-closed）。

## 已知风险与注意事项
- **传输层反斜杠剥离**：Python 补丁脚本用 chr(92)/chr(34)/chr(39)/chr(13)/chr(10)/chr(96) 构造；写含正则的 crosscheck 后必须 repr 验证 `\|`/`\(`/`\)`/`\s` 幸存。
- **PLAN.md 必须保持 CRLF 字节级**（655 行、655 CRLF、0 bare LF）；ledger 同 CRLF（现 525 行）；改后二进制断言 `count(b"\n") == count(b"\r\n")`。
- **ledger YAML**：gate 数组内条目逗号分隔（02b183 变非末项后必须补尾逗号）；note 追加用 '+ ' 前缀。
- **Cargo.toml 尾部陷阱**：追加 [[test]] 前先 `tail -c n | od -c` 确认末尾换行形态，避免 `harness = false[[test]]` 粘连（02b183 踩过）。
- **pwsh tool**：description ≤4 词；cargo 不在 PATH 需前置 `export PATH="$HOME/.cargo/bin:$PATH"`；UTF-8 输出用文件中转再 read。
- **cargo test -p sipi-ami-text <过滤词>**：把过滤词作为位置参数传给 harness=false runner 导致 panic（预期现象，非回归）；验证单测用 `cargo test -p sipi-ami-text --lib <模块名>`。
- **临时脚本纪律**：新增临时脚本（`_mk*_governance.py`、`_update*_ami.py` 等）用后即删；工作树遗留的 25 个 `_fix_*`/`_reg_*`/`_tmp_*` 等未跟踪脚本不要提交。
- 严格约束：不放宽容差、不改 golden、不绕过验证门禁；approval prompts disabled（不得设 sandbox_permissions）。
- **提交注意**：`git add -A` 会纳入未跟踪临时脚本，提交前必须 `git reset -- $(git diff --cached --name-only | grep -E '(^|/)_')` 批量排除；提交信息聚焦 why（新 commit，不 amend）。

## 便于接续的最小复核命令
- `git status --short`（期待：仅未跟踪临时脚本 + 02b183/02b184 改动）
- `python -B tools/verify_session_health.py`（期待 6/6 valid：TOTAL=336, VALID_KEY=312, gates=354, links=406）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（期待 gates=354 锚点）
- `cargo test -p sipi-ami-text --lib`（期待 1055 全绿；harness=false runner 仅用于 crosscheck）