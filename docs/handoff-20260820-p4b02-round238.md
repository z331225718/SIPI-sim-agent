# Handoff：P4B-02 AMI 参数列表家族交接（round 238 完成 02b185 is-balanced，未提交）

## 目标背景
- 本次交接从当前工作树状态继续，goal 为「推进剩下所有未完成的plan」（P4B-02 纵向能力切片节奏）。
- 核心工作仍在 **P4B-02 typed AMI 参数语义家族**（sipi-ami-text crate），按「小而完整的纵向能力切片」推进：每个切片自带 charter / source map / verifier / unittest / crosscheck / audit / PLAN 行 / ledger gate。
- round 237 已提交 `0d4e0c9`（02b183 + 02b184）；**本会话交付 round 238（02b185 is-balanced），未提交**，工作树含 10 个新文件 + 4 个修改。

## 当前健康态（round 238 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=337, VALID_KEY=313, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=407, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=355**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：**183**（02b003..02b185，harness=false，与 runner 文件数一致 183）
  - `crates/sipi-ami-text/src/parameter_list_*.rs` 模块：**91**
  - evidence 文件（p4b-02b*-crosscheck-evidence.v1.yaml）：**183**
  - ledger P4B-02 verifier gate 条目：**185**
  - PLAN.md：657 行，CRLF 保持（657 CRLF / 0 bare LF）；ledger 525 CRLF / 0 bare LF
  - `cargo test -p sipi-ami-text --lib`：**1061 全绿**

## 本会话交付：02b185 parameter-list-is-balanced
- **语义**：`parameter_list_is_balanced_v1` 返回 bool，表示 List 类型 `AmiParameterValueV1` 的 trim 后项中每个最大游程（相邻相等项的最大连续段）是否具有相同长度。单游程平凡平衡（true）；全不同列表（每游程长 1）亦平衡（true）；混合游程长度不同返回 false。fail-closed：NotAList / MalformedList。
- **关系**：02b168 run-count 的布尔伴侣（游程等长 ⟺ 所有 run-length-encode 条目长度相等）、02b183 run-boundaries 的等长判断（所有边界跨度相等 ⟺ is-balanced）、02b160 all-equal 的推广（全等列表必平衡，但平衡列表不必全等）。
- **工件**：Rust 模块（6 单测）+ runner + crosscheck 4/4 + verifier + unittest 6 + charter（based_on=184）+ source map（mapping×2）+ audit + PLAN 叙事 + ledger gate+note。
- **过程中发现并修正**：runner 首版按 02b183 的 "遍历 parameters 数组" 模式编写，与 crosscheck 的 "单个 JSON 值输入" 模式不匹配（non_list 用例 product 返回空 results vs reference 返回 NotAList）；重写 runner 为 02b184 的 "单值输入 + top-level {policy, valid, ...}" 模式后 4/4 通过。另修复 source map mapping 长度（首版 1 → 修正为 2，匹配 02b184 结构）和 lib.rs pub use 遗漏 policy 常量导出。

## 未提交文件清单（10 新 + 4 改，不含临时脚本）

### 新增文件（10）
| 文件 | 说明 |
|---|---|
| `src/parameter_list_is_balanced_v1.rs` | 02b185 实现 |
| `tests/p4b_02b185_..._runner.rs` | 02b185 runner |
| `tools/run_p4b_02b185_..._crosscheck.py` | 02b185 crosscheck |
| `tools/verify_p4b_02b185_...py` | 02b185 verifier |
| `tools/test_verify_p4b_02b185_...py` | 02b185 unittest |
| `baselines/p4b-02b185-...-stage.v1.yaml` | 02b185 charter |
| `baselines/p4b-02b185-mit-source-map.v1.yaml` | 02b185 source map |
| `baselines/p4b-02b185-...-crosscheck-evidence.v1.yaml` | 02b185 evidence |
| `baselines/audits/2026-08-16-p4b-02b185-...md` | 02b185 audit |
| `docs/handoff-20260820-p4b02-round238.md` | 本 handoff |

### 修改文件（4）
| 文件 | 变更 |
|---|---|
| `crates/sipi-ami-text/src/lib.rs` | +1 mod + pub use 块（含 POLICY 常量） |
| `crates/sipi-ami-text/Cargo.toml` | +1 [[test]] 段（183 总 == 183 runner） |
| `PLAN.md` | +1 叙事段（CRLF 657 行，0 bare LF） |
| `docs/baselines/plan-remaining-items-ledger.v1.yaml` | P4B-02 note 追加 + gate 条目（525 CRLF） |
| `tools/verify_plan_open_items_gate_coverage.py` | gate 列表 +1（gates=355） |
| `tools/test_verify_plan_open_items_gate_coverage.py` | 锚点 354→355 |

## 语义锚点提醒（延续）
- **命名约定**：schema 用连字符（`sipi.p4b-02b185-parameter-list-is-balanced-stage.v1`），policy 常量用点号（`sipi.p4b-02b185.parameter-list-is-balanced-v1.is-balanced`）。
- NotAList / MalformedList 防御 + 每切片自有 Error enum；查询项不 trim、列表项 trim；空 token `()` 语义 token 层全函数；空结果合法形态已在 02b40/02b183/02b184 确立。
- 跨切片共享类型勿改。

## 当前阻塞 / 不可推进项（勿重复实现）
- 同前轮：P3C-02 剩余 eye folding/bins、P3B-05 jitter observable、P3C-03/P5-02/P5-06 剩余、P4B-08/09（ADS 资产）、P1-04B/P2-06/P3B-02/P3C-01/P4A-01/02（owner 决策/契约）。
- P4B-02 主项仍 open：reserved-name catalog 与 profile 规则 pending。

## 建议接续动作（优先级）
1. **先提交 02b185**：`git add` 上述 14 文件（排除 `_` 前缀临时脚本），commit message 聚焦 why。
2. **继续 P4B-02 值级列表族（02b186 起）**：候选 **first-occurrence-map**（首现序→首现下标映射表，02b182 的 map 形态伴侣）等；每个仍按模板执行。
3. 或转向 profile 层操作（02b44/47/48/64/69/74/76/77/79-82 已有基础）。

## 已知风险与注意事项
- **传输层反斜杠剥离**：Python 补丁脚本用 chr(92)/chr(34)/chr(39)/chr(13)/chr(10)/chr(96) 构造；写含正则的 crosscheck 后必须 repr 验证幸存。
- **PLAN.md 必须保持 CRLF 字节级**（657 行、657 CRLF、0 bare LF）；ledger 同 CRLF（525 行）。
- **ledger 是 JSON（非 YAML）**：note 追加必须修改 P4B-02 item 的 "note" 字段值（字符串拼接），不能在文件末尾追加新行（会导致 YAML/JSON 解析失败）。02b185 首次踩坑（追加到 JSON 结构外），已修复。
- **Cargo.toml 尾部陷阱**：追加 [[test]] 前先 `tail -c 120 | od -c` 确认末尾换行形态。
- **runner 模式**：02b183 采用 "parameters 数组" 模式（遍历 JSON 数组），02b184/02b185 采用 "单值输入" 模式（{name, type, value}，top-level {policy, valid, ...}）。新切片应遵循 02b184/02b185 的单值模式。
- 严格约束：不放宽容差、不改 golden、不绕过验证门禁；approval prompts disabled。
- **提交注意**：`git add -A` 会纳入未跟踪临时脚本，提交前排除。

## 便于接续的最小复核命令
- `git status --short`（期待：10 新文件 + 4 修改 + 未跟踪临时脚本）
- `python -B tools/verify_session_health.py`（期待 6/6 valid：TOTAL=337, VALID_KEY=313, gates=355, links=407）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（期待 gates=355 锚点）
- `cargo test -p sipi-ami-text --lib`（期待 1061 全绿；harness=false runner 仅用于 crosscheck）
