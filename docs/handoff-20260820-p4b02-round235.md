# Handoff：P4B-02 AMI 参数列表家族交接（round 235 完成 02b183）

## 目标背景
- 本次交接从当前工作树状态继续，goal 为「推进剩下所有未完成的plan」（P4B-02 纵向能力切片节奏）。
- 核心工作仍在 **P4B-02 typed AMI 参数语义家族**（sipi-ami-text crate），按「小而完整的纵向能力切片」推进：每个切片自带 charter / source map / verifier / unittest / crosscheck / audit / PLAN 行 / ledger gate。
- 上一轮（round 234）交付 02b182 first-occurrence-indices；**本轮交付 02b183 run-boundaries**（round 234 handoff 优先级最高的候选）。

## 当前健康态（round 235 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=335, VALID_KEY=311, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=405, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=353**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：**181**（02b003..02b183，harness=false，与 runner 文件数一致 181）
  - `crates/sipi-ami-text/src/parameter_list_*.rs` 模块：**89**
  - evidence 文件（p4b-02b*-crosscheck-evidence.v1.yaml）：**181**
  - ledger P4B-02 verifier gate 条目：**183**
  - PLAN.md：655 行，CRLF 保持（655 CRLF / 0 bare LF）
  - `cargo test -p sipi-ami-text --lib`：**1049 全绿**

## 本轮交付：02b183 run-boundaries
- **02b183 run-boundaries（本会话交付）**：`parameter_list_run_boundaries_v1.rs`：`parameter_list_run_boundaries_v1` 返回 List 值 trim 后项相邻相等最大游程的每段 (零基含起点, 零基含终点) 边界对（02b0 原始字节相等绑定；按游程顺序，首游程起点恒 0、末游程终点恒 item_count - 1）；列表按 02b1 规则非空故至少一个游程存在（单项目列表为 (0, 0)），对个数等于 02b168 游程数、各对覆盖与 02b119 游程编码相同分解、02b171 最长游程起点即跨度最大对的起点。fail-closed：NotAList / MalformedList。6 Rust 单测 + crosscheck **4/4 matched_hash_bound**（mixed/all_equal/all_distinct/non_list）。
- 治理工件全套：charter（based_on=182 列表+上一 charter 路径，182 条）、source map（mapping 长度 2）、audit、verifier + 6 单测（含证据篡改拒绝）、crosscheck 脚本（evidence 以 CRLF 写入，与 evidence 家族一致）、PLAN 叙事（插在 02b182 audit 链接后，CRLF 无损）、ledger gate+note、coverage gate 352→353。

## 语义锚点提醒（延续）
- 命名约定实为两类：**schema 用连字符**（`sipi.p4b-02bNNN-<name>-stage.v1` / `-crosscheck-evidence.v1` / `sipi.p4b-02bNNN.mit-source-map.v1`），**policy 常量用点号**（`sipi.p4b-02bNNN.<name>-v1.<short>`）。本轮发现并修正：evidence schema 首版误写为点号 `sipi.p4b-02b183.parameter-list-...`，已改连字符并与 02b182 实测文件一致。
- NotAList / MalformedList 防御 + 每切片自有 Error enum；查询项不 trim、列表项 trim；空 token `()` 语义 token 层全函数。
- 跨切片共享类型勿改。

## 当前阻塞 / 不可推进项（勿重复实现）
- 同前轮：P3C-02 剩余 eye folding/bins、P3B-05 jitter observable、P3C-03/P5-02/P5-06 剩余、P4B-08/09（ADS 资产）、P1-04B/P2-06/P3B-02/P3C-01/P4A-01/02（owner 决策/契约）。
- P4B-02 主项仍 open：reserved-name catalog 与 profile 规则 pending。

## 建议接续动作（优先级）
1. 继续 P4B-02 值级列表族（02b184 起）候选：**is-balanced**（值无关位置统计）、**pairwise-distinct-adjacent**（相邻不同对）、**first-occurrence-map**（首现序→首现下标映射表，02b182 的 map 形态伴侣）等；每个仍按「模块+注册→runner+crosscheck→verifier+unittest→charter+source map+audit→updater（PLAN/ledger/coverage gate）→verify+pytest+session health」模板执行，完成后纳入同一提交或单独提交。
2. 或转向 profile 层操作（02b44/47/48/64/69/74/76/77/79-82 已有基础）。
3. 若继续其它主项，先读对应 charter/evidence 现状再动手（fail-closed）。

## 已知风险与注意事项
- **传输层反斜杠剥离**：Python 补丁脚本用 chr(92)/chr(34)/chr(39)/chr(13)/chr(10)/chr(96) 构造；写含正则的 crosscheck 后必须 repr 验证 `\|`/`\(`/`\)`/`\s` 幸存。
- **PLAN.md 必须保持 CRLF 字节级**（655 行、655 CRLF、0 bare LF）；ledger 同 CRLF（现 524 行）；改后二进制断言 `count(b"\n") == count(b"\r\n")`。
- **ledger YAML**：gate 数组内条目逗号分隔（02b182 变非末项后必须补尾逗号）；note 追加用 '+ ' 前缀。
- **schema/policy 命名**：见上文提醒（schema 连字符、policy 点号），新切片先 grep 上一切片实测字符串再落笔。
- **pwsh tool**：description ≤4 词；cargo 不在 PATH 需前置 `export PATH="$HOME/.cargo/bin:$PATH"`；UTF-8 输出用文件中转再 read。
- **cargo test -p sipi-ami-text <过滤词>**：把过滤词作为位置参数传给 harness=false runner 导致 panic（预期现象，非回归）；验证单测用 `cargo test -p sipi-ami-text --lib <模块名>`。
- **临时脚本纪律**：新增临时脚本（`_mk*_governance.py`、`_update*_ami.py` 等）用后即删；工作树遗留的 27 个 `_fix_*`/`_reg_*`/`_tmp_*` 等未跟踪脚本不要提交。
- 严格约束：不放宽容差、不改 golden、不绕过验证门禁；approval prompts disabled（不得设 sandbox_permissions）。
- **提交注意**：`git add -A` 会纳入未跟踪临时脚本，提交前必须 `git reset -- $(git diff --cached --name-only | grep -E '(^|/)_')` 批量排除；提交信息聚焦 why（新 commit，不 amend）。

## 便于接续的最小复核命令
- `git status --short`（期待：仅未跟踪临时脚本 + 本轮 02b183 改动）
- `python -B tools/verify_session_health.py`（期待 6/6 valid：TOTAL=335, VALID_KEY=311, gates=353, links=405）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（期待 gates=353 锚点）
- `cargo test -p sipi-ami-text --lib`（期待 1049 全绿；harness=false runner 仅用于 crosscheck）