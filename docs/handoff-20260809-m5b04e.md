# Handoff: M5B Candidate Bundle Preflight

## Current State

- SIPI candidate branch `codex/m5b-ami-bundle` is at `c1d4d97`; its worktree
  is clean. The user-owned `agent-spice` worktree remains read-only evidence
  and must not be changed. Its untracked local files are not source evidence.
- M5B-04e accepts an external Windows x64 *candidate policy*, not a runtime:
  `tools/build_m5b_ami_candidate_bundle.py` and its verifier bind candidate
  build-info, SIPI commit/Cargo.lock, `agent-spice@2cc92316` Git-object license
  evidence, executable identity, and a declared system-only closure.
- The real external release observation has manifest SHA-256
  `50f4a4f2d4aa12abbcc9cbb7e5fa30c2d5fc104ee1d43174f6e5682cf0ae00cb` and
  executable SHA-256
  `8fcbe4c059875286b6a2f325b3442f19d39d6be78bc44dc198a2f9b1d165bb1a`.
  It is input-pinned only; do not call it reproducible or promoted.
- Python tests are 7/7 and locked Rust fmt/clippy/test passed (21 tests).
  OpenCode `msg_7949d9ab636f` reports 0 P1 / 0 P2; OMP request
  `msg_d9d9f01901e0` is pending provider recovery. User authorization permits
  this one available independent review for continuation.

## Boundaries

- Do not edit `native/crates/sipi-circuit/engine.lock`, production resolver,
  default/auto routing, Web, GUI, optimizer, numerical code, or golden files.
- Candidate manifests reject vendor `.dll`, `.ami`, and `.ibs` bundle contents.
  Vendor DLL/AMI/IBIS and loader closure remain `blocked_unknown`.
- P3 limits are explicit: `--source-root` is an operator trust boundary and the
  system-DLL list is declared rather than PE-import scanned.
- M5B-04f now parses actual PE normal and delay imports and measures separate
  external twin builds. `405dfb1` found matching closure but different bytes,
  COFF timestamps and `.rdata`; `byteReproducible=false` keeps promotion
  blocked. OpenCode `msg_0a88f7642e48` found 0 P1 / 0 P2.

## Next Steps

1. M5B-04 remains open to explain/eliminate the measured twin-build byte drift,
   then independently repeat it before any explicit lock-promotion review;
   authorized real DLL lifecycle and AMI waveform/BER/eye parity remain after
   that. None may become default/auto before those gates.
2. Continue accumulating M5B-01 Task 13 nightly history and retain its missing
   remote wheel, panic/OOM, and real Agent-Spice fault evidence as open.
3. M5B-03 remains open for an actually independent same-config RFM Link
   receiver comparison; do not add a second S-parameter/RFM resolver.
4. M5B-06 through M5B-09 still require user/legal approval before any
   filter-repo history operation.
