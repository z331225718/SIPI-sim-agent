# Handoff：P4B-02 AMI 参数列表家族交接（round 245 完成 02b192 relative-frequency-map，未提交）

## 目标背景
- 本轮交付 **02b192 parameter-list-relative-frequency-map**（item→比例 count/total 的 BTreeMap 形态），未提交。
- 02b192 是 02b181 frequency-normalized 的 map 形态伴侣（相同数据：BTreeMap 按键 O(1) 查找 vs 首现序 Vec）；逐项比例 == 02b190 count / len，最大项 == 02b180 prevalence ratio。

## 当前健康态（round 245 验证，全部 6 项 gate 绿）
- Session health 6/6：
  - coverage-gate-sweep：**TOTAL=344, VALID_KEY=320, RC0_ALT_KEY=7, RC2_NEEDS_ARGS=17, BAD=0**
  - slice-reference-audit：files_checked=35, slices=9, valid
  - plan-reference-audit：**links_checked=414, missing=0, valid**
  - owner-input-request：entries=11（external_asset=4, owner_decision=7）
  - remaining-items-ledger：items=28
  - open-item-gate-coverage：**gates=362**, items=46
- 关键计数：
  - Cargo.toml `[[test]]` 段：**190**（02b003..02b192，与 runner 文件数一致 190）
  - `parameter_list_*.rs` 模块：**98**
  - `cargo test -p sipi-ami-text --lib`：**1103 全绿**
  - PLAN.md：671 行 CRLF（669 基线 + 2 行叙事），0 bare LF / 0 lone CR；ledger 532 CRLF

## 本会话交付：02b192 parameter-list-relative-frequency-map
- **语义**：`parameter_list_relative_frequency_map_v1` 返回 `BTreeMap<String, f64>`，键为每个 distinct trimmed item，值为比例 `count / total`（(0, 1] 内，和为 1.0）。02b181 frequency-normalized 的 map 形态伴侣。
- **f64 位精确**：crosscheck 以 12 位小数字符串逐位比较（产品 `format!("{x:.12}")` vs Python `f"{x:.12f}"`），4/4 matched_hash_bound。
- **工件**：Rust 模块（6 单测）+ runner（单值 JSON 输入模式，12 位小数字符串输出）+ crosscheck 4/4 + verifier + unittest 6 + charter（based_on=191）+ source map（mapping×2）+ audit + PLAN 叙事 + ledger note+gate。
- **零踩坑**：updater 沿用二进制 + CRLF 感知 line_end，一次通过。

## 未提交文件清单（11 新 + 6 改，本轮增量）
- 新增：`src/parameter_list_relative_frequency_map_v1.rs`、runner、crosscheck、verifier、unittest、charter、source map、evidence、audit、handoff
- 修改：lib.rs（+mod+pub use）、Cargo.toml（+[[test]]，190 段）、PLAN.md（+叙事，671 CRLF）、ledger（note+gate）、coverage gate 361→362、test 锚点 361→362

## 建议接续动作
1. **先提交 02b190 + 02b191 + 02b192**（33 新 + 6 改），排除 `_` 前缀临时脚本。
2. **继续 02b193+**：候选包括 prevalence-map（02b180 的 map 形态）或 profile 层操作。
3. 或转向其它主项（先读 charter/evidence 现状）。

## 便于接续的最小复核命令
- `git status --short`（33 新 + 6 改）
- `python -B tools/verify_session_health.py`（6/6：TOTAL=344, VALID_KEY=320, gates=362, links=414）
- `python -m pytest tools/test_verify_plan_open_items_gate_coverage.py -q`（gates=362 锚点）
- `cargo test -p sipi-ami-text --lib`（1103 全绿）

## 风险与注意事项（历史踩坑，务必遵守）
- **updater 必须二进制模式 + CRLF 感知 line_end**：`read_bytes`/`write_bytes`；`line_end = data.index(b"\r\n", idx) + 2`；插入前断言 anchor 唯一 + 目标叙事不存在。
- **f64 切片**：产品 runner 用 `format!("{x:.12}")` 字符串输出，Python 参考用 `f"{x:.12f}"`，逐位字符串比较（12 位小数）。
- **ledger 是 CRLF**：gate 尾部 anchor 用 `\r\n`；coverage 工具（.py）是 LF，anchor 用 `\n`。
- 命名约定：charter/evidence schema 用连字符；policy 常量用点号。
- ledger 是 JSON（非 YAML），note 追加需修改 "note" 字段值，不能文件末尾追加行。
- Cargo.toml 追加 `[[test]]` 前用 `tail -c 50 | od -c` 确认尾部换行。
