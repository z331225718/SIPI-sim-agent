# Handoff: SIPI-sim-agent M3/M4 平台侧完成状态（交后续 GPT 会话）

> 更新于 2026-08-08 06:30（**M3 完成 checkpoint accepted**；PyAMI Rust 重做决策；本会话转独立审查 agent）。本文件是唯一交接源；旧内容已并入最新状态。

## Current state

### 2026-08-09 continuation

- **M5B-01 staged native-wheel integrity accepted**：clean PyBERT worktree 的
  `c045d8e` 将 external `--wheel-file` 与 SHA-256 绑定，先于 venv 创建和
  `pip` 调用校验；fresh isolated `-I` import 对固定 wheel 成功，单字节
  损坏则 `installer_invoked=false` 且不产生 venv。OMP
  `msg_677f77326ebf` 为 0 P1/0 P2。它只是 Windows staged artifact
  integrity/import 观测，不是远端 wheel、RSS/perf budget、Task13/M5B-01
  completion 或 history migration unlock。
- **M5B-01d locked real-child failure accepted**：同一 clean worktree 的
  `0e6d3ad` 只从 `f6ba031` Git blob 物化 `block_2.rfm`，严格核验
  `111ff6…` Agent-Spice executable、engine lock size/build-info/capability，
  并先走 `AgentSpiceChannelRunner` 正常控制请求。测试私有 subclass 随后仅把
  两个输出路径改为“父路径为文件”，使真实 locked child 在输出提交时以
  `os error 3` / exit 1 失败且不发布 response；不绕过 production runner。
  `AgentSpiceChannelRunner` 也固定用 UTF-8 replacement decoding 保留非本地
  编码 stderr。OMP `msg_5489a72da428` 与 OpenCode `msg_028c550955ba`
  均审计为 0 P1/0 P2。该切片不是
  RFM 数值、Link/receiver parity、timeout/cancel、panic/OOM 或通用引擎可靠性
  证明，Task13/M5B-01 仍未完成。
- **M5B-01e restricted Rust fault-worker accepted**：新的 clean PyBERT
  worktree 的 `8eb62be` 新增 `task13-fault-injection` feature-gated worker，
  默认 build/wheel/runtime 都不可发现。外部 builder 产生 source commit、Cargo
  lock/toolchain、worker/`pybert-core` manifest hash 和 exe SHA 的 manifest；
  runner 只接受 path/SHA/manifest 三元组。Windows Job Object 在 worker 读取
  stdin `go` 前已挂载 64 MiB process/job memory、active-process=1、10 s
  timeout、kill-on-close。最终外部重建/replay 绑定 `8eb62be`，panic 返回 101；
  128 MiB allocation 在 memory limit 内失败且不发布 artifact。OMP
  `msg_c58bddbf36e4`、OpenCode `msg_0db9cc9c708d` 均为 0 P1/0 P2。该项只证明
  受限项目 worker containment，不证明 `pybert_native` production extension
  panic/OOM parity，也不关闭 Task13/M5B-01。
- **M5B-01f production wheel memory containment accepted**：新的 clean PyBERT
  worktree 的 `b6fd1ef` 对 SHA 绑定的 release wheel 作 fresh venv 运行；它仅
  从该 venv 的 site-packages 以 `-I` 加载 public `pybert_native`，并使用现有
  approved RLGC request。child 在 `go` 前已被 Windows Job 的 active-process=1
  与 process/job memory 上限接管。外置报告绑定 wheel `08438587…`，记录 control
  peak 527 MiB，fault cap 15.5 MiB、peak 15.3 MiB、返回 `3221226505`，且没有
  success artifact。OpenCode `msg_e1e196e7147e` 为 0 P1/0 P2（P3 仅记录显式
  opt-in 与 baseline 派生 threshold）；按用户“任一审计可继续”决定接受，OMP
  请求 `msg_506a1680f3b9` 仍 pending，恢复后补记。它只是一个 public workload
  memory-containment 子门，不是 production panic trigger、panic/OOM parity、RSS
  budget、remote-wheel certification 或 Task13/M5B-01 completion。
- **M5B-04 exact raw host-output equivalence accepted**：`e82236c` 将固定
  `example_rx` raw Init/GetWave/Close call 的 retained PyAMI reference 与
  Rust candidate 作 Windows byte-exact 比较。reference 从 `f6ba031` Git
  object 临时物化 PyAMI source/fixture，使用 `c_long` 且三次 ABI 状态均为
  `1`；不走 `run_ami_model`。Init/GetWave arrays、clock sentinel 序列和
  UTF-8 可观测文本均逐字节相等。OpenCode `msg_c77a3738f99a` 与 OMP
  `msg_e9d222e8622b` 均为 0 P1/0 P2。此项仅是单 fixture raw ABI boundary
  equivalence，不是通用 AMI、Link BER/eye、高层 Python pipeline parity、
  promotion、分发或跨平台认证；Python host 继续保留 reference。后续
  `4e9875d` 修正 root runner 对既有 candidate bundle/PE assurance 测试的
  直接执行契约（去除未锁定 pytest 依赖并补真实 main 入口），OMP
  `msg_4011e18dd283` 复核 0 P1/0 P2，root gate 实测 61/61。
- **M5B-04 stateful raw ABI matrix pending audit**：`ee7b5f5` 为 explicit
  candidate-only host 增加 v2 sequence request。外置 Windows report 对授权
  `f6ba031` `example_rx` Git objects 的 Init-only、single GetWave 和不同长度
  two-block sequence 逐 case 比较 retained PyAMI。所有 Init/GetWave/Close 均
  严格 `c_long == 1`，raw f64le、clock sentinel 序列与 UTF-8 text byte-exact；
  每 case 只 Close 一次。v1、`engine.lock`、default/auto/Web/GUI/optimizer 均
  未改。OMP `msg_26c293b208eb` 与 OpenCode `msg_c22462cac3f9` 审计均为 0
  P1/0 P2；OpenCode 仅记录 candidate 未入 `engine.lock` 与 deterministic stub
  sequence 的 P3 边界，均符合本 slice 的非生产设计。它不构成通用 AMI、Link
  BER/eye、Python high-level parity、promotion、分发或跨平台认证。
- **M5B-04 exact authorized runtime contract accepted**：`614a328` 将用户
  授权限制为 PyBERT `f6ba031` 的 exact `example_rx` Git objects、外部
  Windows candidate runtime only；严格 `c_long` ABI 保持 `1=success`、
  `0=failure`。空参数 fail-closed；`(example_rx)` 的 candidate Init 和
  Init+GetWave 都执行 Close 成功。OpenCode `msg_239b906d9789` 与 OMP
  `msg_9c9e9f744eef` 均为 0 P1 / 0 P2。
- **M5B-04 replay gate accepted**：`f09052a` 以外部 candidate bundle 和
  report 为显式输入，直接从 Git object 临时物化 fixture 并核验完整
  lifecycle/sidecars 后清理；不走 Python host。OpenCode `msg_12f96080b263`
  与 OMP `msg_2617d1872130` 均为 0 P1 / 0 P2，仍不构成 AMI numerical
  parity、promotion 或 vendor redistribution。
- 这不是 vendor bytes 迁入/再分发、DLL dependency closure、engine lock
  promotion、默认/auto/Web/GUI/optimizer route、跨平台认证或 AMI 数值
  parity。外部运行证据须在未来 promotion 前保留或做成可重放 gate。
- M5B-01 余项现为时间/外部/用户决策门：30-day nightly 积累、批准 RSS/perf
  budget、受控 SHA-pinned remote wheel、production native-extension panic/OOM
  parity；它们不能靠继续添加 synthetic drill 伪造关闭。下一可实施大边界是
  M5B-04 的 `example_rx` exact-fixture stateful raw-ABI lifecycle matrix
  parity（Init-only、连续 GetWave、Close 一次），仍不得升级为通用 AMI 或 Link
  parity。M5B-03 independent RFM receiver parity 继续 open。用户已批准未来在
  临时 clone 执行 M5B-06/07 filter-repo。`eebdd54` 已双审接受 521-path history
  preflight（OpenCode `msg_f75c1b4c1c2f`、OMP `msg_ffe14c9062ed` 均为 0
  P1/0 P2），但实际迁入仍等待 M5B-01 clean accepted anchor；
  `migrationEligible=false` 且 history map 仍 pending。

目标为“继续推进 M3/M4，每轮做单独审计”。**M3 完成（checkpoint accepted）、M4 平台侧完成，全部经独立审计闭环**；剩余均为外部依赖项（见 Next steps）。

- **M3 完成（2026-08-08）**：M3-07b 真实引擎示例（managed dependency lock `ae26c0f` / 真实 Agent-Spice rfm-response + static_inputs `b616ee7`+`272a4d1` / 真实 pybert bundle `632459f` / 双引擎纵向验收 `2905550`）+ M3-11 G2b Windows 认证（`a81a1ec`，Windows x86_64 managed worker 五字段 hard 可晋级 certified），全部 OMP 审计 0 P1/0 P2。原 M3-08b 数值等价接线移至 PLAN §9.3 M5B-03 验收。
- **M4 平台侧完成**：M4-09 显式 transform policies（`sipi.transform-policy.v1`，实现名机制）、M4-10 cache identity（producer role+instance+bundle 进 key）、M4 wrap checkpoint（退出条件/必测矩阵核验）。逐切片独立审计 0 P1/0 P2。
- **M3 收尾-3b**：publish-crash 恢复。driver 两条 publish 路径统一为 staged 顺序（attempt→publishing→落盘 success-manifest→SQLite commit）；`Supervisor.reconcile` 新增 `_reconcile_publishing`——有可校验 manifest 则按文件字节 sha 重建 succeeded（CAS 幂等），无/损坏则 failed，execution/node 级 cancel 置 cancelled。不自动重跑引擎、不伪造成功。OMP 审计 0 P1/0 P2。
- **M3-11 受管 hard resource enforcement**：`run_process` 支持 resource_limits——Windows Job Object（memory/process count/CPU time + accounting 触限检测），POSIX setrlimit（memory/CPU，process_count 诚实 unsupported）；`execute_backend` 仅 required 时强制、artifact_bytes 超限 ResourceLimit、resource_usage.actual_enforcement 如实；`preflight` 新增 `platform_enforcement`（managed worker hard 满足 required，否则 fail-closed）。Windows 本机认证；未宣称三 OS 认证、未篡改 M2-11 certified 门禁。OMP 审计 0 P1/0 P2（commit `b7e2edc`）。
- **P1 修复（上轮）**：rule-ledger `authority_sha256` 改为 git blob（LF）值，`verify_m1_rule_ledger.py` 先 LF 规范化再哈希，CRLF/LF 检出结果一致。
- **门禁**：`tools/run_all_tests.py` 54/54 通过；`git diff --check` 干净；工作树干净（HEAD `e222e12`）。
- 关键提交：`ae26c0f`（M3-07b 切片1 managed dependency lock）、`b616ee7`/`272a4d1`（切片2 真实 RFM adapter + static_inputs）、`632459f`（切片3 真实 pybert bundle）、`2905550`（切片4 双引擎纵向验收）、`a81a1ec`（切片5 G2b Windows 认证）、`89bfca7`（M3 完成 checkpoint）、`e222e12`（PyAMI Rust 重做决策入 PLAN）。

## Next steps

**2026-08-08 状态：M3 完成（checkpoint accepted）。解锁证据链（parity gate approved / engine bundle lock / 许可分类）已全部核验并消费完毕。**

按依赖解锁顺序推进；每切片仍须单独提交 + 独立只读审计。

1. **M5 Circuit/Link 内核收敛（PLAN §9）**（下一主方向）：
   - M5A（agent-spice 侧）：M5A-01~05 已在原仓完成（lib.rs 抽取 / Circuit API 冻结 / cargo 门禁 / oracle lane）；M5A-06/07 历史迁入（filter-repo → `engines/agent-spice` + `native/crates/sipi-circuit`）待执行；**M5A-09 PyAMI clean-room Rust 重做**（见交接备注）。
   - M5B（pybert 侧）：parity gate 已对 7 profiles approved；五步解锁链闭环（profile gate → auto 档门禁 → web 指标原生派生 → G4 S2P 决策）；下一步 Rust 成默认分档 / M5B-06/07/08 历史迁入与收编。
   - M5C：`sipi-types`/`sipi-pipeline` workspace 建立。
2. **M4-06b Rust conformance**：等 M5C Rust workspace 建立后补 Rust 侧五 DTO consumer（记录已声明 deferred，不谎称双语言）。
3. **RFM→Channel DTO 生产接线（M5B-03，含原 M3-08b）**：校验主线=agent-spice engine.lock（protocol/capabilities 断言）；S2P 通道 DTO 由 `utility/sparam.py` 产出并在 Python 边界校验；无第二套 resolver（G4 决策已落档）。
4. **M3-11 Linux/macOS 认证**：暂缓（需真实认证环境，用户决策）。

## Relevant artifacts

- `docs/baselines/audits/2026-08-07-m3.md` — M3 全部切片审计记录（含收尾-3b、M3-07b 四切片、G2b Windows 认证）
- `docs/baselines/audits/2026-08-07-m4.md` — M4 全部切片审计记录（含 wrap checkpoint）
- `docs/baselines/audits/2026-08-07-m2-platform.md` — M2 平台侧收尾与许可 owner 决策
- `PLAN.md` — 总计划（M3 §7、M4 §8、M5 §9、迁移依赖 §13.4）；M5A-09 已扩展为 PyAMI 整体 Rust 重做
- `SPEC.md` — 规格（M3 门禁、M4 退出条件 8.3/8.4、Shared Data MVP）
- `docs/adr/ADR-004-semantic-dtos-without-algorithm-unification.md` — DTO 不合并算法的边界
- `docs/migration-guide.md` — 旧命令到 `sipi` operation 映射
- `docs/baselines/conformance/m4-dto-conformance.json` — M4 DTO conformance 记录
- `schemas/authority.v1.yaml` + `fixtures/contracts/v1/rule-ledger.json` — schema 权威登记与 M1 义务账本（sha 已 LF 规范化）
- `packages/sipi-adapters/src/sipi_adapters/process.py` — 受管进程执行与 M3-11 enforcement 实现位置
- `packages/sipi-runtime/src/sipi_runtime/supervisor.py` — reconcile 与 M3 收尾-3b 恢复实现位置
- `tests/runtime/test_real_engine_vertical.py`、`tests/adapters/test_real_pybert_bundle.py`、`tests/runtime/test_two_engine_vertical.py` — 真实引擎验收证据
- `tests/adapters/test_g2b_fault_fixtures.py`、`tools/build_m3_capabilities_certified.py` — G2b Windows 认证证据与晋级工具

## Suggested skills

- `orchestration` — 后续每个切片都要走 Orca run-create/task-create/worker-start/check/worker-release + ack 的独立审计循环
- `orca-cli` — 终端/工作树/审计 worker 的低层控制
- `handoff` — 长会话继续推进时再次压缩交接

## 交接备注（给 GPT）

- 用户已授权：每切片独立提交 + 独立只读审计（OMP `term_e75ea1bb` 稳定可靠；CodeBuddy 需先确认终端健康，此前两次派发均触发 Orca runtime 断连）。
- **用户决策 2026-08-08：PyAMI（两条款 BSD、Python）统一到 Rust，随 M5A-09 clean-room 重做**（IBIS/AMI 解析器 + AMI 语义 + DLL host 接口，Rust `libloading` 加载供应商 DLL），不保留 Python 侧；旧 PyAMI 在 parity 通过前保留为 reference。已更新 PLAN §9.2 M5A-09 / §9.3 M5B-04 / §13.2 / §13.4 结论 3/5 / §14 映射（commit `e222e12`）。
- 解锁证据链完整：parity gate approved（pybert `8a6b0fe`）+ agent-spice engine.lock/bundle（`2583926e`）+ 许可分类（agent-spice `2cc92316` / agent-com `5272ffe`），全部经 SIPI 侧对照实际产物核验。
- M3/M4 平台侧无剩余可做实现项；M5 规划/实现（PLAN §9）为下一主方向。
- **会话角色转换（2026-08-08 06:30）：本会话（opencode，终端 `term_b0c8b68d`）转为独立审查 agent**——后续只做只读审查/核验，不再推进实现；实现推进由交接给 GPT 的主会话承担。审查请求经 Orca orchestration 消息投递（`orca orchestration send`，OMP 终端收件箱用 `orca orchestration check --unread --inject` 读取），审计结论经 `orca orchestration send --from <本终端> --to <请求方> --type status --subject '...审计结论' --body ...` 发回。
