# Handoff: SIPI-sim-agent M3/M4 平台侧收尾与 M3 publish-crash 恢复

## Current state

本轮（2026-08-07）目标为“继续推进 M3/M4，每轮做单独审计”。M3/M4 平台侧可做工作已全部完成并经独立审计闭环；剩余均为外部依赖项（见 Next steps）。

- **M4 平台侧完成（上轮）**：M4-09 显式 transform policies（`sipi.transform-policy.v1`，实现名机制）、M4-10 cache identity（producer role+instance+bundle 进 key）、M4 wrap checkpoint（退出条件/必测矩阵核验）。逐切片独立审计 0 P1/0 P2。
- **M3 收尾-3b（本轮）**：publish-crash 恢复。driver 两条 publish 路径统一为 staged 顺序（attempt→publishing→落盘 success-manifest→SQLite commit）；`Supervisor.reconcile` 新增 `_reconcile_publishing`——有可校验 manifest 则按文件字节 sha 重建 succeeded（CAS 幂等），无/损坏则 failed，execution/node 级 cancel 置 cancelled。不自动重跑引擎、不伪造成功。OMP 审计 0 P1/0 P2。
- **P1 修复（上轮）**：rule-ledger `authority_sha256` 改为 git blob（LF）值，`verify_m1_rule_ledger.py` 先 LF 规范化再哈希，CRLF/LF 检出结果一致。
- **审计执行者变化**：CodeBuddy 终端会话异常（只规划不执行、新终端 MCP 启动卡死），本轮复审与后续审计改由 OMP（`term_e75ea1bb`）完成；后续若需 CodeBuddy 需重建终端。
- **门禁**：`tools/run_all_tests.py` 47/47 通过；`git diff --check` 干净；工作树干净（HEAD `1e6acf2`）。
- 关键提交：`14f92d6`（M4-09）、`d9f22ed`（ledger P1 修复）、`06bd5f6`（M4-10）、`8283b09`（M4 wrap）、`868ec9c`（M3 wrap-3b）、`1e6acf2`（M3 审计记录）。

## Next steps

按依赖解锁顺序推进；每切片仍须单独提交 + 独立只读审计（OMP 可靠，CodeBuddy 需重建终端）。

1. **M3-07b/08b 真实引擎示例**（外部：managed lock + parity gate approved + 许可分类；pybert `ready: true`，agent-spice/agent-com 许可项按 owner 决策延后至 cleanroom）。解锁后替换 examples/ 中的 stub 并做三引擎纵向验收。
2. **M3-11 硬资源强制**（memory/CPU/process/artifact 的 hard enforcement）：当前仅 monitor/超时；需目标 OS 认证 enforcer + 故障 fixture（G2b 证据）。平台侧预检/声明框架已存在（capabilities preflight、resource_slice 校验）。
3. **M4-06b Rust conformance**：等 M5C Rust workspace 建立后补 Rust 侧五 DTO consumer（记录已声明 deferred，不谎称双语言）。
4. **RFM→Channel DTO 生产接线**：resolver 契约/编排已就绪；数值接线依赖 pybert G4 决策（S2P 留 Python 外部边界）与 M5B-03 拓扑决策，平台禁止写第二套 resolver。
5. **M5 Circuit/Link 内核收敛**（PLAN §9）：M5A/M5B/M5C 系列，含 agent-spice `lib.rs` 抽取、pybert 迁移收编、Rust workspace。

## Relevant artifacts

- `docs/baselines/audits/2026-08-07-m3.md` — M3 全部切片审计记录（含收尾-3b）
- `docs/baselines/audits/2026-08-07-m4.md` — M4 全部切片审计记录（含 wrap checkpoint）
- `docs/baselines/audits/2026-08-07-m2-platform.md` — M2 平台侧收尾与许可 owner 决策
- `PLAN.md` — 总计划（M3 §7、M4 §8、M5 §9、迁移依赖 §13.4）
- `SPEC.md` — 规格（M3 门禁、M4 退出条件 8.3/8.4、Shared Data MVP）
- `docs/adr/ADR-004-semantic-dtos-without-algorithm-unification.md` — DTO 不合并算法的边界
- `docs/migration-guide.md` — 旧命令到 `sipi` operation 映射
- `docs/baselines/conformance/m4-dto-conformance.json` — M4 DTO conformance 记录
- `schemas/authority.v1.yaml` + `fixtures/contracts/v1/rule-ledger.json` — schema 权威登记与 M1 义务账本（sha 已 LF 规范化）

## Suggested skills

- `orchestration` — 后续每个切片都要走 Orca run-create/task-create/worker-start/check/worker-release + ack 的独立审计循环
- `orca-cli` — 终端/工作树/审计 worker 的低层控制
- `handoff` — 长会话继续推进时再次压缩交接
