# Handoff: M5B PyBERT tiering accepted, Link integration next

## Current state

- Target repository `main` is clean at `14e91f2`; source worktree
  `C:\Users\z3312\code\.m5b-pybert-source` is clean on
  `codex/m5b-source-gates` at `975c0a7`, based on clean candidate `b3d072e`.
  Do not use or modify the dirty user-owned `C:\Users\z3312\code\Py-bert-agent`
  main worktree.
- M5B-02 is accepted. Source commits `056dd23` and `975c0a7` make
  `PYBERT_ENGINE=rust` a strict offline-golden/build-admitted tier, while
  `auto` and `compare` keep their raw-candidate behavior. It covers exactly
  seven approved profiles; this is not a Task 13 or full M5B-01 claim.
- OMP audit `msg_67f33dcd1ae8` and independent review
  `msg_64baf5602a92` both report 0 P1 / 0 P2 / 0 P3. The cross-review rebuilt
  the audited source native wheel and passed 240 tests plus all twelve frozen
  golden verifiers (the repeatability baseline is ten fixtures times ten).
- `PLAN.md` and `docs/baselines/audits/2026-08-08-m5.md` record the accepted
  tiering gate and the explicit remaining Task 13 reliability scope.
- Local source check finding to preserve: `native/pybert-core` has no tracked
  `Cargo.lock`, so `cargo --locked` at that manifest fails before execution.
  `native/pybert-python --locked` does build/test through its tracked lock.
  Rustfmt on the core also reports pre-existing formatting drift in six Rust
  files. Treat lock/format remediation as a separate M5B-01 reliability slice,
  not as evidence that the tiering change is defective.

## Next steps

1. Implement M5B-03 as a production RFM response -> existing Python external
   boundary -> M4 `resolve_channel` -> PyBERT link-core path. Keep the M4
   resolver as the sole platform resolver; do not reimplement S2P/S4P,
   conditioning, port semantics, or FFT scaling in SIPI.
2. Use the existing PyBERT `AgentSpiceChannelRunner` /
   `NativeCurrentDrivenLinkRequest` boundary as source evidence for current
   sign, port selection, and NumPy-compatible FFT/IFFT. Add a reproducible
   platform compare report with provenance before accepting M5B-03.
3. Close the remaining M5B-01 Task 13 evidence in separately committed source
   slices: core lock/format policy, nightly slow fixture, end-to-end RSS and
   performance budget, wheel corruption/remote-wheel coverage, panic/OOM and
   genuine child-process fault exercises. Do not alter golden numeric data.
4. M5B-04 retains the Python AMI host as reference; M5A's Rust host ABI is not
   AMI parity. M5B-05 remains an explicit GUI/optimizer unsupported matrix.
5. Only after M5B-01 through M5B-05 receive the large dual audits, request an
   explicit user confirmation before any PyBERT `filter-repo` work. Licence
   provenance must remain factual: PyBERT root is BSD-3-Clause while the Rust
   crate currently declares MIT; do not decide that conflict without the user
   or legal owner.

## Relevant artifacts

- `PLAN.md` -- M5B task table and migration constraints.
- `docs/baselines/audits/2026-08-08-m5.md` -- M5A records and M5B-01/02 audit.
- `docs/adr/ADR-006-inherit-pybert-v1-and-existing-handoffs.md` -- platform
  adapter boundary.
- `packages/sipi-adapters/src/sipi_adapters/channel_resolver.py` -- sole M4
  production resolver.
- `C:\Users\z3312\code\.m5b-pybert-source\docs\rust-migration\2026-08-08-s2p-topology-decision-g4.md`
  -- S2P remains the Python external boundary.
- `C:\Users\z3312\code\.m5b-pybert-source\src\pybert\engine\agent_spice_channel.py`
  and `src\pybert\engine\rust_backend.py` -- RFM current-drive and Rust link
  handoff evidence.

## Suggested skills

- `orca-cli` and `orchestration` -- send the next large-block review to the
  same OMP and independent review terminals.
- `handoff` -- compact the next long M5B work interval before transfer.
